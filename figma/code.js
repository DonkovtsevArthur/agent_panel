/* generated by scripts/build-figma.js - do not edit */
var __HTML_UI__ = [
  "<!DOCTYPE html>\n<html lang=\"en\" data-surface=\"figma\">\n  <head>\n    <meta charset=\"UTF-8\" />\n    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\" />\n    <title>Harbor Agents</title>\n    <link\n      rel=\"stylesheet\"\n      href=\"https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0&display=block\"\n    />\n    <style>\n/* Harbor Figma UI — visual subset of media/panel.css + Figma theme aliases */\n:root {\n  --vscode-foreground: var(--figma-color-text, #1e1e1e);\n  --vscode-descriptionForeground: var(--figma-color-text-secondary, #6b6b6b);\n  --vscode-editor-background: var(--figma-color-bg, #ffffff);\n  --vscode-sideBar-background: var(--figma-color-bg-secondary, #f5f5f5);\n  --vscode-input-background: var(--figma-color-bg, #ffffff);\n  --vscode-input-foreground: var(--figma-color-text, #1e1e1e);\n  --vscode-input-placeholderForeground: var(--figma-color-text-tertiary, #999);\n  --vscode-input-border: var(--figma-color-border, #e5e5e5);\n  --vscode-focusBorder: var(--figma-color-border-selected-strong, #0d99ff);\n  --vscode-button-background: var(--figma-color-bg-brand, #0d99ff);\n  --vscode-button-foreground: var(--figma-color-text-onbrand, #ffffff);\n  --vscode-textLink-foreground: var(--figma-color-text-brand, #0d99ff);\n  --vscode-editorWidget-background: var(--figma-color-bg, #ffffff);\n  --vscode-widget-border: var(--figma-color-border, #e5e5e5);\n  --vscode-editorGroup-border: var(--figma-color-border, #e5e5e5);\n  --vscode-widget-shadow: rgba(0, 0, 0, 0.35);\n  --mode-plan: #a67c00;\n  --mode-ask: #2d6a4f;\n  --mode-agent: #0d99ff;\n  --harbor-font-size: 13px;\n}\n\nhtml, body {\n  height: 100%;\n  margin: 0;\n  overflow: hidden;\n  font-size: var(--harbor-font-size, 13px);\n  background: var(--vscode-editor-background);\n  color: var(--vscode-foreground);\n}\n\n#workspaceShell.workspace-shell {\n  display: flex;\n  flex-direction: row;\n  height: 100%;\n  min-height: 0;\n  position: relative;\n}\n#workspaceShell.workspace-shell[hidden] {\n  display: none !important;\n}\n\n.empty-hint {\n  color: var(--vscode-descriptionForeground, #888);\n  text-align: center;\n  margin: auto;\n  padding: 24px;\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  gap: 12px;\n}\n.empty-hint .text-btn {\n  margin-top: 4px;\n}\n\n.settings-top {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n  padding: 6px 10px 8px;\n  flex-shrink: 0;\n  border-bottom: 1px solid color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n}\n.settings-title {\n  font-size: 12.5px;\n  font-weight: 600;\n  flex: 1;\n}\n\n/* Harbor toggle (same look as IDE mcp-switch) */\n.settings-switch {\n  position: relative;\n  display: inline-flex;\n  align-items: center;\n  flex-shrink: 0;\n  width: 34px;\n  height: 18px;\n  cursor: pointer;\n}\n.settings-switch input {\n  position: absolute;\n  opacity: 0;\n  width: 0;\n  height: 0;\n}\n.settings-switch-track {\n  width: 34px;\n  height: 18px;\n  border-radius: 999px;\n  background: color-mix(in srgb, var(--vscode-foreground) 22%, transparent);\n  transition: background 0.15s ease;\n  position: relative;\n}\n.settings-switch-track::after {\n  content: \"\";\n  position: absolute;\n  top: 2px;\n  left: 2px;\n  width: 14px;\n  height: 14px;\n  border-radius: 50%;\n  background: var(--vscode-editor-background, #fff);\n  box-shadow: 0 0 0 1px color-mix(in srgb, var(--vscode-foreground) 20%, transparent);\n  transition: transform 0.15s ease;\n}\n.settings-switch input:checked + .settings-switch-track {\n  background: var(--vscode-focusBorder, #0d99ff);\n}\n.settings-switch input:checked + .settings-switch-track::after {\n  transform: translateX(16px);\n}\n\n.status-line {\n  min-height: 14px;\n  font-size: 11px;\n  color: var(--vscode-descriptionForeground);\n  margin-top: 4px;\n  padding: 0 10px 6px;\n}\n\n/* Figma Inspect panel is always narrow — settings nav on top like compact VS Code */\nhtml[data-surface=\"figma\"] .settings-layout {\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\nhtml[data-surface=\"figma\"] .settings-nav {\n  flex: 0 0 auto;\n  width: 100%;\n  flex-direction: row;\n  flex-wrap: wrap;\n  border-right: none;\n  border-bottom: 1px solid color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n  padding-bottom: 8px;\n}\nhtml[data-surface=\"figma\"] .settings-nav-item {\n  width: auto;\n  flex: 1 1 auto;\n}\nhtml[data-surface=\"figma\"] .settings-body {\n  padding: 12px 12px 24px;\n  max-width: none;\n}\nhtml[data-surface=\"figma\"] .settings-modal-card {\n  max-width: calc(100% - 24px);\n  margin: 12px;\n}\nhtml[data-surface=\"figma\"] #settingsScreen {\n  display: flex;\n  flex-direction: column;\n  min-height: 0;\n  height: 100%;\n}\nhtml[data-surface=\"figma\"] #settingsScreen[hidden] {\n  display: none !important;\n}\n\n/* Always show delete on chat rows (narrow Inspect panel, limited hover) */\nhtml[data-surface=\"figma\"] .agent-row-wrap .row-action {\n  opacity: 1;\n  pointer-events: auto;\n}\nhtml[data-surface=\"figma\"] .agents-empty {\n  padding: 16px 10px;\n  color: var(--vscode-descriptionForeground, #888);\n  font-size: 12.5px;\n  text-align: center;\n}\n\n/* Selection chip inside composer */\nhtml[data-surface=\"figma\"] .composer .selection-preview {\n  display: flex;\n  gap: 8px;\n  align-items: center;\n  margin: 0 0 6px;\n  padding: 6px 8px;\n  border-radius: var(--radius-sm, 8px);\n  background: color-mix(in srgb, var(--vscode-foreground) 6%, transparent);\n  font-size: 12px;\n  color: var(--vscode-descriptionForeground);\n}\nhtml[data-surface=\"figma\"] .composer .selection-preview[hidden] {\n  display: none !important;\n}\nhtml[data-surface=\"figma\"] .composer .selection-preview img {\n  width: 36px;\n  height: 36px;\n  object-fit: contain;\n  border-radius: 4px;\n  background: #fff;\n  flex-shrink: 0;\n}\n\nhtml[data-surface=\"figma\"] .mode-picker[data-mode=\"ask\"] .model-label {\n  color: var(--mode-ask, #2d6a4f);\n  font-weight: 500;\n}\nhtml[data-surface=\"figma\"] .mode-picker[data-mode=\"plan\"] .model-label {\n  color: var(--mode-plan, #a67c00);\n  font-weight: 500;\n}\nhtml[data-surface=\"figma\"] .mode-picker[data-mode=\"agent\"] .model-label {\n  color: var(--",
  "mode-agent, #0d99ff);\n  font-weight: 500;\n}\nhtml[data-surface=\"figma\"] .model-menu .model-option[data-mode=\"ask\"] .model-option-label {\n  color: var(--mode-ask, #2d6a4f);\n  font-weight: 500;\n}\nhtml[data-surface=\"figma\"] .model-menu .model-option[data-mode=\"plan\"] .model-option-label {\n  color: var(--mode-plan, #a67c00);\n  font-weight: 500;\n}\nhtml[data-surface=\"figma\"] .model-menu .model-option[data-mode=\"agent\"] .model-option-label {\n  color: var(--mode-agent, #0d99ff);\n  font-weight: 500;\n}\nhtml[data-surface=\"figma\"] .model-menu.is-fixed {\n  position: fixed;\n  z-index: 10000;\n}\n\nhtml[data-surface=\"figma\"] .composer .selection-preview.is-clickable {\n  cursor: pointer;\n}\nhtml[data-surface=\"figma\"] .composer .selection-preview.is-clickable:hover {\n  background: color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n}\nhtml[data-surface=\"figma\"] .figma-tool-steps {\n  display: flex;\n  flex-direction: column;\n  gap: 4px;\n  margin: 0 0 8px;\n}\nhtml[data-surface=\"figma\"] .figma-tool-step {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 8px;\n  padding: 4px 8px;\n  border-radius: 6px;\n  font-size: 11.5px;\n  background: color-mix(in srgb, var(--vscode-foreground) 6%, transparent);\n  color: var(--vscode-descriptionForeground);\n}\nhtml[data-surface=\"figma\"] .figma-tool-step.is-ok {\n  color: var(--mode-ask, #2d6a4f);\n}\nhtml[data-surface=\"figma\"] .figma-tool-step.is-error {\n  color: #c0392b;\n}\nhtml[data-surface=\"figma\"] .figma-tool-step-label {\n  font-weight: 500;\n}\nhtml[data-surface=\"figma\"] .msg.user[data-mode=\"agent\"] {\n  border-color: color-mix(\n    in srgb,\n    var(--mode-agent, #0d99ff) var(--mode-border-user, 40%),\n    transparent\n  );\n}\nhtml[data-surface=\"figma\"] .composer[data-mode=\"agent\"] {\n  border-color: color-mix(\n    in srgb,\n    var(--mode-agent, #0d99ff) var(--mode-border-composer, 22%),\n    transparent\n  );\n}\n\n/* Safety: multi-line panel selectors must not drop .agents-top layout */\n.agents-top {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n  padding: 6px 10px 8px;\n  flex-shrink: 0;\n}\n\n/* Live «выполняю» row while a Figma turn is in flight */\nhtml[data-surface=\"figma\"] .figma-run-working {\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n  margin: 2px 0 4px;\n  padding: 2px 2px;\n  font-size: calc(var(--harbor-font-size, 13px) * 0.92);\n  color: var(--vscode-descriptionForeground, #888);\n  opacity: 0.95;\n}\nhtml[data-surface=\"figma\"] .figma-run-working-dot {\n  width: 5px;\n  height: 5px;\n  border-radius: 999px;\n  flex: none;\n  background: var(--harbor-user-tint, #8fa4b8);\n  animation: harbor-timeline-dot-pulse 1.2s ease-in-out infinite;\n}\nhtml[data-surface=\"figma\"] .figma-run-working[data-mode=\"plan\"] .figma-run-working-dot {\n  background: var(--mode-plan, #a67c00);\n}\nhtml[data-surface=\"figma\"] .figma-run-working[data-mode=\"ask\"] .figma-run-working-dot {\n  background: var(--mode-ask, #2d6a4f);\n}\nhtml[data-surface=\"figma\"] .figma-run-working[data-mode=\"agent\"] .figma-run-working-dot {\n  background: var(--mode-agent, #0d99ff);\n}\nhtml[data-surface=\"figma\"] .figma-run-working-label {\n  font-weight: 500;\n}\n\n\n:root {\n  color-scheme: light dark;\n  --radius-xs: 6px;\n  --radius-sm: 8px;\n  --radius-md: 10px;\n  --radius-lg: 14px;\n  --radius-xl: 18px;\n  --radius-pill: 999px;\n  /* Mode accent colors — mixed onto borders (user bubbles stronger, composer softer) */\n  --mode-plan: #a67c00;\n  --mode-ask: #2d6a4f;\n  --mode-border-user: 40%;\n  --mode-border-composer: 22%;\n  /* Chat: white workspace, grey bubbles (former panel bg) */\n  --harbor-chat-bg: var(--vscode-editor-background, #ffffff);\n  --harbor-bubble-bg: var(--vscode-sideBar-background);\n  /* UI: system sans like Cursor chat */\n  --harbor-font-family: var(\n    --vscode-font-family,\n    -apple-system,\n    BlinkMacSystemFont,\n    \"SF Pro\",\n    \"Segoe UI\",\n    system-ui,\n    sans-serif\n  );\n  /* Code: bundled JetBrains Mono */\n  --harbor-font-mono: \"JetBrains Mono\", var(\n    --vscode-editor-font-family,\n    ui-monospace,\n    Menlo,\n    Monaco,\n    Consolas,\n    monospace\n  );\n  --harbor-font-size: var(--vscode-font-size, 13px);\n  --harbor-user-tint: #8fa4b8;\n  --harbor-model-inset: 12px;\n}\n\n* {\n  box-sizing: border-box;\n}\n\n   html { scrollbar-color }, из‑за этого webkit игнорируется\n\n*::-webkit-scrollbar {\n  width: 6px;\n  height: 6px;\n}\n\n*::-webkit-scrollbar-track {\n  background: var(--harbor-chat-bg);\n}\n\n*::-webkit-scrollbar-thumb {\n  background: color-mix(in srgb, var(--vscode-foreground) 28%, transparent);\n  border-radius: var(--radius-pill);\n  border: 1px solid var(--harbor-chat-bg);\n  background-clip: padding-box;\n}\n\n*::-webkit-scrollbar-thumb:hover {\n  background: color-mix(in srgb, var(--vscode-foreground) 42%, transparent);\n  border: 1px solid var(--harbor-chat-bg);\n  background-clip: padding-box;\n}\n\n.material-symbols-outlined {\n  font-family: \"Material Symbols Outlined\";\n  font-weight: normal;\n  font-style: normal;\n  font-size: 16px;\n  line-height: 1;\n  letter-spacing: normal;\n  text-transform: none;\n  display: inline-block;\n  white-space: nowrap;\n  word-wrap: normal;\n  direction: ltr;\n  -webkit-font-feature-settings: \"liga\";\n  -webkit-font-smoothing: antialiased;\n  font-variation-settings: \"FILL\" 0, \"wght\" 400, \"GRAD\" 0, \"opsz\" 24;\n  color: currentColor;\n  user-select: none;\n  flex-shrink: 0;\n  speak: never;\n}\n\n*::-webkit-scrollbar-corner {\n  background: var(--harbor-chat-bg);\n}\n\n  html {\n    scrollbar-color: unset !important;\n    scrollbar-width: unset !important;\n  }\n\n  * {\n    scrollbar-width: thin;\n    scrollbar-color: color-mix(in srgb, var(--vscode-foreground) 28%, transparent)\n      var(--harbor-chat-bg);\n  }\n\nhtml,\nbody {\n  height: 100%;\n  margin: 0;\n  padding: 0;\n  font-family: var(--harbor-font-family);\n  font-size: var(--harbor-font-size);\n  color: var(--vscode-foreground);\n  background: var(--harbor-chat-bg);\n  -webkit-font-smoothing: antialiased;\n}\n\nbody {\n  display: flex;\n  flex-direction: column;\n  min-height: 0;\n}\n\n#messages {\n  flex: 1;\n  overflow-y: auto;\n  /* padding",
  "-top: 0 — иначе sticky user-сообщения оставляют щель и сквозь неё виден скролл */\n  padding: 0 16px 20px 12px;\n  scrollbar-gutter: stable;\n  display: flex;\n  flex-direction: column;\n  /* safe flex-end: прижимает сообщения к низу, когда чат короткий,\n     но при переполнении откатывается к start — иначе flex-end «unsafe»\n     прячет верхние сообщения за пределами scrollable-области и скролл\n     в теле чата не дотягивается до них. */\n  justify-content: safe flex-end;\n  gap: 14px;\n  min-height: 0;\n}\n\n.chat-turn {\n  display: flex;\n  flex-direction: column;\n  gap: 8px;\n  min-width: 0;\n}\n\n.msg {\n  white-space: pre-wrap;\n  word-break: break-word;\n  line-height: 1.45;\n  font-size: var(--harbor-font-size);\n}\n\n.msg-body {\n  min-width: 0;\n}\n\n.msg-wrap {\n  display: flex;\n  flex-direction: column;\n  align-items: flex-end;\n  align-self: flex-end;\n  max-width: calc(100% - 8px);\n  margin-right: 2px;\n  gap: 2px;\n}\n\n.msg-wrap.msg-wrap-assistant {\n  align-self: stretch;\n  align-items: flex-end;\n  max-width: calc(100% - var(--harbor-model-inset) * 2);\n  width: auto;\n  margin-left: var(--harbor-model-inset);\n  margin-right: var(--harbor-model-inset);\n  box-sizing: border-box;\n}\n\n.msg-wrap.msg-wrap-assistant > .msg-actions {\n  margin-top: 2px;\n  margin-bottom: 12px;\n}\n\n.msg-wrap.msg-wrap-user {\n  align-self: stretch;\n  max-width: 100%;\n  width: auto;\n  margin-right: 0;\n  position: sticky;\n  top: 0;\n  z-index: 6;\n  background: var(--harbor-chat-bg);\n  padding-top: 4px;\n  padding-bottom: 2px;\n  /* перекрывает субпиксельный просвет при sticky */\n  box-shadow: 0 -2px 0 1px var(--harbor-chat-bg);\n}\n\n.chat-turn:first-child .msg-wrap.msg-wrap-user {\n  padding-top: 8px;\n}\n\n.msg-wrap .msg.user {\n  align-self: stretch;\n  max-width: 100%;\n  width: auto;\n  margin-right: 0;\n}\n\n.msg-wrap .msg.assistant {\n  align-self: stretch;\n  max-width: 100%;\n  margin-right: 0;\n}\n\n.msg {\n  white-space: pre-wrap;\n}\n\n.msg.user {\n  background: color-mix(\n    in srgb,\n    var(--harbor-user-tint) 8%,\n    var(--harbor-chat-bg)\n  );\n  border: none;\n  align-self: stretch;\n  max-width: 100%;\n  width: auto;\n  margin-right: 0;\n  border-radius: var(--radius-sm);\n  padding: 7px 12px;\n  cursor: text;\n  white-space: normal;\n}\n\n.msg.user[data-mode=\"plan\"] {\n  background: color-mix(in srgb, var(--mode-plan) 10%, var(--harbor-chat-bg));\n}\n\n.msg.user[data-mode=\"ask\"] {\n  background: color-mix(in srgb, var(--mode-ask) 10%, var(--harbor-chat-bg));\n}\n\n.msg.user.is-editing {\n  padding: 0;\n  background: transparent;\n  border: none;\n  box-shadow: none;\n  border-radius: 0;\n  cursor: default;\n  width: 100%;\n  max-width: 100%;\n  align-self: stretch;\n}\n\n.msg-wrap.msg-wrap-user:has(.msg.user.is-editing) {\n  align-self: stretch;\n  max-width: 100%;\n  width: auto;\n}\n\n.msg.assistant {\n  background: transparent;\n  border: none;\n  align-self: stretch;\n  border-radius: 0;\n  padding: 2px 2px;\n  color: var(--vscode-foreground);\n  white-space: normal;\n  line-height: 1.62;\n}\n\n.msg.assistant .msg-body {\n  white-space: normal;\n}\n\n.msg-actions {\n  display: flex;\n  justify-content: flex-end;\n  gap: 2px;\n  opacity: 1;\n  pointer-events: auto;\n}\n\n.msg-actions .icon-btn.msg-regenerate,\n.msg-actions .icon-btn.msg-copy,\n.msg-actions .icon-btn.msg-branch {\n  width: 18px;\n  height: 18px;\n  min-width: 18px;\n  min-height: 18px;\n  opacity: 0.7;\n  background: transparent !important;\n  border-radius: var(--radius-xs);\n  color: var(--vscode-descriptionForeground, #888);\n  transition: color 0.12s ease, opacity 0.12s ease, background 0.12s ease;\n}\n\n.msg-actions .icon-btn.msg-regenerate:hover,\n.msg-actions .icon-btn.msg-regenerate:focus-visible,\n.msg-actions .icon-btn.msg-copy:hover,\n.msg-actions .icon-btn.msg-copy:focus-visible,\n.msg-actions .icon-btn.msg-branch:hover,\n.msg-actions .icon-btn.msg-branch:focus-visible {\n  opacity: 1;\n  background: color-mix(in srgb, var(--vscode-foreground) 12%, transparent) !important;\n  color: var(--vscode-foreground);\n}\n\n.msg-actions .icon-btn.msg-regenerate .material-symbols-outlined,\n.msg-actions .icon-btn.msg-copy .material-symbols-outlined,\n.msg-actions .icon-btn.msg-branch .material-symbols-outlined {\n  font-size: 13px;\n  line-height: 1;\n  pointer-events: none;\n}\n\n.msg-edit-composer {\n  display: flex;\n  flex-direction: column;\n  gap: 8px;\n  width: 100%;\n  padding: 12px 12px 10px;\n  border-radius: var(--radius-sm);\n  background: var(--vscode-input-background, var(--harbor-chat-bg));\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n}\n\n.msg-edit-input {\n  width: 100%;\n  /* Match #prompt sizing so the edit box reads as the same composer. */\n  min-height: calc(1.45em * 3);\n  max-height: calc(1.45em * 15);\n  overflow-y: auto;\n  resize: none;\n  background: transparent;\n  color: var(--vscode-input-foreground);\n  border: none;\n  outline: none;\n  padding: 0;\n  font-family: inherit;\n  font-size: var(--harbor-font-size);\n  line-height: 1.45;\n  box-sizing: border-box;\n}\n\n.msg-edit-input::placeholder {\n  color: var(--vscode-input-placeholderForeground, rgba(127, 127, 127, 0.8));\n}\n\n.msg-edit-footer {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n  min-width: 0;\n}\n\n.msg-edit-footer-left {\n  display: flex;\n  align-items: center;\n  gap: 4px;\n  min-width: 0;\n  flex: 1;\n  overflow: hidden;\n}\n\n.msg-edit-footer-left > .mode-picker {\n  flex-shrink: 0;\n}\n\n.msg-edit-footer-left > .composer-plus {\n  flex-shrink: 0;\n}\n\n.msg-edit-footer-left > .composer-plus .icon-btn {\n  width: 28px;\n  height: 28px;\n  flex-shrink: 0;\n  border-radius: var(--radius-pill);\n  background: color-mix(in srgb, var(--vscode-foreground) 7%, transparent);\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n}\n\n.msg-edit-footer-left > .mode-picker .model-trigger,\n.msg-edit-footer-right > .msg-edit-model-picker .model-trigger {\n  padding: 4px 10px;\n  border-radius: var(--radius-sm);\n  background: transparent;\n  border: 1px solid transparent;\n  color: var(--vscode-foreground);\n}\n\n.msg-edit-footer-left > .mode-picker .model-trigger:hover:not(:d",
  "isabled),\n.msg-edit-footer-left > .mode-picker.is-open .model-trigger,\n.msg-edit-footer-right > .msg-edit-model-picker .model-trigger:hover:not(:disabled),\n.msg-edit-footer-right > .msg-edit-model-picker.is-open .model-trigger {\n  background: color-mix(in srgb, var(--vscode-foreground) 7%, transparent);\n  border-color: color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n}\n\n.msg-edit-footer-right > .msg-edit-model-picker {\n  flex: 0 1 auto;\n  min-width: 0;\n}\n\n.msg-edit-footer-right > .reason-picker {\n  flex: 0 0 auto;\n  max-width: none;\n}\n\n.msg-edit-footer-right > .reason-picker .model-trigger {\n  padding: 4px 10px;\n  border-radius: var(--radius-sm);\n  background: transparent;\n  border: none;\n  outline: 1px solid transparent;\n  outline-offset: 0;\n  color: var(--vscode-foreground);\n}\n\n.msg-edit-footer-right > .reason-picker .model-trigger:hover:not(:disabled),\n.msg-edit-footer-right > .reason-picker.is-open .model-trigger {\n  background: color-mix(in srgb, var(--vscode-foreground) 9%, transparent);\n  outline-color: color-mix(in srgb, var(--vscode-foreground) 18%, transparent);\n  color: var(--vscode-foreground);\n}\n\n.msg-edit-model-menu,\n.msg-edit-mode-menu {\n  z-index: 1100;\n}\n\n.msg-edit-mode-picker .model-option {\n  align-items: flex-start;\n}\n\n.msg-edit-mode-picker .model-option-label {\n  font-weight: 500;\n}\n\n.msg-edit-footer-right {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n  flex-shrink: 0;\n  position: relative;\n  z-index: 2;\n}\n\n.msg.assistant .md-code {\n  font-family: var(--harbor-font-mono);\n  font-size: 0.92em;\n  padding: 1px 5px;\n  border-radius: var(--radius-xs);\n  background: color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n  color: var(--vscode-foreground);\n}\n\n.md-pre-wrap {\n  position: relative;\n  margin: 6px 0;\n}\n\n.md-pre-meta {\n  display: flex;\n  flex-wrap: wrap;\n  align-items: center;\n  gap: 6px;\n  margin: 0 0 4px;\n  padding: 0 2px;\n  font-size: 11px;\n  color: var(--vscode-descriptionForeground, #888);\n}\n\n.md-pre-meta-main {\n  display: flex;\n  flex-wrap: wrap;\n  align-items: center;\n  gap: 6px;\n  min-width: 0;\n}\n\n.md-file-icon {\n  width: 23px;\n  height: 17px;\n  font-size: 7px;\n}\n\n.md-pre-toggle {\n  justify-content: space-between;\n  padding: 5px 7px;\n  border-radius: var(--radius-sm);\n  cursor: pointer;\n  user-select: none;\n}\n\n.md-pre-toggle:hover {\n  color: var(--vscode-foreground);\n  background: color-mix(in srgb, var(--vscode-foreground) 6%, transparent);\n}\n\n.md-pre-toggle:focus-visible {\n  outline: 1px solid var(--vscode-focusBorder);\n  outline-offset: 1px;\n}\n\n.md-pre-chevron {\n  flex: 0 0 auto;\n  font-size: 18px;\n  transition: transform 120ms ease;\n}\n\n.md-pre-wrap.is-collapsed .md-pre {\n  display: none;\n}\n\n.md-pre-wrap.is-collapsed .md-pre-meta {\n  margin-bottom: 0;\n}\n\n.md-pre-wrap:not(.is-collapsed) .md-pre-chevron {\n  transform: rotate(180deg);\n}\n\n.md-pre-meta-sep {\n  opacity: 0.55;\n}\n\n.md-pre-lines {\n  font-variant-numeric: tabular-nums;\n}\n\n.md-pre-path {\n  word-break: break-all;\n}\n\n.msg.assistant .md-pre,\n.msg.user .md-pre {\n  margin: 0;\n  padding: 8px 10px;\n  border-radius: var(--radius-sm);\n  overflow-x: auto;\n  border: 1px solid var(--vscode-editorGroup-border);\n  background: var(\n    --vscode-textCodeBlock-background,\n    var(--vscode-editor-background)\n  );\n  color: var(--vscode-editor-foreground);\n  font-family: var(--harbor-font-mono);\n  font-size: 0.9em;\n  white-space: pre-wrap;\n  word-break: break-word;\n}\n\n.md-pre-wrap.has-lines .md-pre {\n  white-space: normal;\n  padding-left: 0;\n}\n\n.md-pre-wrap.has-lines code {\n  display: block;\n  white-space: pre;\n}\n\n.md-line {\n  display: flex;\n  align-items: flex-start;\n  min-width: 0;\n}\n\n.md-ln {\n  flex: 0 0 auto;\n  width: 3.2em;\n  padding: 0 8px 0 6px;\n  margin-right: 8px;\n  text-align: right;\n  user-select: none;\n  opacity: 0.45;\n  border-right: 1px solid color-mix(in srgb, var(--vscode-foreground) 14%, transparent);\n  font-variant-numeric: tabular-nums;\n}\n\n.md-line-text {\n  flex: 1 1 auto;\n  min-width: 0;\n  white-space: pre-wrap;\n  word-break: break-word;\n}\n\n.md-pre .syntax-keyword {\n  color: var(\n    --vscode-symbolIcon-keywordForeground,\n    var(--vscode-textLink-foreground)\n  );\n}\n\n.md-pre .syntax-literal {\n  color: var(\n    --vscode-debugTokenExpression-boolean,\n    var(\n      --vscode-symbolIcon-booleanForeground,\n      var(--vscode-editor-foreground)\n    )\n  );\n}\n\n.md-pre .syntax-string {\n  color: var(\n    --vscode-debugTokenExpression-string,\n    var(--vscode-symbolIcon-stringForeground, var(--vscode-editor-foreground))\n  );\n}\n\n.md-pre .syntax-number {\n  color: var(\n    --vscode-debugTokenExpression-number,\n    var(--vscode-symbolIcon-numberForeground, var(--vscode-editor-foreground))\n  );\n}\n\n.md-pre .syntax-comment {\n  color: var(\n    --vscode-editorLineNumber-foreground,\n    var(--vscode-descriptionForeground)\n  );\n  font-style: italic;\n}\n\n.md-pre .syntax-type {\n  color: var(\n    --vscode-symbolIcon-classForeground,\n    var(--vscode-editor-foreground)\n  );\n}\n\n.md-pre .syntax-function {\n  color: var(\n    --vscode-symbolIcon-functionForeground,\n    var(--vscode-editor-foreground)\n  );\n}\n\n.md-pre .syntax-variable,\n.md-pre .syntax-property {\n  color: var(--vscode-editor-foreground);\n}\n\nbody.vscode-light .md-pre .syntax-keyword {\n  color: #af00db;\n}\n\nbody.vscode-light .md-pre .syntax-literal {\n  color: #0000ff;\n}\n\nbody.vscode-light .md-pre .syntax-string {\n  color: #a31515;\n}\n\nbody.vscode-light .md-pre .syntax-number {\n  color: #098658;\n}\n\nbody.vscode-light .md-pre .syntax-comment {\n  color: #008000;\n}\n\nbody.vscode-light .md-pre .syntax-type {\n  color: #267f99;\n}\n\nbody.vscode-light .md-pre .syntax-function {\n  color: #795e26;\n}\n\nbody.vscode-light .md-pre .syntax-variable,\nbody.vscode-light .md-pre .syntax-property {\n  color: #001080;\n}\n\nbody.vscode-dark .md-pre .syntax-keyword {\n  color: #c586c0;\n}\n\nbody.vscode-dark .md-pre .syntax-literal {\n  color: #569cd6;\n}\n\nbody.vscode-dark .md-pre .syntax-string {\n  color: #ce9178;\n}\n\nbody.vscode-dark .md-pre .syntax-number {\n  color: #b5cea8",
  ";\n}\n\nbody.vscode-dark .md-pre .syntax-comment {\n  color: #6a9955;\n}\n\nbody.vscode-dark .md-pre .syntax-type {\n  color: #4ec9b0;\n}\n\nbody.vscode-dark .md-pre .syntax-function {\n  color: #dcdcaa;\n}\n\nbody.vscode-dark .md-pre .syntax-variable,\nbody.vscode-dark .md-pre .syntax-property {\n  color: #9cdcfe;\n}\n\n.msg.user .msg-body {\n  white-space: normal;\n  width: auto;\n  max-width: 100%;\n}\n\n.msg.user.is-plan-implement .msg-body {\n  display: flex;\n  align-items: center;\n}\n\n.msg.user .plan-implement-chip {\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n  font-size: 13px;\n  font-weight: 500;\n  line-height: 1.3;\n}\n\n.msg.user .plan-implement-chip .material-symbols-outlined {\n  font-size: 18px;\n  font-variation-settings: \"FILL\" 0, \"wght\" 400, \"GRAD\" 0, \"opsz\" 24;\n}\n\n.msg.user .md-p,\n.msg.user .md-pre-wrap,\n.msg.user .msg-text,\n.msg.user .msg-file-chip {\n  max-width: 100%;\n}\n\n.msg.user .md-p {\n  margin: 0 0 6px;\n}\n\n.msg.user .md-p:last-child {\n  margin-bottom: 0;\n}\n\n.msg.assistant .md-pre code {\n  font-family: inherit;\n  font-size: inherit;\n  padding: 0;\n  background: transparent;\n}\n\n.msg.assistant .md-strong {\n  font-weight: 600;\n  color: var(--vscode-foreground);\n}\n\n.msg.assistant .md-em {\n  font-style: italic;\n}\n\n.msg.assistant .md-del {\n  text-decoration: line-through;\n  opacity: 0.85;\n}\n\n.msg.assistant .md-p {\n  margin: 0 0 8px;\n  white-space: pre-wrap;\n  word-break: break-word;\n}\n\n.msg.assistant .md-p:last-child {\n  margin-bottom: 0;\n}\n\n.msg.assistant .md-list {\n  margin: 0 0 8px;\n  padding-left: 1.35em;\n}\n\n.msg.assistant .md-list:last-child {\n  margin-bottom: 0;\n}\n\n.msg.assistant .md-li {\n  margin: 0 0 4px;\n  padding-left: 0.15em;\n}\n\n.msg.assistant .md-li:last-child {\n  margin-bottom: 0;\n}\n\n.msg.assistant .md-quote {\n  margin: 0 0 8px;\n  padding: 4px 0 4px 10px;\n  border-left: 2px solid color-mix(in srgb, var(--vscode-foreground) 25%, transparent);\n  color: var(--vscode-descriptionForeground, #888);\n}\n\n.msg.assistant .md-quote:last-child {\n  margin-bottom: 0;\n}\n\n.msg.assistant .md-hr {\n  margin: 10px 0;\n  border: none;\n  border-top: 1px solid color-mix(in srgb, var(--vscode-foreground) 18%, transparent);\n}\n\n.msg.assistant .md-link {\n  color: var(--vscode-textLink-foreground, #006ab1);\n  text-decoration: none;\n  cursor: pointer;\n}\n\n.msg.assistant .md-link:hover {\n  color: var(--vscode-textLink-activeForeground, #006ab1);\n  text-decoration: underline;\n}\n\n.msg.assistant .md-file {\n  font-family: var(--harbor-font-mono);\n  font-size: 0.92em;\n  padding: 1px 5px;\n  border-radius: var(--radius-xs);\n  background: color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n  color: var(--vscode-textLink-foreground, #006ab1);\n  text-decoration: none;\n  cursor: pointer;\n}\n\n.msg.assistant .md-file:hover {\n  text-decoration: underline;\n  color: var(--vscode-textLink-activeForeground, #006ab1);\n}\n\n.msg.tool .md-link,\n.msg.error .md-link {\n  color: var(--vscode-textLink-foreground, #006ab1);\n  text-decoration: none;\n  cursor: pointer;\n  word-break: break-all;\n}\n\n.msg.tool .md-link:hover,\n.msg.error .md-link:hover {\n  color: var(--vscode-textLink-activeForeground, #006ab1);\n  text-decoration: underline;\n}\n\n.msg.tool .tool-output {\n  white-space: normal;\n}\n\n.msg.assistant .md-h {\n  font-weight: 600;\n  color: var(--vscode-foreground);\n  margin: 6px 0 2px;\n  line-height: 1.35;\n}\n\n.msg.assistant .md-h1 {\n  font-size: 1.15em;\n}\n\n.msg.assistant .md-h2 {\n  font-size: 1.08em;\n}\n\n.msg.assistant .md-h3 {\n  font-size: 1.02em;\n  opacity: 0.95;\n}\n\n.msg.assistant br + .md-h {\n  margin-top: 8px;\n}\n\n.msg.assistant .md-table-wrap {\n  margin: 8px 0;\n  overflow-x: auto;\n  max-width: 100%;\n}\n\n.msg.assistant .md-table-wrap:last-child {\n  margin-bottom: 0;\n}\n\n.msg.assistant .md-table {\n  width: max-content;\n  max-width: 100%;\n  border-collapse: collapse;\n  font-size: 0.92em;\n  line-height: 1.4;\n}\n\n.msg.assistant .md-td {\n  padding: 5px 8px;\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 16%, transparent);\n  vertical-align: top;\n  word-break: break-word;\n}\n\n.msg.assistant .md-table th.md-td {\n  font-weight: 600;\n  background: color-mix(in srgb, var(--vscode-foreground) 8%, transparent);\n  white-space: nowrap;\n}\n\n.msg.assistant .md-task {\n  margin: 0 4px 0 0;\n  vertical-align: middle;\n}\n\n.msg.assistant .md-li .md-p {\n  margin: 0;\n  display: inline;\n}\n\n.msg.assistant .md-quote .md-p:last-child {\n  margin-bottom: 0;\n}\n\n.msg.system,\n.msg.tool {\n  background: transparent;\n  border: none;\n  border-radius: 0;\n  padding: 2px 2px;\n  opacity: 0.65;\n  font-size: 12px;\n  color: var(--vscode-descriptionForeground, #888);\n}\n\n.tool-group {\n  display: flex;\n  flex-direction: column;\n  align-items: flex-start;\n  gap: 2px;\n  align-self: stretch;\n  max-width: 100%;\n}\n\n.tool-group-toggle {\n  display: inline-flex;\n  align-items: center;\n  gap: 4px;\n  margin: 0;\n  padding: 0;\n  border: none;\n  border-radius: 0;\n  background: transparent;\n  box-shadow: none;\n  color: var(--vscode-descriptionForeground, #888);\n  font: inherit;\n  font-size: 11px;\n  line-height: 1.35;\n  cursor: pointer;\n  opacity: 0.9;\n  appearance: none;\n  -webkit-appearance: none;\n}\n\n.tool-group-toggle[hidden] {\n  display: none !important;\n}\n\n.tool-group-toggle:hover:not(:disabled) {\n  opacity: 1;\n  color: var(--vscode-foreground);\n}\n\n.tool-group-toggle:disabled {\n  cursor: default;\n  opacity: 0.9;\n}\n\n.tool-group-toggle:focus,\n.tool-group-toggle:focus-visible {\n  outline: none;\n  box-shadow: none;\n  border: none;\n  background: transparent;\n}\n\n.tool-group-summary {\n  display: inline;\n  order: 1;\n  font-variant-numeric: tabular-nums;\n}\n\n.tool-group-chevron {\n  order: 2;\n  font-size: 14px;\n  transition: transform 0.12s ease;\n}\n\n.tool-group-duration {\n  order: 3;\n  font-variant-numeric: tabular-nums;\n  white-space: nowrap;\n  color: var(--vscode-descriptionForeground, #888);\n}\n\n.tool-group-duration[hidden] {\n  display: none !important;\n}\n\n.tool-group-chevron[hidden],\n.tool-group:not(.has-steps) .tool-group-chevron {\n  display: none !important;\n}\n\n.tool-group:not(.i",
  "s-collapsed) .tool-group-chevron {\n  transform: rotate(180deg);\n}\n\n.tool-group-body {\n  display: flex;\n  flex-direction: column;\n  gap: 2px;\n  width: 100%;\n  padding-left: 2px;\n}\n\n.tool-group.is-collapsed .tool-group-body {\n  display: none;\n}\n\n.chat-turn > .tool-group,\n.chat-turn > .reasoning-group,\n.chat-turn > .msg.assistant,\n.chat-turn > .msg.error,\n.chat-turn > .msg.system,\n.chat-turn > .agent-status {\n  margin-left: var(--harbor-model-inset);\n  margin-right: var(--harbor-model-inset);\n  max-width: calc(100% - var(--harbor-model-inset) * 2);\n  width: auto;\n  align-self: stretch;\n  box-sizing: border-box;\n}\n\n.tool-group.agent-timeline {\n  gap: 0;\n  margin-top: 2px;\n  margin-bottom: 4px;\n  padding-left: 2px;\n}\n\n.tool-group.agent-timeline:not(.is-collapsed) {\n  border-left: 1px solid color-mix(in srgb, var(--vscode-foreground) 7%, transparent);\n}\n\n.tool-group.agent-timeline .tool-group-toggle {\n  margin-left: -2px;\n  opacity: 0.9;\n}\n\n.tool-group.agent-timeline .tool-group-toggle::before {\n  content: \"\";\n  width: 5px;\n  height: 5px;\n  border-radius: 999px;\n  background: var(--vscode-descriptionForeground, #888);\n  opacity: 0.6;\n  flex: none;\n}\n\n.tool-group.agent-timeline:not([data-sealed]) .tool-group-toggle::before,\n.tool-group.agent-timeline.has-steps.is-run-working .tool-group-toggle::before {\n  background: var(--harbor-user-tint, #8fa4b8);\n  opacity: 1;\n  animation: harbor-timeline-dot-pulse 1.2s ease-in-out infinite;\n}\n\n  .tool-group-toggle::before {\n  background: var(--mode-plan);\n}\n\n  .tool-group-toggle::before {\n  background: var(--mode-ask);\n}\n\n  .tool-group-toggle::before {\n  background: #3fa76a;\n  opacity: 0.9;\n  animation: none;\n}\n\n.tool-group.agent-timeline[data-failed].has-steps .tool-group-toggle::before {\n  background: var(--vscode-errorForeground, #f44747);\n  opacity: 1;\n  animation: none;\n}\n\n@keyframes harbor-timeline-dot-pulse {\n  0%,\n  100% {\n    opacity: 0.35;\n  }\n  50% {\n    opacity: 1;\n  }\n}\n\n.tool-group.agent-timeline .tool-group-chevron {\n  font-size: 15px;\n}\n\n.tool-group.agent-timeline.is-collapsed .tool-group-chevron {\n  transform: rotate(-90deg);\n}\n\n.tool-group.agent-timeline:not(.is-collapsed) .tool-group-chevron {\n  transform: rotate(0deg);\n}\n\n.tool-group.agent-timeline .tool-group-body,\n.agent-timeline-body {\n  display: flex;\n  flex-direction: column;\n  gap: 7px;\n  width: 100%;\n  padding: 0 0 0 9px;\n}\n\n.tool-group-live-note {\n  display: none;\n  margin: 3px 0 1px 7px;\n  font-size: calc(var(--harbor-font-size) * 0.92);\n  line-height: 1.45;\n  color: var(--vscode-descriptionForeground, #888);\n  opacity: 0.85;\n  overflow-wrap: anywhere;\n  max-width: 100%;\n}\n\n  .tool-group-live-note:not(:empty) {\n  display: block;\n}\n\n.tool-group.agent-timeline.is-collapsed .tool-group-body,\n.tool-group.agent-timeline.is-collapsed .agent-timeline-body {\n  display: none;\n}\n\n.agent-step,\n.tool-group .agent-step.msg.tool {\n  display: flex;\n  align-items: flex-start;\n  gap: 8px;\n  opacity: 0.9;\n  font-size: calc(var(--harbor-font-size) * 0.9);\n  line-height: 1.5;\n  color: var(--vscode-descriptionForeground, #888);\n  padding: 0;\n  max-width: 100%;\n}\n\n.agent-step-thinking {\n  flex-direction: column;\n  gap: 2px;\n  color: var(--vscode-descriptionForeground, #888);\n  opacity: 0.95;\n  background: transparent;\n  border: none;\n}\n\n.agent-step-head {\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n  color: var(--vscode-descriptionForeground, #888);\n  font-size: calc(var(--harbor-font-size) * 0.9);\n}\n\n.agent-step-thinking-text {\n  white-space: pre-wrap;\n  overflow-wrap: anywhere;\n  padding-left: 22px;\n  color: var(--vscode-descriptionForeground, #aaa);\n  font-size: calc(var(--harbor-font-size) * 0.9);\n  line-height: 1.5;\n}\n\n.agent-step-thinking.is-thinking-collapsed .agent-step-thinking-text {\n  display: none;\n}\n\n.agent-step-thinking-toggle {\n  display: inline-flex;\n  align-items: center;\n  margin: 0;\n  padding: 0;\n  border: none;\n  background: transparent;\n  box-shadow: none;\n  color: var(--vscode-descriptionForeground, #888);\n  font: inherit;\n  font-size: 11px;\n  cursor: pointer;\n  opacity: 0.85;\n  appearance: none;\n  -webkit-appearance: none;\n}\n\n.agent-step-thinking-toggle:hover {\n  opacity: 1;\n  color: var(--vscode-foreground);\n}\n\n.agent-step-thinking-toggle:focus,\n.agent-step-thinking-toggle:focus-visible {\n  outline: none;\n  box-shadow: none;\n}\n\n.agent-step-thinking-toggle[hidden] {\n  display: none;\n}\n\n.agent-step-text {\n  color: var(--vscode-descriptionForeground, #aaa);\n  background: transparent;\n}\n\n.agent-step-text .agent-step-label {\n  color: var(--vscode-descriptionForeground, #aaa);\n  line-height: 1.5;\n}\n\n.agent-step-text p,\n.agent-step-text ul,\n.agent-step-text ol {\n  margin: 0;\n}\n\n.agent-step[data-step-kind=\"tool\"] .agent-step-label {\n  color: var(--vscode-descriptionForeground, #888);\n}\n\n.agent-step-description {\n  padding-left: 22px;\n  color: var(--vscode-descriptionForeground, #aaa);\n  font-size: calc(var(--harbor-font-size) * 0.9);\n  line-height: 1.5;\n  overflow-wrap: anywhere;\n  margin-top: 2px;\n}\n\n.agent-step-description p,\n.agent-step-description ul,\n.agent-step-description ol {\n  margin: 0 0 4px;\n}\n\n.agent-step-description > :last-child {\n  margin-bottom: 0;\n}\n\n.agent-step-icon {\n  font-size: 14px;\n  margin-top: 1px;\n  flex-shrink: 0;\n  opacity: 0.85;\n}\n\n.agent-step[data-status=\"running\"] .agent-step-icon {\n  animation: agent-step-spin 0.9s linear infinite;\n}\n\n.agent-step[data-status=\"done\"] {\n  opacity: 0.75;\n}\n\n.agent-step[data-status=\"error\"] {\n  color: var(--vscode-errorForeground, #f44747);\n  opacity: 1;\n}\n\n.agent-step[data-step-kind=\"compaction\"],\n.agent-step[data-step-kind=\"retry\"] {\n  opacity: 0.85;\n  font-style: italic;\n}\n\n.agent-step-label {\n  min-width: 0;\n  overflow-wrap: anywhere;\n}\n\n.agent-step-duration {\n  margin-left: 0.45em;\n  color: var(--vscode-descriptionForeground, rgba(128, 128, 128, 0.9));\n  font-variant-numeric: tabular-nums;\n  white-space: nowrap;\n}\n\n.agent-step-error {\n  color: var(--vscode-errorForeground, #f44747);\n  font-weight: 500;\n",
  "}\n\n.tool-group .agent-step.msg.tool.agent-step-todo,\n.agent-step.agent-step-todo {\n  display: flex;\n  flex-direction: column;\n  align-items: stretch;\n  gap: 4px;\n  padding: 4px 8px;\n  border-radius: 4px;\n  background: var(--vscode-editorWidget-background, var(--harbor-chat-bg));\n  border: 1px solid var(--vscode-widget-border, rgba(128, 128, 128, 0.25));\n  /* Прилипает к верху при скролле. top задаётся через CSS-переменную\n     --harbor-todo-top, которая равна высоте user-пузыря (выставляется из JS\n     через ResizeObserver). Если переменной нет (нет user-сообщения) —\n     карточка прилипает к top:0. */\n  position: sticky;\n  top: var(--harbor-todo-top, 0px);\n  z-index: 5;\n}\n\n.agent-step.agent-step-todo .agent-step-icon {\n  animation: none;\n}\n\n.agent-step-todo[data-status=\"error\"] .todo-plan-item[data-todo-status=\"in_progress\"] .todo-plan-item-icon {\n  animation: none;\n}\n\n.msg.error {\n  background: transparent;\n  border: none;\n  border-radius: 0;\n  padding: 2px 2px;\n  color: var(--vscode-errorForeground);\n  white-space: normal;\n}\n\n.msg.error .msg-body {\n  white-space: normal;\n}\n\n.msg.error .msg-error-row {\n  display: flex;\n  align-items: flex-start;\n  gap: 4px;\n  min-width: 0;\n}\n\n.msg.error .msg-error-text {\n  flex: 1 1 auto;\n  min-width: 0;\n}\n\n.msg.error .msg-error-help {\n  flex-shrink: 0;\n  width: 20px;\n  height: 20px;\n  margin-top: 1px;\n  opacity: 0.55;\n  background: transparent;\n  color: inherit;\n}\n\n.msg.error .msg-error-help .material-symbols-outlined {\n  font-size: 16px;\n  line-height: 1;\n}\n\n.msg.error .msg-error-help:hover,\n.msg.error .msg-error-help:focus-visible,\n.msg.error .msg-error-help[aria-expanded=\"true\"] {\n  opacity: 1;\n  background: transparent;\n}\n\n.msg.error .msg-error-detail {\n  margin: 6px 0 0;\n  padding: 8px 10px;\n  max-height: 220px;\n  overflow: auto;\n  white-space: pre-wrap;\n  word-break: break-word;\n  font-family: var(--vscode-editor-font-family, ui-monospace, monospace);\n  font-size: 11px;\n  line-height: 1.45;\n  color: var(--vscode-descriptionForeground, #888);\n  background: var(--vscode-textCodeBlock-background, rgba(127, 127, 127, 0.12));\n  border-radius: var(--radius-sm);\n}\n\n.msg.error .md-p,\n.msg.error .md-list,\n.msg.error .md-quote,\n.msg.error .md-table {\n  color: inherit;\n}\n\n.agent-status {\n  padding: 2px 2px 4px;\n  font-size: 12px;\n  color: var(--vscode-descriptionForeground, #888);\n  min-height: 16px;\n  align-self: flex-start;\n  max-width: 100%;\n}\n\n.agent-status-in-messages {\n  margin-top: 2px;\n  margin-left: var(--harbor-model-inset);\n  margin-right: var(--harbor-model-inset);\n}\n\n.agent-status:not([hidden]) {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n}\n\n.agent-status-model {\n  color: var(--vscode-disabledForeground, #6e6e6e);\n  font-weight: 400;\n}\n\n.agent-status:not([hidden])::before {\n  content: \"\";\n  width: 6px;\n  height: 6px;\n  border-radius: 50%;\n  background: var(--vscode-focusBorder, #007fd4);\n  animation: pulse 1s ease-in-out infinite;\n}\n\n.agent-status[data-phase=\"reading\"]::before,\n.agent-status[data-phase=\"listing\"]::before {\n  background: var(--vscode-charts-blue, #3794ff);\n}\n\n.agent-status[data-phase=\"running\"]::before {\n  background: var(--vscode-charts-orange, #e2a03f);\n}\n\n.agent-status[data-phase=\"editing\"]::before {\n  background: var(--vscode-charts-green, #89d185);\n}\n\n.agent-status[data-phase=\"done\"]::before {\n  background: var(--vscode-charts-green, #89d185);\n  animation: none;\n  opacity: 1;\n}\n\n.agent-status[data-phase=\"cline\"]::before {\n  background: var(--vscode-charts-purple, #b180d7);\n}\n\n.composer-plan-actions,\n\n.composer-plan-actions[hidden],\n\n.composer-plan-build {\n  background: color-mix(in srgb, var(--vscode-button-background, #0e639c) 22%, transparent);\n}\n\n.composer-plan-build:hover {\n  border-color: color-mix(in srgb, var(--vscode-foreground) 30%, transparent);\n  background: color-mix(in srgb, var(--vscode-button-background, #0e639c) 32%, transparent);\n}\n\n.composer-plan-build:disabled {\n  opacity: 0.55;\n  cursor: default;\n}\n\n.composer-wrap {\n  padding: 10px 10px 10px;\n  position: relative;\n  z-index: 30;\n  overflow: visible;\n  min-width: 0;\n  max-width: 100%;\n  box-sizing: border-box;\n}\n\n.composer {\n  display: flex;\n  flex-direction: column;\n  gap: 7px;\n  padding: 9px 11px 8px;\n  border-radius: var(--radius-md);\n  background: var(--vscode-input-background, var(--harbor-chat-bg));\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n  overflow: visible;\n  position: relative;\n}\n\n.composer[data-mode=\"plan\"],\n.msg-edit-composer[data-mode=\"plan\"],\n.msg-edit-composer:has(.msg-edit-mode-picker[data-mode=\"plan\"]) {\n  border-color: color-mix(\n    in srgb,\n    var(--mode-plan) var(--mode-border-composer),\n    transparent\n  );\n}\n\n.composer[data-mode=\"ask\"],\n.msg-edit-composer[data-mode=\"ask\"],\n.msg-edit-composer:has(.msg-edit-mode-picker[data-mode=\"ask\"]) {\n  border-color: color-mix(\n    in srgb,\n    var(--mode-ask) var(--mode-border-composer),\n    transparent\n  );\n}\n\n.composer-wrap.is-drop-target .composer,\n.composer.is-drop-target {\n  border-color: var(--vscode-focusBorder, #007fd4);\n  box-shadow: inset 0 0 0 1px var(--vscode-focusBorder, #007fd4);\n}\n\n.chat-screen.is-file-drag .composer {\n  border-color: var(--vscode-focusBorder, #007fd4);\n  box-shadow: inset 0 0 0 1px var(--vscode-focusBorder, #007fd4);\n}\n\n.composer-drop-hint {\n  position: absolute;\n  inset: 0;\n  z-index: 5;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  padding: 12px;\n  border-radius: inherit;\n  background: color-mix(\n    in srgb,\n    var(--vscode-editorWidget-background, #252526) 88%,\n    transparent\n  );\n  border: 1px dashed var(--vscode-focusBorder, #007fd4);\n  pointer-events: none;\n  text-align: center;\n}\n\n.composer-drop-hint[hidden] {\n  display: none !important;\n}\n\n.composer-drop-hint-text {\n  font-size: 12px;\n  line-height: 1.35;\n  color: var(--vscode-foreground);\n}\n\n.composer-drop-hint kbd {\n  display: inline-block;\n  padding: 1px 5px;\n  margin: 0 2px;\n  border-radius: var(--radius-xs);\n  border: 1px s",
  "olid color-mix(in srgb, var(--vscode-foreground) 25%, transparent);\n  background: color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n  font-family: inherit;\n  font-size: 11px;\n}\n\n.selection-preview {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 6px;\n  margin-bottom: 6px;\n}\n\n.selection-preview[hidden] {\n  display: none !important;\n}\n\n.composer-mention-chip {\n  margin: 0;\n  cursor: pointer;\n}\n\n.composer-mention-chip .attach-chip-remove {\n  margin-left: 0;\n}\n\n.msg-attachments {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 6px;\n  margin-bottom: 8px;\n}\n\n.msg.user.has-attach .msg-body {\n  display: flex;\n  flex-direction: column;\n  align-items: flex-start;\n  gap: 8px;\n  width: 100%;\n  max-width: 100%;\n}\n\n.msg.user .msg-attachments .msg-file-chip {\n  margin: 0;\n}\n\n.msg.user.has-attach .msg-attachments {\n  margin: 0;\n  flex-shrink: 0;\n}\n\n.msg.user.has-attach .msg-text {\n  min-width: 0;\n  width: 100%;\n}\n\n.msg.user.has-attach .msg-attach-image img {\n  width: auto;\n  height: auto;\n  max-width: min(240px, 100%);\n  max-height: 160px;\n  object-fit: contain;\n  background: color-mix(in srgb, var(--vscode-foreground) 4%, transparent);\n}\n\n.msg-attach {\n  display: inline-flex;\n  align-items: center;\n  gap: 4px;\n  max-width: 100%;\n  padding: 4px 8px;\n  border-radius: var(--radius-md);\n  background: color-mix(in srgb, var(--vscode-foreground) 7%, transparent);\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n  font-size: 11px;\n  line-height: 1.2;\n}\n\n.msg-attach .material-symbols-outlined {\n  font-size: 14px;\n  opacity: 0.8;\n}\n\n.msg-attach-image {\n  position: relative;\n  flex-direction: column;\n  align-items: flex-start;\n  padding: 0;\n  gap: 0;\n  background: color-mix(in srgb, var(--vscode-foreground) 4%, transparent);\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n  border-radius: var(--radius-lg);\n  overflow: hidden;\n  cursor: zoom-in;\n  font: inherit;\n  color: inherit;\n}\n\n.msg-attach-image img {\n  display: block;\n  max-width: min(220px, 100%);\n  max-height: 160px;\n  border-radius: var(--radius-lg);\n  object-fit: contain;\n}\n\n.msg-attach-image-badge {\n  position: absolute;\n  right: 4px;\n  bottom: 4px;\n  width: 18px;\n  height: 18px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  border-radius: var(--radius-pill);\n  background: color-mix(\n    in srgb,\n    var(--vscode-editorWidget-background, #252526) 82%,\n    transparent\n  );\n  color: var(--vscode-foreground);\n  pointer-events: none;\n}\n\n.msg-attach-image-badge .material-symbols-outlined {\n  font-size: 12px;\n}\n\n.msg-attach-name {\n  min-width: 0;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  max-width: 200px;\n  opacity: 0.85;\n}\n\n.msg-edit-composer .msg-attachments {\n  margin: 8px 8px 0;\n}\n\n.msg-edit-composer .msg-attachments.msg-edit-attach-preview {\n  margin: 0;\n}\n\n.composer > .context-meter {\n  position: absolute;\n  top: 10px;\n  right: 12px;\n  z-index: 41;\n}\n\n#prompt {\n  width: 100%;\n  /* ~3 lines min, up to ~15 lines — grow with content via autoResizePrompt */\n  min-height: calc(1.45em * 3);\n  max-height: calc(1.45em * 15);\n  overflow-y: auto;\n  resize: none;\n  background: transparent;\n  color: var(--vscode-input-foreground);\n  border: none;\n  outline: none;\n  padding: 0;\n  font-family: inherit;\n  font-size: var(--harbor-font-size);\n  line-height: 1.45;\n  box-sizing: border-box;\n}\n\n#prompt::placeholder {\n  color: var(--vscode-input-placeholderForeground, rgba(127, 127, 127, 0.8));\n}\n\n.composer-footer {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n  min-width: 0;\n}\n\n.composer-footer-left {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n  min-width: 0;\n  flex: 1;\n  overflow: hidden;\n}\n\n.composer-footer-left > .composer-plus .icon-btn {\n  width: 28px;\n  height: 28px;\n  flex-shrink: 0;\n  border-radius: var(--radius-pill);\n  background: color-mix(in srgb, var(--vscode-foreground) 7%, transparent);\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n}\n\n.composer-footer-right > #modelPicker .model-trigger {\n  padding: 4px 10px;\n  border-radius: var(--radius-sm);\n  background: transparent;\n  border: 1px solid transparent;\n  color: var(--vscode-foreground);\n}\n\n.composer-footer-right > #modelPicker .model-trigger:hover:not(:disabled),\n.composer-footer-right > #modelPicker.is-open .model-trigger {\n  background: color-mix(in srgb, var(--vscode-foreground) 7%, transparent);\n  border-color: color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n}\n\n.composer-footer-left > .mode-picker .model-trigger {\n  padding: 4px 10px;\n  border-radius: var(--radius-sm);\n  background: transparent;\n  border: 1px solid transparent;\n  color: var(--vscode-foreground);\n}\n\n.composer-footer-left > .mode-picker .model-trigger:hover:not(:disabled),\n.composer-footer-left > .mode-picker.is-open .model-trigger {\n  background: color-mix(in srgb, var(--vscode-foreground) 7%, transparent);\n  border-color: color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n}\n\n.composer-input-row {\n  display: flex;\n  align-items: flex-start;\n  gap: 8px;\n  min-width: 0;\n}\n\n.composer-footer-left > .mode-picker {\n  flex-shrink: 0;\n}\n\n.composer-input-row > #prompt {\n  flex: 1 1 auto;\n  width: auto;\n  min-width: 0;\n  /* leaves room for the context ring floating in the top-right corner */\n  padding-right: 20px;\n}\n\n.composer-footer-right > #modelPicker {\n  flex: 0 1 auto;\n  min-width: 0;\n}\n\n.composer-footer-right > .reason-picker {\n  flex: 0 0 auto;\n  max-width: none;\n}\n\n.composer-footer-right > .reason-picker .model-trigger {\n  padding: 4px 10px;\n  border-radius: var(--radius-sm);\n  background: transparent;\n  border: none;\n  outline: 1px solid transparent;\n  outline-offset: 0;\n  color: var(--vscode-foreground);\n}\n\n.composer-footer-right > .reason-picker .model-trigger:hover:not(:disabled),\n.composer-footer-right > .reason-picker.is-open .model-trigger {\n  background: color-mix(in srgb, var(--vscode-foreground) 9%, transparent);\n  outli",
  "ne-color: color-mix(in srgb, var(--vscode-foreground) 18%, transparent);\n  color: var(--vscode-foreground);\n}\n\n.mode-picker[data-mode=\"plan\"] .model-trigger,\n.mode-picker[data-mode=\"plan\"] .model-trigger:hover:not(:disabled),\n.mode-picker[data-mode=\"plan\"].is-open .model-trigger {\n  color: var(--mode-plan);\n}\n\n.mode-picker[data-mode=\"ask\"] .model-trigger,\n.mode-picker[data-mode=\"ask\"] .model-trigger:hover:not(:disabled),\n.mode-picker[data-mode=\"ask\"].is-open .model-trigger {\n  color: var(--mode-ask);\n}\n\n.mode-picker .model-option[data-mode=\"plan\"] .model-option-label,\n#modeMenu .model-option[data-mode=\"plan\"] .model-option-label,\n.msg-edit-mode-menu .model-option[data-mode=\"plan\"] .model-option-label {\n  color: var(--mode-plan);\n  font-weight: 500;\n}\n\n.mode-picker .model-option[data-mode=\"ask\"] .model-option-label,\n#modeMenu .model-option[data-mode=\"ask\"] .model-option-label,\n.msg-edit-mode-menu .model-option[data-mode=\"ask\"] .model-option-label {\n  color: var(--mode-ask);\n  font-weight: 500;\n}\n\n.mode-picker .model-option,\n#modeMenu .model-option,\n.msg-edit-mode-menu .model-option {\n  align-items: flex-start;\n}\n\n.mode-picker .model-option-label,\n#modeMenu .model-option-label,\n.msg-edit-mode-menu .model-option-label {\n  flex: 0 1 auto;\n}\n\n.mode-picker .model-check,\n#modeMenu .model-check,\n.msg-edit-mode-menu .model-check {\n  margin-top: 1px;\n}\n\n.msg-mention {\n  display: inline;\n  margin: 0;\n  padding: 0 2px;\n  border: none;\n  border-radius: var(--radius-xs);\n  background: color-mix(in srgb, var(--vscode-textLink-foreground, #3794ff) 16%, transparent);\n  color: var(--vscode-textLink-foreground, #3794ff);\n  font: inherit;\n  line-height: inherit;\n  cursor: pointer;\n  vertical-align: baseline;\n}\n\n.msg-file-chip,\n.msg-mention.msg-file-chip {\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n  margin: 0;\n  padding: 3px 8px 3px 4px;\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n  border-radius: var(--radius-md);\n  background: color-mix(in srgb, var(--vscode-foreground) 6%, transparent);\n  color: var(--vscode-foreground);\n  font-size: 11px;\n  line-height: 1.2;\n  vertical-align: middle;\n}\n\n.msg.user .msg-file-chip {\n  margin: 0 0 4px;\n}\n\n.msg-file-chip-name {\n  min-width: 0;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  max-width: 160px;\n}\n\n.msg-file-chip-line {\n  opacity: 0.65;\n  flex-shrink: 0;\n}\n\n.msg-mention:hover {\n  background: color-mix(in srgb, var(--vscode-textLink-foreground, #3794ff) 28%, transparent);\n  text-decoration: underline;\n}\n\n.msg-mention.msg-file-chip:hover {\n  background: color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n  text-decoration: none;\n}\n\n.composer-plus {\n  position: relative;\n  flex-shrink: 0;\n}\n\n.composer-plus-menu {\n  position: absolute;\n  left: 0;\n  bottom: calc(100% + 6px);\n  min-width: 168px;\n  padding: 4px;\n  border-radius: var(--radius-lg);\n  background: var(--vscode-editorWidget-background, var(--vscode-input-background, #fff));\n  border: 1px solid var(--vscode-widget-border, rgba(127, 127, 127, 0.28));\n  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.18);\n  z-index: 1000;\n}\n\n.composer-plus-menu.is-fixed {\n  position: fixed;\n  right: auto;\n  z-index: 10000;\n}\n\n.composer-plus-menu.opens-down {\n  top: calc(100% + 6px);\n  bottom: auto;\n}\n\n.composer-plus-menu[hidden] {\n  display: none !important;\n}\n\n.composer-plus-item {\n  width: 100%;\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  margin: 0;\n  padding: 7px 8px;\n  border: none;\n  border-radius: var(--radius-sm);\n  background: transparent;\n  color: var(--vscode-foreground);\n  font-family: inherit;\n  font-size: 12px;\n  font-weight: 400;\n  text-align: left;\n  cursor: pointer;\n}\n\n.composer-plus-item:hover {\n  background: color-mix(in srgb, var(--vscode-foreground) 8%, transparent);\n}\n\n.composer-plus-item.is-disabled,\n.composer-plus-item:disabled {\n  opacity: 0.45;\n  cursor: not-allowed;\n}\n\n.composer-plus-item.is-disabled:hover,\n.composer-plus-item:disabled:hover {\n  background: transparent;\n}\n\n.composer-plus-item .material-symbols-outlined {\n  font-size: 16px;\n  opacity: 0.85;\n}\n\n.composer-plus.is-open .icon-btn {\n  opacity: 1;\n  background: color-mix(in srgb, var(--vscode-foreground) 14%, transparent);\n}\n\n.composer-footer-right {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n  flex-shrink: 0;\n  position: relative;\n  z-index: 2;\n}\n\n.icon-btn {\n  flex-shrink: 0;\n  width: 26px;\n  height: 26px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  background: color-mix(\n    in srgb,\n    var(--vscode-foreground) 8%,\n    transparent\n  );\n  color: var(--vscode-foreground);\n  border: none;\n  border-radius: var(--radius-pill);\n  padding: 0;\n  cursor: pointer;\n  opacity: 0.85;\n  position: relative;\n}\n\n.icon-btn:hover {\n  opacity: 1;\n  background: color-mix(\n    in srgb,\n    var(--vscode-foreground) 14%,\n    transparent\n  );\n}\n\n.icon-btn:disabled {\n  opacity: 0.35;\n  cursor: default;\n}\n\n.icon-btn .material-symbols-outlined {\n  font-size: 16px;\n  pointer-events: none;\n}\n\n.row-action .material-symbols-outlined {\n  font-size: 15px;\n}\n\n.model-picker {\n  position: relative;\n  min-width: 0;\n  flex: 1;\n}\n\n.model-trigger {\n  display: inline-flex;\n  align-items: center;\n  gap: 4px;\n  max-width: 100%;\n  min-width: 0;\n  margin: 0;\n  padding: 3px 6px;\n  border: none;\n  border-radius: var(--radius-md);\n  background: transparent;\n  color: #9a9a9a;\n  font-family: var(--harbor-font-family);\n  font-size: 12px;\n  font-weight: 400;\n  font-synthesis: none;\n  -webkit-font-smoothing: antialiased;\n  line-height: 1.2;\n  cursor: pointer;\n}\n\n.model-trigger:hover:not(:disabled) {\n  background: color-mix(in srgb, var(--vscode-foreground) 6%, transparent);\n  color: #7e7e7e;\n}\n\n.model-trigger:disabled {\n  opacity: 0.45;\n  cursor: default;\n}\n\n.model-label {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  min-width: 0;\n}\n\n.model-chevron {\n  flex-shrink: 0;\n  opacity: 0.75;\n  font-size: 14px;\n  transition: transform 0.12s e",
  "ase;\n}\n\n.model-picker.is-open .model-trigger {\n  background: color-mix(in srgb, var(--vscode-foreground) 8%, transparent);\n  color: #6f6f6f;\n}\n\n.model-picker.is-open .model-chevron {\n  transform: rotate(180deg);\n}\n\n.model-picker.is-open.opens-down .model-chevron {\n  transform: none;\n}\n\n.model-icon-compact {\n  display: none;\n  flex-shrink: 0;\n  font-size: 16px;\n  opacity: 0.9;\n}\n\n.composer.is-compact #modelPicker .model-label {\n  display: none;\n}\n\n.composer.is-compact #modelPicker .model-icon-compact {\n  display: inline-flex;\n}\n\n.composer.is-compact .composer-footer-right > #modelPicker .model-trigger,\n.composer.is-compact .composer-footer-right > .reason-picker .model-trigger {\n  padding: 4px 6px;\n}\n\n.model-menu {\n  position: absolute;\n  left: 0;\n  right: auto;\n  bottom: calc(100% + 6px);\n  min-width: 220px;\n  max-width: min(320px, 90vw);\n  max-height: 240px;\n  overflow-y: auto;\n  padding: 4px;\n  border-radius: var(--radius-lg);\n  background: var(--vscode-editorWidget-background, var(--vscode-input-background, #fff));\n  border: 1px solid var(--vscode-widget-border, rgba(127, 127, 127, 0.28));\n  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.18);\n  z-index: 1000;\n}\n\n.model-menu.opens-down {\n  top: calc(100% + 6px);\n  bottom: auto;\n}\n\n.model-menu.is-fixed {\n  position: fixed;\n  /* top / left / bottom / max-height задаются в JS */\n  right: auto;\n  z-index: 10000;\n}\n\n.model-menu[hidden] {\n  display: none !important;\n}\n\n.model-option {\n  width: 100%;\n  display: flex;\n  align-items: center;\n  justify-content: flex-start;\n  gap: 8px;\n  margin: 0;\n  padding: 7px 8px;\n  border: none;\n  border-radius: var(--radius-sm);\n  background: transparent;\n  color: var(--vscode-foreground);\n  font-family: inherit;\n  font-size: 12px;\n  font-weight: 400;\n  text-align: left;\n  cursor: pointer;\n}\n\n.model-option:hover {\n  background: color-mix(in srgb, var(--vscode-foreground) 8%, transparent);\n}\n\n.model-option.is-active {\n  background: color-mix(in srgb, var(--vscode-focusBorder, #007fd4) 14%, transparent);\n}\n\n.model-option.is-empty {\n  cursor: default;\n  color: #9a9a9a;\n  font-weight: 400;\n}\n\n.model-option-fav {\n  flex-shrink: 0;\n  display: inline-flex;\n  align-items: center;\n  margin-left: auto;\n  color: var(--vscode-button-background, #0e639c);\n  opacity: 0.95;\n}\n\n.model-option-fav .material-symbols-outlined {\n  font-size: 14px;\n  line-height: 1;\n}\n\n.model-option-label {\n  flex: 1;\n  min-width: 0;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n\n.model-check {\n  flex-shrink: 0;\n  color: var(--vscode-focusBorder, #007fd4);\n}\n\n.model-check + .model-option-fav {\n  margin-left: 0;\n}\n\n.model-option:not(:has(.model-option-fav)) .model-check {\n  margin-left: auto;\n}\n\n.model-check .material-symbols-outlined {\n  font-size: 14px;\n}\n\nbutton.primary {\n  width: 28px;\n  height: 28px;\n  border-radius: var(--radius-pill);\n  border: none;\n  background: var(--vscode-foreground);\n  color: var(--vscode-sideBar-background);\n  cursor: pointer;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  padding: 0;\n  flex-shrink: 0;\n  position: relative;\n}\n\nbutton.primary .icon-send,\nbutton.primary .icon-queue,\nbutton.primary .icon-stop {\n  display: inline-block;\n  font-size: 16px;\n  pointer-events: none;\n}\n\nbutton.primary .icon-queue,\nbutton.primary .icon-stop {\n  display: none;\n}\n\nbutton.primary.is-stop .icon-send,\nbutton.primary.is-queue .icon-send {\n  display: none;\n}\n\nbutton.primary.is-stop .icon-queue,\nbutton.primary.is-queue .icon-stop {\n  display: none;\n}\n\nbutton.primary.is-stop .icon-stop {\n  display: inline-block;\n}\n\nbutton.primary.is-queue .icon-queue {\n  display: inline-block;\n}\n\nbutton.primary:hover {\n  opacity: 0.88;\n}\n\nbutton.primary:disabled {\n  opacity: 0.35;\n  cursor: default;\n}\n\n.workspace-shell {\n  position: relative;\n  flex: 1;\n  min-height: 0;\n  min-width: 0;\n  display: flex;\n  flex-direction: row;\n  align-items: stretch;\n}\n\n.workspace-shell[hidden] {\n  display: none !important;\n}\n\n.agents-rail-backdrop {\n  display: none;\n}\n\n.agents-rail {\n  flex: 0 0 280px;\n  width: 280px;\n  min-width: 0;\n  max-width: 320px;\n  border-right: 1px solid\n    color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n  background: var(--vscode-sideBar-background);\n}\n\n.agents-rail .agents-title {\n  flex: 1;\n  min-width: 0;\n}\n\n.agents-rail .agent-trailing {\n  flex-basis: 4.8em;\n  min-width: 4.8em;\n}\n\n.workspace-shell.is-narrow.is-rail-open .agents-rail-backdrop {\n  display: block;\n  position: absolute;\n  inset: 0;\n  /* Above .composer-wrap (30) so the rail drawer covers the compose field\n     instead of sitting under it. */\n  z-index: 60;\n  border: none;\n  padding: 0;\n  margin: 0;\n  background: color-mix(in srgb, var(--vscode-widget-shadow, #000) 35%, transparent);\n  cursor: pointer;\n}\n\n.workspace-shell.is-narrow.is-rail-open .agents-rail {\n  position: absolute;\n  left: 0;\n  top: 0;\n  bottom: 0;\n  z-index: 61;\n  width: min(300px, 85%);\n  max-width: none;\n  flex: none;\n  box-shadow: 4px 0 24px\n    color-mix(in srgb, var(--vscode-widget-shadow, #000) 28%, transparent);\n}\n\n.workspace-shell > .chat-screen {\n  flex: 1;\n  min-width: 0;\n}\n\n.screen {\n  flex: 1;\n  min-height: 0;\n  display: flex;\n  flex-direction: column;\n}\n\n.screen[hidden] {\n  display: none !important;\n}\n\n.workspace-shell > .agents-rail {\n  flex: 0 0 280px;\n}\n\n.workspace-shell > .chat-screen {\n  flex: 1 1 auto;\n}\n\nhtml[data-surface=\"settings\"] #workspaceShell,\nhtml[data-surface=\"settings\"] #agentsScreen,\nhtml[data-surface=\"settings\"] #archiveScreen,\nhtml[data-surface=\"settings\"] #chatScreen {\n  display: none !important;\n}\n\nhtml[data-surface=\"settings\"] body {\n  min-height: 100vh;\n}\n\nhtml[data-surface=\"settings\"] #settingsScreen:not([hidden]) {\n  display: flex;\n  flex-direction: column;\n  min-height: 100vh;\n  padding-top: 48px;\n  box-sizing: border-box;\n}\n\nhtml[data-surface=\"settings\"] .settings-layout {\n  flex: 1;\n  min-height: 0;\n}\n\n.settings-layout {\n  display: flex;\n  flex: 1;\n  min-height: 0;\n  align-items: stretch;\n}\n\n.settings-nav {\n",
  "  flex: 0 0 220px;\n  width: 220px;\n  padding: 8px 8px 16px;\n  border-right: 1px solid color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n  overflow-y: auto;\n  display: flex;\n  flex-direction: column;\n  gap: 2px;\n}\n\n.settings-nav-item {\n  display: flex;\n  align-items: center;\n  gap: 10px;\n  width: 100%;\n  margin: 0;\n  padding: 8px 10px;\n  border: none;\n  border-radius: var(--radius-sm);\n  background: transparent;\n  color: var(--vscode-foreground);\n  font: inherit;\n  font-size: 13px;\n  text-align: left;\n  cursor: pointer;\n}\n\n.settings-nav-item .material-symbols-outlined {\n  font-size: 18px;\n  opacity: 0.85;\n  flex-shrink: 0;\n}\n\n.settings-nav-label {\n  line-height: 1.25;\n}\n\n.settings-nav-item:hover {\n  background: color-mix(in srgb, var(--vscode-foreground) 8%, transparent);\n}\n\n.settings-nav-item.is-active {\n  background: color-mix(in srgb, var(--vscode-focusBorder, #007fd4) 22%, transparent);\n  color: var(--vscode-foreground);\n}\n\n.settings-nav-item.is-active .material-symbols-outlined {\n  opacity: 1;\n}\n\n.settings-body {\n  flex: 1;\n  overflow-y: auto;\n  padding: 12px 20px 24px;\n  min-height: 0;\n  max-width: 760px;\n  box-sizing: border-box;\n}\n\n.settings-panel[hidden] {\n  display: none !important;\n}\n\n.settings-panel .settings-section-title {\n  margin-top: 0;\n}\n\n.settings-panel .mcp-body {\n  margin: 0;\n  padding: 0;\n  max-width: none;\n}\n\n@media (max-width: 640px) {\n  .settings-layout {\n    flex-direction: column;\n  }\n\n  .settings-nav {\n    flex: 0 0 auto;\n    width: 100%;\n    flex-direction: row;\n    flex-wrap: wrap;\n    border-right: none;\n    border-bottom: 1px solid color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n    padding-bottom: 8px;\n  }\n\n  .settings-nav-item {\n    width: auto;\n    flex: 1 1 auto;\n  }\n}\n\n.agents-top,\n.chat-top {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n  padding: 6px 10px 8px;\n  flex-shrink: 0;\n}\n\n.chat-top .icon-btn {\n  flex-shrink: 0;\n}\n\n.chat-screen:has(.chat-branches:not([hidden])) .chat-top {\n  padding-bottom: 6px;\n}\n\n.agents-title {\n  flex: 1;\n  font-size: 13px;\n  font-weight: 600;\n}\n\n.settings-save-status {\n  position: fixed;\n  top: 10px;\n  right: 14px;\n  z-index: 20;\n  font-size: 11px;\n  opacity: 0.8;\n  color: var(--vscode-descriptionForeground);\n  pointer-events: none;\n}\n\n.agents-list {\n  flex: 1;\n  overflow-y: auto;\n  padding: 4px 8px 12px;\n  min-height: 0;\n}\n\n.settings-section {\n  margin-bottom: 18px;\n}\n\n.settings-section-title {\n  margin: 0 0 10px;\n  font-size: 11px;\n  font-weight: 600;\n  color: var(--vscode-descriptionForeground, #888);\n  text-transform: uppercase;\n  letter-spacing: 0.03em;\n}\n\n.settings-section-note {\n  margin: -4px 0 10px;\n  font-size: 12px;\n  opacity: 0.7;\n  line-height: 1.35;\n}\n\n.settings-figma-actions {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 8px;\n  margin: 8px 0 4px;\n}\n\n.settings-figma-pat {\n  margin-top: 10px;\n  padding-top: 8px;\n  border-top: 1px solid var(--vscode-widget-border, rgba(128, 128, 128, 0.35));\n}\n\n.settings-field {\n  display: flex;\n  flex-direction: column;\n  gap: 4px;\n  margin-bottom: 10px;\n}\n\n.settings-field.settings-check {\n  flex-direction: row;\n  align-items: center;\n  gap: 8px;\n}\n\n.settings-label {\n  font-size: 12px;\n  color: var(--vscode-foreground);\n}\n\n.settings-field-hint {\n  font-size: 11px;\n  opacity: 0.7;\n  line-height: 1.3;\n}\n\n.settings-font-size-row {\n  display: flex;\n  align-items: center;\n  gap: 12px;\n}\n\n.settings-range {\n  flex: 1;\n  min-width: 0;\n  height: 18px;\n  margin: 2px 0;\n  padding: 0;\n  border: none;\n  outline: none;\n  appearance: none;\n  -webkit-appearance: none;\n  cursor: pointer;\n  background: linear-gradient(\n    to right,\n    var(--vscode-button-background, #0e639c) 0,\n    var(--vscode-button-background, #0e639c) var(--harbor-range-fill, 22%),\n    color-mix(in srgb, var(--vscode-foreground) 16%, transparent)\n      var(--harbor-range-fill, 22%),\n    color-mix(in srgb, var(--vscode-foreground) 16%, transparent) 100%\n  );\n  background-size: 100% 4px;\n  background-repeat: no-repeat;\n  background-position: center;\n  border-radius: var(--radius-pill);\n}\n\n.settings-range:focus,\n.settings-range:focus-visible {\n  outline: none;\n}\n\n.settings-range::-webkit-slider-runnable-track {\n  height: 4px;\n  border: none;\n  border-radius: var(--radius-pill);\n  background: transparent;\n}\n\n.settings-range::-webkit-slider-thumb {\n  -webkit-appearance: none;\n  appearance: none;\n  width: 14px;\n  height: 14px;\n  margin-top: -5px;\n  border: none;\n  border-radius: var(--radius-pill);\n  background: var(--vscode-button-background, #0e639c);\n  box-shadow: 0 0 0 1px color-mix(in srgb, var(--vscode-foreground) 14%, transparent);\n}\n\n.settings-range:focus-visible::-webkit-slider-thumb {\n  box-shadow: 0 0 0 2px var(--vscode-focusBorder, #007fd4);\n}\n\n.settings-range::-moz-range-track {\n  height: 4px;\n  border: none;\n  border-radius: var(--radius-pill);\n  background: color-mix(in srgb, var(--vscode-foreground) 16%, transparent);\n}\n\n.settings-range::-moz-range-progress {\n  height: 4px;\n  border: none;\n  border-radius: var(--radius-pill);\n  background: var(--vscode-button-background, #0e639c);\n}\n\n.settings-range::-moz-range-thumb {\n  width: 14px;\n  height: 14px;\n  border: none;\n  border-radius: var(--radius-pill);\n  background: var(--vscode-button-background, #0e639c);\n  box-shadow: 0 0 0 1px color-mix(in srgb, var(--vscode-foreground) 14%, transparent);\n}\n\n.settings-font-size-value {\n  flex: 0 0 4.5em;\n  font-size: 12px;\n  font-variant-numeric: tabular-nums;\n  text-align: right;\n  color: var(--vscode-descriptionForeground, #888);\n}\n\n.settings-font-preview {\n  margin: 4px 0 0;\n  padding: 10px 12px;\n  border-radius: var(--radius-lg);\n  background: color-mix(in srgb, var(--vscode-foreground) 4%, transparent);\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n  font-size: var(--harbor-font-size);\n  line-height: 1.45;\n}\n\n.settings-input {\n  width: 100%;\n  box-sizing: border-box;\n  margin: 0;\n  padding: 6px 8px;\n  border: 1px solid var(--vscode-input-border, trans",
  "parent);\n  border-radius: var(--radius-sm);\n  background: var(--vscode-input-background, #3c3c3c);\n  color: var(--vscode-input-foreground, #ccc);\n  font-family: var(--harbor-font-family);\n  font-size: 12px;\n}\n\n.settings-input:focus {\n  outline: 1px solid var(--vscode-focusBorder, #007fd4);\n  outline-offset: -1px;\n}\n\n.settings-select-picker {\n  position: relative;\n  display: inline-flex;\n  align-self: flex-start;\n  width: auto;\n  max-width: 100%;\n  flex: 0 0 auto;\n  min-width: 0;\n}\n\n.settings-select-picker > select.settings-select-native,\n.settings-select-picker > select.settings-input {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  padding: 0;\n  margin: -1px;\n  overflow: hidden;\n  clip: rect(0, 0, 0, 0);\n  white-space: nowrap;\n  border: 0;\n  opacity: 0;\n  pointer-events: none;\n}\n\n.settings-select-picker .model-trigger {\n  width: auto;\n  max-width: 100%;\n  box-sizing: border-box;\n  justify-content: flex-start;\n  padding: 4px 10px;\n  border-radius: var(--radius-sm);\n  background: color-mix(in srgb, var(--vscode-foreground) 7%, transparent);\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n  color: var(--vscode-foreground);\n}\n\n.settings-select-picker .model-trigger:hover:not(:disabled),\n.settings-select-picker.is-open .model-trigger {\n  background: color-mix(in srgb, var(--vscode-foreground) 7%, transparent);\n  color: var(--vscode-foreground);\n}\n\n.settings-select-picker .model-trigger:focus-visible {\n  outline: 1px solid var(--vscode-focusBorder, #007fd4);\n  outline-offset: 1px;\n}\n\n.settings-select-picker .model-label {\n  flex: 0 1 auto;\n  text-align: left;\n}\n\n.settings-select-menu.model-menu {\n  min-width: 240px;\n}\n\nhtml[data-harbor-host=\"jetbrains\"] select.harbor-select-open {\n  outline: 1px solid var(--vscode-focusBorder, #007fd4);\n  outline-offset: -1px;\n}\n\n.settings-textarea {\n  resize: vertical;\n  min-height: 96px;\n  line-height: 1.4;\n}\n\n.settings-speed-models {\n  display: flex;\n  flex-direction: column;\n  gap: 2px;\n  margin-top: 6px;\n  max-height: 220px;\n  overflow: auto;\n  border: 1px solid var(--vscode-widget-border, rgba(128, 128, 128, 0.35));\n  border-radius: var(--radius-sm);\n  padding: 4px;\n}\n\n.settings-speed-model-row {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  padding: 6px 8px;\n  border-radius: var(--radius-xs);\n  user-select: none;\n}\n\n.settings-speed-model-row:hover {\n  background: var(--vscode-list-hoverBackground, rgba(128, 128, 128, 0.12));\n}\n\n.settings-speed-model-row.is-model-off {\n  opacity: 0.72;\n}\n\n.settings-speed-model-row.is-fast-off .settings-speed-model-name {\n  opacity: 0.85;\n}\n\n.settings-speed-model-row .settings-speed-fast-check {\n  margin: 0;\n  flex-shrink: 0;\n}\n\n.settings-speed-model-row .settings-speed-model-name {\n  flex: 1;\n  min-width: 0;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  font-size: 12px;\n}\n\n.settings-speed-model-row .settings-model-switch {\n  margin-left: auto;\n}\n\n.settings-speed-models-empty {\n  padding: 10px 8px;\n  font-size: 12px;\n  color: var(--vscode-descriptionForeground, #888);\n}\n\n.settings-hint {\n  margin: 0 0 8px;\n  font-size: 11px;\n  color: var(--vscode-descriptionForeground, #888);\n}\n\n.settings-hint.is-error {\n  color: var(--vscode-errorForeground, #f14c4c);\n}\n\n.settings-group-title {\n  margin-top: 16px;\n  margin-bottom: 8px;\n}\n\n.settings-panel .settings-group-title {\n  margin-top: 16px;\n}\n\n.settings-limits-row {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: 10px;\n  margin-bottom: 4px;\n}\n\n.settings-limits-row .settings-field {\n  margin-bottom: 0;\n}\n\n.settings-limits-row .settings-field-hint {\n  margin-top: -2px;\n}\n\n.settings-panel[data-settings-panel=\"commit\"] > .settings-group-title:first-of-type {\n  margin-top: 12px;\n}\n\n.settings-panel[data-settings-panel=\"commit\"] .settings-limits-row {\n  margin-bottom: 10px;\n}\n\n@media (max-width: 520px) {\n  .settings-panel[data-settings-panel=\"commit\"] .settings-limits-row {\n    grid-template-columns: 1fr;\n  }\n}\n\n.settings-card {\n  display: flex;\n  flex-direction: column;\n  margin: 0 0 4px;\n  overflow: hidden;\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n  border-radius: var(--radius-lg);\n  background: color-mix(in srgb, var(--vscode-foreground) 4%, transparent);\n}\n\n.settings-toggle-row {\n  display: flex;\n  align-items: center;\n  gap: 12px;\n  margin: 0;\n  padding: 10px 12px;\n  cursor: pointer;\n}\n\n.settings-toggle-row + .settings-toggle-row {\n  border-top: 1px solid color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n}\n\n.settings-toggle-row:hover {\n  background: color-mix(in srgb, var(--vscode-foreground) 5%, transparent);\n}\n\n.settings-toggle-text {\n  flex: 1;\n  min-width: 0;\n  display: flex;\n  flex-direction: column;\n  gap: 2px;\n}\n\n.settings-toggle-title {\n  font-size: 13px;\n  font-weight: 600;\n  color: var(--vscode-foreground);\n}\n\n.settings-toggle-hint {\n  font-size: 11px;\n  line-height: 1.35;\n  color: var(--vscode-descriptionForeground, #888);\n}\n\n.settings-prompt-card {\n  margin: 0 0 4px;\n  overflow: hidden;\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n  border-radius: var(--radius-lg);\n  background: color-mix(in srgb, var(--vscode-foreground) 4%, transparent);\n}\n\n.settings-prompt-toggle {\n  display: flex;\n  align-items: center;\n  gap: 10px;\n  width: 100%;\n  margin: 0;\n  padding: 10px 12px;\n  border: 0;\n  border-radius: 0;\n  appearance: none;\n  background: transparent;\n  color: inherit;\n  font: inherit;\n  text-align: left;\n  cursor: pointer;\n}\n\n.settings-prompt-toggle:hover {\n  background: color-mix(in srgb, var(--vscode-foreground) 5%, transparent);\n}\n\n.settings-prompt-toggle:focus-visible {\n  outline: 1px solid var(--vscode-focusBorder, #007fd4);\n  outline-offset: -1px;\n}\n\n.settings-prompt-toggle-text {\n  flex: 1;\n  min-width: 0;\n  display: flex;\n  flex-direction: column;\n  gap: 2px;\n}\n\n.settings-prompt-title {\n  font-size: 13px;\n  font-weight: 600;\n}\n\n.settings-prompt-preview {\n  font-size: 11px;\n  line-height: 1.",
  "35;\n  color: var(--vscode-descriptionForeground, #888);\n  white-space: nowrap;\n  overflow: hidden;\n  text-overflow: ellipsis;\n}\n\n.settings-prompt-card.is-open .settings-prompt-preview {\n  display: none;\n}\n\n.settings-prompt-chevron {\n  flex-shrink: 0;\n  font-size: 20px;\n  opacity: 0.55;\n  transform: rotate(-90deg);\n  transition: transform 0.12s ease;\n}\n\n.settings-prompt-card.is-open .settings-prompt-chevron {\n  transform: rotate(0deg);\n}\n\n.settings-prompt-body {\n  padding: 0 12px 12px;\n}\n\n.settings-prompt-body .settings-textarea {\n  min-height: 120px;\n}\n\n.settings-json-textarea {\n  min-height: 140px;\n  font-family: var(--harbor-font-mono);\n  font-size: 11px;\n}\n\n.settings-json-actions {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 8px;\n  margin-bottom: 6px;\n}\n\n.settings-modal-tabs {\n  display: flex;\n  gap: 4px;\n  padding: 0 12px 4px;\n}\n\n.settings-modal-tab {\n  border: 1px solid transparent;\n  background: transparent;\n  color: var(--vscode-descriptionForeground, #888);\n  font: inherit;\n  font-size: 12px;\n  padding: 6px 10px;\n  border-radius: var(--radius-sm);\n  cursor: pointer;\n}\n\n.settings-modal-tab:hover {\n  background: color-mix(in srgb, var(--vscode-foreground) 8%, transparent);\n  color: var(--vscode-foreground);\n}\n\n.settings-modal-tab.is-active {\n  background: color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n  color: var(--vscode-foreground);\n  border-color: color-mix(in srgb, var(--vscode-foreground) 14%, transparent);\n}\n\n.settings-model-row {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  padding: 8px 10px;\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n  border-radius: var(--radius-md);\n  background: color-mix(in srgb, var(--vscode-foreground) 3%, transparent);\n  transition: border-color 0.12s ease;\n}\n\n.settings-models {\n  display: flex;\n  flex-direction: column;\n  gap: 10px;\n}\n\n.settings-provider-group {\n  display: flex;\n  flex-direction: column;\n  gap: 4px;\n  padding: 8px;\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 14%, transparent);\n  border-radius: var(--radius-lg);\n  background: color-mix(in srgb, var(--vscode-foreground) 4%, transparent);\n}\n\n.settings-provider-group .settings-provider-head {\n  margin: 0;\n  padding: 4px 2px 6px;\n  border: none;\n  border-radius: 0;\n  background: transparent;\n}\n\n.settings-provider-group .settings-model-row {\n  background: color-mix(in srgb, var(--vscode-editor-background, #1e1e1e) 55%, transparent);\n}\n\n.settings-model-row:hover {\n  border-color: color-mix(in srgb, var(--vscode-focusBorder, #007fd4) 55%, transparent);\n}\n\n.settings-model-row.is-disabled {\n  opacity: 0.62;\n}\n\n.settings-models-empty {\n  padding: 10px 4px;\n  font-size: 12px;\n  opacity: 0.7;\n}\n\n.settings-model-switch {\n  position: relative;\n  display: inline-flex;\n  align-items: center;\n  flex-shrink: 0;\n  width: 34px;\n  height: 18px;\n  cursor: pointer;\n}\n\n.settings-model-toggle {\n  position: absolute;\n  opacity: 0;\n  width: 0;\n  height: 0;\n}\n\n.settings-model-switch-ui {\n  width: 34px;\n  height: 18px;\n  border-radius: var(--radius-pill);\n  background: color-mix(in srgb, var(--vscode-foreground) 22%, transparent);\n  transition: background 0.15s ease;\n  position: relative;\n}\n\n.settings-model-switch-ui::after {\n  content: \"\";\n  position: absolute;\n  top: 2px;\n  left: 2px;\n  width: 14px;\n  height: 14px;\n  border-radius: 50%;\n  background: var(--vscode-editor-background, #1e1e1e);\n  box-shadow: 0 0 0 1px color-mix(in srgb, var(--vscode-foreground) 20%, transparent);\n  transition: transform 0.15s ease;\n}\n\n.settings-model-toggle:checked + .settings-model-switch-ui {\n  background: var(--vscode-button-background, #0e639c);\n}\n\n.settings-model-toggle:checked + .settings-model-switch-ui::after {\n  transform: translateX(16px);\n  background: var(--vscode-button-foreground, #fff);\n  box-shadow: none;\n}\n\n.settings-model-toggle:focus-visible + .settings-model-switch-ui {\n  outline: 1px solid var(--vscode-focusBorder, #007fd4);\n  outline-offset: 2px;\n}\n\n.settings-model-info {\n  flex: 1;\n  min-width: 0;\n  display: flex;\n  flex-direction: column;\n  gap: 1px;\n}\n\n.settings-model-title {\n  display: flex;\n  align-items: center;\n  gap: 4px;\n  min-width: 0;\n}\n\n.settings-model-name {\n  font-size: 13px;\n  font-weight: 600;\n  white-space: nowrap;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  min-width: 0;\n}\n\n.settings-model-info-btn {\n  flex-shrink: 0;\n  width: 22px;\n  height: 22px;\n  opacity: 0.55;\n  background: transparent;\n}\n\n.settings-model-info-btn .material-symbols-outlined {\n  font-size: 16px;\n  line-height: 1;\n}\n\n.settings-model-info-btn:hover,\n.settings-model-info-btn:focus-visible {\n  opacity: 1;\n  background: transparent;\n}\n\n.settings-model-fav {\n  flex-shrink: 0;\n  width: 22px;\n  height: 22px;\n  opacity: 0.4;\n  background: transparent;\n  color: var(--vscode-foreground);\n}\n\n.settings-model-fav .material-symbols-outlined {\n  font-size: 16px;\n  line-height: 1;\n}\n\n.settings-model-fav:hover,\n.settings-model-fav:focus-visible {\n  opacity: 0.85;\n  background: transparent;\n}\n\n.settings-model-fav.is-on {\n  opacity: 1;\n  color: var(--vscode-button-background, #0e639c);\n}\n\n.settings-model-fav.is-on:hover,\n.settings-model-fav.is-on:focus-visible {\n  opacity: 1;\n  background: transparent;\n  color: var(--vscode-button-background, #0e639c);\n}\n\n.settings-model-id {\n  font-size: 11px;\n  opacity: 0.65;\n  white-space: nowrap;\n  overflow: hidden;\n  text-overflow: ellipsis;\n}\n\n.settings-model-tip {\n  position: fixed;\n  top: 0;\n  left: 0;\n  z-index: 1000;\n  min-width: 200px;\n  max-width: min(280px, calc(100vw - 16px));\n  padding: 8px 10px;\n  border-radius: var(--radius-md);\n  background: var(--vscode-editorWidget-background, #252526);\n  color: var(--vscode-editorWidget-foreground, var(--vscode-foreground));\n  border: 1px solid var(--vscode-editorWidget-border, rgba(127, 127, 127, 0.35));\n  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.28);\n  font-size: 11px;\n  line-height: 1.35;\n  pointer-events: none;\n  will-change: transform;\n}\n\n.settings-model-tip[hi",
  "dden] {\n  display: none !important;\n}\n\n.settings-model-tip-row {\n  display: grid;\n  grid-template-columns: auto 1fr;\n  gap: 8px 12px;\n  align-items: baseline;\n}\n\n.settings-model-tip-row + .settings-model-tip-row {\n  margin-top: 4px;\n}\n\n.settings-model-tip-key {\n  opacity: 0.65;\n  white-space: nowrap;\n}\n\n.settings-model-tip-val {\n  font-weight: 500;\n  overflow-wrap: anywhere;\n  text-align: right;\n  font-variant-numeric: tabular-nums;\n}\n\n.settings-model-limits {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: 8px;\n}\n\n.settings-model-limits .settings-field {\n  margin-bottom: 0;\n}\n\n.settings-model-remove,\n.settings-model-edit {\n  width: 28px;\n  height: 28px;\n  font-size: 18px;\n  line-height: 1;\n  flex-shrink: 0;\n}\n\n.settings-modal {\n  position: fixed;\n  inset: 0;\n  z-index: 1200;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  padding: 16px;\n}\n\n.settings-modal[hidden] {\n  display: none !important;\n}\n\n.settings-modal-backdrop {\n  position: absolute;\n  inset: 0;\n  background: color-mix(in srgb, #000 45%, transparent);\n}\n\n.settings-modal-card {\n  position: relative;\n  z-index: 1;\n  width: min(100%, 400px);\n  max-height: min(90vh, 640px);\n  overflow: hidden;\n  border-radius: var(--radius-lg);\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 16%, transparent);\n  background: var(--vscode-editor-background, var(--vscode-sideBar-background));\n  box-shadow: 0 12px 40px color-mix(in srgb, #000 35%, transparent);\n  display: flex;\n  flex-direction: column;\n}\n\n.settings-modal-head {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 8px;\n  padding: 12px 12px 8px;\n  flex: 0 0 auto;\n}\n\n.settings-modal-title {\n  margin: 0;\n  font-size: 14px;\n  font-weight: 600;\n}\n\n.settings-modal-body {\n  padding: 4px 12px 8px;\n  flex: 1 1 auto;\n  min-height: 0;\n  overflow: hidden;\n}\n\n.settings-modal-foot {\n  display: flex;\n  justify-content: flex-end;\n  gap: 8px;\n  padding: 8px 12px 12px;\n  flex: 0 0 auto;\n}\n\n.settings-modal-done {\n  font-weight: 600;\n}\n\n.settings-modal-tabs {\n  flex: 0 0 auto;\n}\n\n.settings-fetch-models-list {\n  display: flex;\n  flex-direction: column;\n  gap: 2px;\n  max-height: min(36vh, 260px);\n  overflow: auto;\n  margin: 0;\n  padding: 2px 0;\n  border: none;\n  border-radius: 0;\n}\n\n.settings-fetch-model-row {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  padding: 5px 4px;\n  border-radius: var(--radius-xs);\n  cursor: pointer;\n  min-width: 0;\n}\n\n.settings-fetch-model-row:hover {\n  background: color-mix(in srgb, var(--vscode-foreground) 8%, transparent);\n}\n\n.settings-fetch-model-row.is-existing {\n  opacity: 0.72;\n  cursor: default;\n}\n\n.settings-fetch-model-row input {\n  flex: 0 0 auto;\n  margin: 0;\n}\n\n.settings-fetch-model-id {\n  flex: 1 1 auto;\n  min-width: 0;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  font-family: var(--harbor-font-mono);\n  font-size: 12px;\n}\n\n.settings-fetch-model-badge {\n  flex: 0 0 auto;\n  font-size: 11px;\n  color: var(--vscode-descriptionForeground, #888);\n}\n\n.settings-add-actions {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 4px 12px;\n  margin: 0 0 4px;\n}\n\n.settings-provider-head {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  padding: 8px 10px;\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 16%, transparent);\n  border-radius: var(--radius-md);\n  background: color-mix(in srgb, var(--vscode-foreground) 6%, transparent);\n}\n\n.settings-provider-head .settings-model-name {\n  display: flex;\n  align-items: center;\n  flex-wrap: wrap;\n  gap: 6px;\n  font-weight: 600;\n  min-width: 0;\n}\n\n.provider-status-dot {\n  width: 8px;\n  height: 8px;\n  border-radius: 50%;\n  flex: 0 0 auto;\n  background: var(--vscode-descriptionForeground, #888);\n}\n\n.provider-status-dot[data-state=\"connected\"] {\n  background: var(--vscode-testing-iconPassed, #3fb950);\n}\n\n.provider-status-dot[data-state=\"connecting\"] {\n  background: var(--vscode-charts-yellow, #cca700);\n}\n\n.provider-status-dot[data-state=\"error\"] {\n  background: var(--vscode-errorForeground, #f14c4c);\n}\n\n.provider-status-dot[data-state=\"unknown\"] {\n  background: var(--vscode-descriptionForeground, #888);\n}\n\n.provider-status-title {\n  min-width: 0;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n\n.provider-status-text {\n  font-size: 11px;\n  font-weight: 500;\n  color: var(--vscode-descriptionForeground, #888);\n  white-space: nowrap;\n}\n\n.provider-status-text[data-state=\"connected\"] {\n  color: var(--vscode-testing-iconPassed, #3fb950);\n}\n\n.provider-status-text[data-state=\"connecting\"] {\n  color: var(--vscode-charts-yellow, #cca700);\n}\n\n.provider-status-text[data-state=\"error\"] {\n  color: var(--vscode-errorForeground, #f14c4c);\n}\n\n.provider-status-text[data-state=\"unknown\"] {\n  color: var(--vscode-descriptionForeground, #888);\n}\n\n.settings-model-row.is-under-provider {\n  margin-left: 0;\n  border-style: solid;\n}\n\n.settings-new-provider {\n  display: flex;\n  flex-direction: column;\n  gap: 8px;\n  margin: 0 0 10px;\n  padding: 10px;\n  border: 1px dashed color-mix(in srgb, var(--vscode-foreground) 18%, transparent);\n  border-radius: var(--radius-md);\n  background: color-mix(in srgb, var(--vscode-foreground) 4%, transparent);\n}\n\n.settings-new-provider[hidden] {\n  display: none;\n}\n\n.settings-add-model {\n  align-self: flex-start;\n}\n\n.text-btn {\n  margin: 0;\n  padding: 4px 8px;\n  border: none;\n  border-radius: var(--radius-sm);\n  background: transparent;\n  color: var(--vscode-textLink-foreground, #3794ff);\n  font-family: var(--harbor-font-family);\n  font-size: 12px;\n  cursor: pointer;\n}\n\n.text-btn:hover {\n  background: color-mix(in srgb, var(--vscode-foreground) 8%, transparent);\n}\n\n.agent-block {\n  border-radius: var(--radius-md);\n  margin-bottom: 4px;\n}\n\n.agent-block.is-open {\n  background: color-mix(in srgb, var(--vscode-foreground) 3%, transparent);\n}\n\n.agent-row-wrap {\n  border-radius: var(--radius-md);\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 16%, transparent);\n  background: var(--vscode-ed",
  "itor-background);\n}\n\n.agent-row-wrap:hover {\n  background: color-mix(in srgb, var(--vscode-foreground) 6%, transparent);\n}\n\n.agent-row {\n  width: 100%;\n  display: grid;\n  grid-template-columns: 1fr auto auto;\n  gap: 8px;\n  align-items: start;\n  padding: 9px 10px;\n  border: none;\n  border-radius: var(--radius-md);\n  background: transparent;\n  color: var(--vscode-foreground);\n  font: inherit;\n  text-align: left;\n  cursor: pointer;\n  min-width: 0;\n  flex: 1;\n}\n\n.agent-row.flat {\n  grid-template-columns: 1fr;\n}\n\n.agent-row.no-expand {\n  cursor: default;\n}\n\n.agent-row:hover {\n  background: transparent;\n}\n\n.agent-block.is-active > .agent-row-wrap,\n.agent-block.is-active > .agent-row-wrap:hover {\n  background: color-mix(in srgb, var(--harbor-user-tint) 18%, transparent);\n  outline: 1px solid color-mix(in srgb, var(--harbor-user-tint) 32%, transparent);\n  outline-offset: -1px;\n}\n\n.agent-block.is-active > .agent-row-wrap > .agent-row {\n  background: transparent;\n}\n\n.agent-block.is-active:not(:hover) > .agent-row-wrap .row-action {\n  opacity: 0.7;\n}\n\n.agent-chevron {\n  margin-top: 3px;\n  color: var(--vscode-descriptionForeground, #888);\n  font-size: 10px;\n  line-height: 1;\n  transition: transform 0.15s ease;\n}\n\n.agent-chevron.is-hidden {\n  visibility: hidden;\n}\n\n.agent-block.is-open .agent-chevron {\n  transform: rotate(90deg);\n}\n\n.agent-main {\n  min-width: 0;\n  position: relative;\n  padding-left: 18px;\n}\n\n.agent-run-status {\n  width: 12px;\n  height: 12px;\n  position: absolute;\n  left: 0;\n  top: 3px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  color: var(--vscode-descriptionForeground, #888);\n}\n\n.agent-run-status-empty {\n  opacity: 0;\n}\n\n.agent-name-row {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  min-width: 0;\n}\n\n.agent-name-wrap {\n  flex: 1;\n  min-width: 0;\n  display: flex;\n  align-items: center;\n}\n\n.agent-trailing {\n  position: relative;\n  flex: 0 0 5.5em;\n  min-width: 5.5em;\n  height: 22px;\n}\n\n.agent-name {\n  display: inline-block;\n  flex: 1;\n  min-width: 0;\n  max-width: 100%;\n  font-size: 13px;\n  font-weight: 600;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  cursor: text;\n  border-radius: var(--radius-xs);\n  outline: none;\n  line-height: 1.3;\n  padding: 0;\n  margin: 0;\n  vertical-align: top;\n}\n\n.agents-list .agent-name {\n  cursor: pointer;\n}\n\n.agent-run-status-running {\n  width: 12px;\n  height: 12px;\n  opacity: 0.95;\n  --cube-accent: var(--vscode-charts-blue, #3794ff);\n}\n\n.agent-run-status-running[data-mode=\"plan\"] {\n  --cube-accent: var(--mode-plan);\n}\n\n.agent-run-status-running[data-mode=\"ask\"] {\n  --cube-accent: var(--mode-ask);\n}\n\n.agent-run-status-running .cube-bit {\n  position: absolute;\n  width: 5px;\n  height: 5px;\n  border-radius: 1px;\n  border: 1px solid color-mix(in srgb, var(--vscode-foreground) 28%, transparent);\n  background: color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n  box-sizing: border-box;\n  animation: cube-build 0.95s ease-in-out infinite;\n}\n\n.agent-run-status-running .cube-bit-1 {\n  left: 0;\n  top: 0;\n  animation-delay: 0s;\n}\n\n.agent-run-status-running .cube-bit-2 {\n  left: 7px;\n  top: 0;\n  animation-delay: 0.18s;\n}\n\n.agent-run-status-running .cube-bit-3 {\n  left: 0;\n  top: 7px;\n  animation-delay: 0.36s;\n}\n\n.agent-run-status-running .cube-bit-4 {\n  left: 7px;\n  top: 7px;\n  animation-delay: 0.54s;\n}\n\n.agent-run-status-success .cube-bit,\n.agent-run-status-error .cube-bit {\n  position: absolute;\n  width: 5px;\n  height: 5px;\n  border-radius: 1px;\n  box-sizing: border-box;\n}\n\n.agent-run-status-success .cube-bit-1,\n.agent-run-status-error .cube-bit-1 {\n  left: 0;\n  top: 0;\n}\n\n.agent-run-status-success .cube-bit-2,\n.agent-run-status-error .cube-bit-2 {\n  left: 7px;\n  top: 0;\n}\n\n.agent-run-status-success .cube-bit-3,\n.agent-run-status-error .cube-bit-3 {\n  left: 0;\n  top: 7px;\n}\n\n.agent-run-status-success .cube-bit-4,\n.agent-run-status-error .cube-bit-4 {\n  left: 7px;\n  top: 7px;\n}\n\n.agent-run-status-success .cube-bit {\n  border: 1px solid var(--vscode-testing-iconPassed, #73c991);\n  background: color-mix(\n    in srgb,\n    var(--vscode-testing-iconPassed, #73c991) 55%,\n    transparent\n  );\n}\n\n.agent-run-status-error .cube-bit {\n  border: 1px solid var(--vscode-errorForeground, #f14c4c);\n  background: color-mix(\n    in srgb,\n    var(--vscode-errorForeground, #f14c4c) 55%,\n    transparent\n  );\n}\n\n@keyframes cube-build {\n  0%,\n  100% {\n    opacity: 0.45;\n    transform: scale(0.9);\n    border-color: color-mix(in srgb, var(--cube-accent, var(--vscode-foreground)) 35%, transparent);\n    background: color-mix(in srgb, var(--cube-accent, var(--vscode-foreground)) 10%, transparent);\n  }\n  50% {\n    opacity: 1;\n    transform: scale(1);\n    border-color: var(--cube-accent, var(--vscode-charts-blue, #3794ff));\n    background: color-mix(\n      in srgb,\n      var(--cube-accent, var(--vscode-charts-blue, #3794ff)) 30%,\n      transparent\n    );\n  }\n}\n\n.agent-name.is-renaming {\n  display: none;\n}\n\n.agent-name-input {\n  display: inline-block;\n  box-sizing: content-box;\n  min-width: 1ch;\n  max-width: 100%;\n  margin: 0;\n  padding: 0;\n  border: none;\n  border-radius: 0;\n  background: transparent;\n  color: inherit;\n  font: inherit;\n  font-size: 13px;\n  font-weight: 600;\n  line-height: 1.3;\n  height: 1.3em;\n  outline: none;\n  box-shadow: none;\n  vertical-align: top;\n  caret-color: var(--vscode-foreground);\n  -webkit-appearance: none;\n  appearance: none;\n}\n\n.agent-name-wrap .agent-name-input {\n  flex: 1;\n  min-width: 0;\n  max-width: 100%;\n}\n\n.agent-name-input.is-chat {\n  color: var(--vscode-foreground);\n}\n\n.agent-name-input::selection {\n  background: var(--vscode-editor-selectionBackground, rgba(14, 99, 156, 0.45));\n  color: var(--vscode-editor-selectionForeground, var(--vscode-foreground));\n}\n\n.chat-agent-name {\n  display: inline-block;\n  max-width: 100%;\n  font-size: 12.5px;\n  font-weight: 600;\n  color: var(--vscode-foreground);\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  cursor: text;\n  ",
  "border-radius: var(--radius-xs);\n  outline: none;\n  line-height: 1.3;\n  padding: 0;\n  margin: 0;\n  vertical-align: top;\n}\n\n.chat-agent-name:hover {\n  background: color-mix(in srgb, var(--vscode-foreground) 6%, transparent);\n}\n\n.chat-agent-name.is-renaming {\n  display: none;\n}\n\n.chat-top-text {\n  position: relative;\n  min-width: 0;\n  flex: 1;\n  display: flex;\n  flex-direction: column;\n  align-items: flex-start;\n  justify-content: center;\n  gap: 1px;\n}\n\n.agent-meta {\n  margin-top: 4px;\n  font-size: 11px;\n  color: var(--vscode-descriptionForeground, #888);\n  min-width: 0;\n}\n\n.agent-chip {\n  display: inline-block;\n  margin-top: 4px;\n  max-width: 100%;\n  padding: 1px 6px;\n  border: none;\n  border-radius: var(--radius-xs);\n  background: color-mix(in srgb, var(--vscode-foreground) 14%, transparent);\n  font-size: 10px;\n  color: var(--vscode-foreground);\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n\n.agent-chip:empty {\n  display: none;\n}\n\n.agent-preview {\n  margin-top: 2px;\n  font-size: 12px;\n  line-height: 1.35;\n  color: var(--vscode-descriptionForeground, #888);\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  min-width: 0;\n}\n\n.agent-preview:empty {\n  display: none;\n}\n\n.agent-preview .md-link,\n.agent-preview .md-code {\n  color: inherit;\n}\n\n.agent-preview .md-link {\n  pointer-events: none;\n  text-decoration: underline;\n  text-decoration-color: color-mix(in srgb, currentColor 45%, transparent);\n}\n\n.agent-preview .md-code {\n  font-family: var(--harbor-font-mono);\n}\n\n.agent-time {\n  font-size: 10px;\n  color: var(--vscode-descriptionForeground, #888);\n  white-space: nowrap;\n  flex-shrink: 0;\n  min-width: 0;\n  text-align: right;\n  padding-top: 0;\n  position: absolute;\n  right: 0;\n  top: 50%;\n  transform: translateY(-50%);\n}\n\n.row-actions {\n  display: flex;\n  align-items: center;\n  justify-content: flex-end;\n  gap: 4px;\n  flex-shrink: 0;\n  position: absolute;\n  right: 0;\n  top: 50%;\n  transform: translateY(-50%);\n}\n\n.agent-row-wrap:hover .agent-time,\n.agent-row-wrap:focus-within .agent-time,\n.agent-block.is-active > .agent-row-wrap .agent-time {\n  visibility: hidden;\n}\n\n.row-action {\n  position: static;\n  transform: none;\n  width: 24px;\n  height: 24px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  border: none;\n  border-radius: var(--radius-sm);\n  padding: 0;\n  background: transparent;\n  color: var(--vscode-descriptionForeground, #888);\n  cursor: pointer;\n  opacity: 0;\n  pointer-events: none;\n  transition: opacity 0.12s ease, color 0.12s ease, background 0.12s ease, box-shadow 0.12s ease;\n}\n\n.archive-row-wrap .row-action,\n.agent-row-wrap:hover .row-action,\n.row-action:focus-visible {\n  opacity: 1;\n  pointer-events: auto;\n}\n\n.row-action svg {\n  display: block;\n}\n\n.agent-chats {\n  display: none;\n  flex-direction: column;\n  gap: 2px;\n  padding: 0 8px 6px 28px;\n}\n\n.agent-block.is-open .agent-chats {\n  display: flex;\n}\n\n.agent-block.is-open .agent-chats:empty {\n  display: none;\n  padding: 0;\n}\n\n.agents-empty {\n  padding: 24px 16px;\n  text-align: center;\n  font-size: 12px;\n  color: var(--vscode-descriptionForeground, #888);\n}\n\n.msg.has-search-match .chat-search-mark,\n.msg.is-search-current .chat-search-mark {\n  background: #5a5a5a;\n  color: #f3f3f3;\n}\n\n.msg-body.has-search-match-fallback {\n  background: transparent;\n}\n\n.msg.is-search-current,\n.msg.is-search-hit,\n.msg-wrap.is-search-hit > .msg {\n  outline: none;\n}\n\n.msg.user.is-search-current,\n.msg.user.is-search-hit,\n.msg-wrap.is-search-hit > .msg.user {\n  border: 1px solid #8a8a8a;\n  border-radius: var(--radius-sm);\n}\n\n.msg.assistant.is-search-current,\n.msg.assistant.is-search-hit,\n.msg-wrap.is-search-hit > .msg.assistant {\n  border: 1px solid #8a8a8a;\n  border-radius: var(--radius-xl);\n  padding: 10px 12px;\n}\n\n.msg.is-search-current .chat-search-mark {\n  background: #727272;\n  color: #fff;\n}\n\n.settings-approvals-block {\n  border-top: 1px solid color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n  border-bottom: 1px solid color-mix(in srgb, var(--vscode-foreground) 10%, transparent);\n  background: color-mix(in srgb, var(--vscode-foreground) 4%, transparent);\n}\n\n.settings-approval-row .settings-approval-select {\n  width: auto;\n  min-width: 118px;\n  flex-shrink: 0;\n  cursor: pointer;\n}\n\n.agent-step-checkpoint-compare {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  margin-left: 6px;\n  padding: 1px 2px;\n  border: none;\n  background: transparent;\n  color: var(--vscode-foreground);\n  opacity: 0.7;\n  cursor: pointer;\n  border-radius: var(--radius-sm, 4px);\n}\n\n.agent-step-checkpoint-compare:hover {\n  opacity: 1;\n  background: color-mix(in srgb, var(--vscode-foreground) 12%, transparent);\n}\n\n.agent-step-checkpoint-compare .material-symbols-outlined {\n  font-size: 14px;\n  line-height: 1;\n}\n\n</style>\n  </head>\n  <body>\n    <div id=\"workspaceShell\" class=\"workspace-shell is-narrow\">\n      <div id=\"agentsRailBackdrop\" class=\"agents-rail-backdrop\" hidden></div>\n\n      <section id=\"agentsScreen\" class=\"screen agents-rail\" hidden>\n        <div class=\"agents-top\">\n          <div class=\"agents-title\" data-i18n=\"agents\">Agents</div>\n          <button type=\"button\" class=\"icon-btn\" id=\"newAgentBtn\" title=\"New chat\" aria-label=\"New chat\">\n            <span class=\"material-symbols-outlined\" aria-hidden=\"true\">add</span>\n          </button>\n        </div>\n        <div id=\"agentsList\" class=\"agents-list\"></div>\n      </section>\n\n      <section id=\"chatScreen\" class=\"screen chat-screen\">\n        <div class=\"chat-top\">\n          <button\n            type=\"button\"\n            class=\"icon-btn\"\n            id=\"toggleAgentsRailBtn\"\n            title=\"Agents\"\n            aria-label=\"Agents\"\n            aria-pressed=\"false\"\n          >\n            <span class=\"material-symbols-outlined\" aria-hidden=\"true\">menu</span>\n          </button>\n          <div class=\"chat-top-text\">\n            <div id=\"chatAgentName\" class=\"chat-agent-name\">Chat</div>\n          </div>\n         ",
  " <button\n            type=\"button\"\n            class=\"icon-btn\"\n            id=\"copyBriefBtn\"\n            title=\"Copy brief\"\n            aria-label=\"Copy brief\"\n            hidden\n          >\n            <span class=\"material-symbols-outlined\" aria-hidden=\"true\">content_copy</span>\n          </button>\n          <button type=\"button\" class=\"icon-btn\" id=\"openSettingsBtn\" title=\"Settings\" aria-label=\"Settings\">\n            <span class=\"material-symbols-outlined\" aria-hidden=\"true\">settings</span>\n          </button>\n        </div>\n        <div id=\"messages\"></div>\n        <div class=\"composer-wrap\" id=\"composerWrap\">\n          <div class=\"composer\" id=\"composer\" data-mode=\"agent\">\n            <div id=\"selectionPreview\" class=\"selection-preview\" hidden>\n              <img id=\"selectionThumb\" alt=\"\" hidden />\n              <div id=\"selectionMeta\" class=\"selection-preview-meta\"></div>\n            </div>\n            <div class=\"composer-input-row\">\n              <textarea id=\"prompt\" rows=\"3\" placeholder=\"Task for the design agent…\"></textarea>\n            </div>\n            <div class=\"composer-footer\">\n              <div class=\"composer-footer-left\">\n                <div class=\"model-picker mode-picker\" id=\"modePicker\" data-mode=\"agent\">\n                  <button\n                    type=\"button\"\n                    class=\"model-trigger\"\n                    id=\"modeTrigger\"\n                    aria-haspopup=\"listbox\"\n                    aria-expanded=\"false\"\n                    title=\"Mode\"\n                  >\n                    <span class=\"model-label\" id=\"modeLabel\">Agent</span>\n                    <span class=\"material-symbols-outlined model-chevron\" aria-hidden=\"true\">expand_more</span>\n                  </button>\n                  <div class=\"model-menu\" id=\"modeMenu\" role=\"listbox\" hidden></div>\n                </div>\n              </div>\n              <div class=\"composer-footer-right\">\n                <div class=\"model-picker\" id=\"modelPicker\">\n                  <button\n                    type=\"button\"\n                    class=\"model-trigger\"\n                    id=\"modelTrigger\"\n                    aria-haspopup=\"listbox\"\n                    aria-expanded=\"false\"\n                    title=\"Model\"\n                  >\n                    <span class=\"material-symbols-outlined model-icon-compact\" aria-hidden=\"true\">smart_toy</span>\n                    <span class=\"model-label\" id=\"modelLabel\">Model</span>\n                    <span class=\"material-symbols-outlined model-chevron\" aria-hidden=\"true\">expand_more</span>\n                  </button>\n                  <div class=\"model-menu\" id=\"modelMenu\" role=\"listbox\" hidden></div>\n                </div>\n                <button class=\"primary\" id=\"sendBtn\" title=\"Send\" aria-label=\"Send\" data-mode=\"send\" type=\"button\">\n                  <span class=\"material-symbols-outlined icon-send\" aria-hidden=\"true\">arrow_upward</span>\n                  <span class=\"material-symbols-outlined icon-stop\" aria-hidden=\"true\">stop</span>\n                </button>\n              </div>\n            </div>\n          </div>\n          <div id=\"statusLine\" class=\"status-line\"></div>\n        </div>\n      </section>\n    </div>\n\n    <section id=\"settingsScreen\" class=\"screen\" hidden>\n      <div id=\"settingsSaveStatus\" class=\"settings-save-status\" hidden>Saved</div>\n      <div class=\"settings-top\">\n        <button type=\"button\" class=\"icon-btn\" id=\"closeSettingsBtn\" title=\"Back\" aria-label=\"Back\">\n          <span class=\"material-symbols-outlined\" aria-hidden=\"true\">arrow_back</span>\n        </button>\n        <div class=\"settings-title\" data-i18n=\"settings\">Settings</div>\n      </div>\n      <div class=\"settings-layout\">\n        <nav class=\"settings-nav\" id=\"settingsNav\" aria-label=\"Settings\">\n          <button type=\"button\" class=\"settings-nav-item is-active\" data-settings-cat=\"models\">\n            <span class=\"material-symbols-outlined\" aria-hidden=\"true\">dns</span>\n            <span class=\"settings-nav-label\" data-i18n=\"modelsProviders\">Models &amp; providers</span>\n          </button>\n          <button type=\"button\" class=\"settings-nav-item\" data-settings-cat=\"language\">\n            <span class=\"material-symbols-outlined\" aria-hidden=\"true\">language</span>\n            <span class=\"settings-nav-label\" data-i18n=\"language\">Language</span>\n          </button>\n          <button type=\"button\" class=\"settings-nav-item\" data-settings-cat=\"appearance\">\n            <span class=\"material-symbols-outlined\" aria-hidden=\"true\">format_size</span>\n            <span class=\"settings-nav-label\" data-i18n=\"appearance\">Appearance</span>\n          </button>\n        </nav>\n        <div class=\"settings-body\" id=\"settingsBody\">\n          <section class=\"settings-panel\" data-settings-panel=\"models\">\n            <h3 class=\"settings-section-title\" data-i18n=\"modelsProviders\">Models &amp; providers</h3>\n            <p class=\"settings-section-note\" data-i18n=\"providersNote\">\n              Base URL and API key for each OpenAI-compatible API. Models are grouped under their provider.\n            </p>\n            <div id=\"settingsProvidersModelsList\" class=\"settings-models\"></div>\n            <div class=\"settings-add-actions\">\n              <button type=\"button\" class=\"text-btn settings-add-model\" id=\"addModelBtn\" data-i18n=\"addModel\">\n                + Model\n              </button>\n              <button type=\"button\" class=\"text-btn settings-add-model\" id=\"addProviderBtn\" data-i18n=\"addProvider\">\n                + Provider\n              </button>\n            </div>\n          </section>\n          <section class=\"settings-panel\" data-settings-panel=\"language\" hidden>\n            <h3 class=\"settings-section-title\" data-i18n=\"language\">Language</h3>\n            <label class=\"settings-field\">\n              <span class=\"settings-label\" data-i18n=\"pluginUiLanguage\">Plugin UI language</span>\n              <select id=\"langSelect\" class=\"settings-input\">\n                <option value=\"en\">English</option",
  ">\n                <option value=\"ru\">Русский</option>\n              </select>\n            </label>\n          </section>\n          <section class=\"settings-panel\" data-settings-panel=\"appearance\" hidden>\n            <h3 class=\"settings-section-title\" data-i18n=\"appearance\">Appearance</h3>\n            <p class=\"settings-section-note\" data-i18n=\"appearanceNote\">Chat text and composer size in the panel.</p>\n            <label class=\"settings-field\">\n              <span class=\"settings-label\" data-i18n=\"fontSize\">Font size</span>\n              <div class=\"settings-font-size-row\">\n                <input id=\"fontSizeRange\" class=\"settings-range\" type=\"range\" min=\"11\" max=\"20\" step=\"1\" value=\"13\" />\n                <span class=\"settings-font-size-value\" id=\"fontSizeValue\">13 px</span>\n              </div>\n              <span class=\"settings-field-hint\" data-i18n=\"fontSizeHint\">Applies to messages and the input field.</span>\n            </label>\n            <p class=\"settings-font-preview\" id=\"fontPreview\" data-i18n=\"fontPreview\">\n              The agent will reply at this size.\n            </p>\n          </section>\n        </div>\n      </div>\n\n      <div id=\"providerEditModal\" class=\"settings-modal\" hidden>\n        <div class=\"settings-modal-backdrop\" data-provider-dismiss=\"1\"></div>\n        <div class=\"settings-modal-card\" role=\"dialog\" aria-modal=\"true\" aria-labelledby=\"providerEditTitle\">\n          <div class=\"settings-modal-head\">\n            <h3 id=\"providerEditTitle\" class=\"settings-modal-title\">Provider</h3>\n            <button type=\"button\" class=\"icon-btn\" id=\"providerEditCloseBtn\" title=\"Close\" aria-label=\"Close\">\n              <span class=\"material-symbols-outlined\" aria-hidden=\"true\">close</span>\n            </button>\n          </div>\n          <div class=\"settings-modal-body\">\n            <label class=\"settings-field\">\n              <span class=\"settings-label\">Name</span>\n              <input id=\"providerEditName\" class=\"settings-input\" type=\"text\" placeholder=\"OpenAI\" />\n            </label>\n            <label class=\"settings-field\">\n              <span class=\"settings-label\">Base URL</span>\n              <input\n                id=\"providerEditBaseUrl\"\n                class=\"settings-input\"\n                type=\"text\"\n                placeholder=\"https://api.openai.com/v1\"\n              />\n            </label>\n            <label class=\"settings-field\">\n              <span class=\"settings-label\">API Key</span>\n              <input id=\"providerEditApiKey\" class=\"settings-input\" type=\"password\" autocomplete=\"off\" />\n            </label>\n          </div>\n          <div class=\"settings-modal-foot\">\n            <button type=\"button\" class=\"text-btn\" id=\"providerEditCancelBtn\" data-i18n=\"cancel\">Cancel</button>\n            <button type=\"button\" class=\"text-btn settings-modal-done\" id=\"providerEditDoneBtn\" data-i18n=\"done\">\n              Done\n            </button>\n          </div>\n        </div>\n      </div>\n\n      <div id=\"modelEditModal\" class=\"settings-modal\" hidden>\n        <div class=\"settings-modal-backdrop\" data-modal-dismiss=\"1\"></div>\n        <div class=\"settings-modal-card\" role=\"dialog\" aria-modal=\"true\" aria-labelledby=\"modelEditTitle\">\n          <div class=\"settings-modal-head\">\n            <h3 id=\"modelEditTitle\" class=\"settings-modal-title\">Model</h3>\n            <button type=\"button\" class=\"icon-btn\" id=\"modelEditCloseBtn\" title=\"Close\" aria-label=\"Close\">\n              <span class=\"material-symbols-outlined\" aria-hidden=\"true\">close</span>\n            </button>\n          </div>\n          <div class=\"settings-modal-body\">\n            <label class=\"settings-field\">\n              <span class=\"settings-label\">Provider</span>\n              <select id=\"modelEditProvider\" class=\"settings-input\"></select>\n            </label>\n            <label class=\"settings-field\">\n              <span class=\"settings-label\">ID</span>\n              <input id=\"modelEditId\" class=\"settings-input\" type=\"text\" placeholder=\"gpt-4o-mini\" />\n            </label>\n            <label class=\"settings-field\">\n              <span class=\"settings-label\">Name</span>\n              <input id=\"modelEditLabel\" class=\"settings-input\" type=\"text\" placeholder=\"shown in the list\" />\n            </label>\n            <label class=\"settings-field settings-check\">\n              <span class=\"settings-label\" data-i18n=\"supportsVision\">Supports images (vision)</span>\n              <span class=\"settings-switch\">\n                <input id=\"modelEditVision\" type=\"checkbox\" />\n                <span class=\"settings-switch-track\" aria-hidden=\"true\"></span>\n              </span>\n            </label>\n          </div>\n          <div class=\"settings-modal-foot\">\n            <button type=\"button\" class=\"text-btn\" id=\"modelEditCancelBtn\" data-i18n=\"cancel\">Cancel</button>\n            <button type=\"button\" class=\"text-btn settings-modal-done\" id=\"modelEditDoneBtn\" data-i18n=\"done\">\n              Done\n            </button>\n          </div>\n        </div>\n      </div>\n    </section>\n\n    <script>\n      (function () {\n        var memoryState = {};\n        globalThis.__harborHost = {\n          postMessage: function (message) {\n            parent.postMessage({ pluginMessage: message }, \"*\");\n          },\n          getState: function () {\n            return memoryState;\n          },\n          setState: function (s) {\n            memoryState = s || {};\n          },\n        };\n      })();\n    </script>\n    <script>\n/**\n * marked v15.0.12 - a markdown parser\n * Copyright (c) 2011-2025, Christopher Jeffrey. (MIT Licensed)\n * https://github.com/markedjs/marked\n */\n\n/**\n * DO NOT EDIT THIS FILE\n * The code in this file is generated from files in ./src/\n */\n(function(g,f){if(typeof exports==\"object\"&&typeof module<\"u\"){module.exports=f()}else if(\"function\"==typeof define && define.amd){define(\"marked\",f)}else {g[\"marked\"]=f()}}(typeof globalThis < \"u\" ? globalThis : typeof self < \"u\" ? self : this,function(){var exports={};var __exports=exports;var ",
  "module={exports};\n\"use strict\";\nvar __defProp = Object.defineProperty;\nvar __getOwnPropDesc = Object.getOwnPropertyDescriptor;\nvar __getOwnPropNames = Object.getOwnPropertyNames;\nvar __hasOwnProp = Object.prototype.hasOwnProperty;\nvar __export = (target, all) => {\n  for (var name in all)\n    __defProp(target, name, { get: all[name], enumerable: true });\n};\nvar __copyProps = (to, from, except, desc) => {\n  if (from && typeof from === \"object\" || typeof from === \"function\") {\n    for (let key of __getOwnPropNames(from))\n      if (!__hasOwnProp.call(to, key) && key !== except)\n        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });\n  }\n  return to;\n};\nvar __toCommonJS = (mod) => __copyProps(__defProp({}, \"__esModule\", { value: true }), mod);\n\n// src/marked.ts\nvar marked_exports = {};\n__export(marked_exports, {\n  Hooks: () => _Hooks,\n  Lexer: () => _Lexer,\n  Marked: () => Marked,\n  Parser: () => _Parser,\n  Renderer: () => _Renderer,\n  TextRenderer: () => _TextRenderer,\n  Tokenizer: () => _Tokenizer,\n  defaults: () => _defaults,\n  getDefaults: () => _getDefaults,\n  lexer: () => lexer,\n  marked: () => marked,\n  options: () => options,\n  parse: () => parse,\n  parseInline: () => parseInline,\n  parser: () => parser,\n  setOptions: () => setOptions,\n  use: () => use,\n  walkTokens: () => walkTokens\n});\nmodule.exports = __toCommonJS(marked_exports);\n\n// src/defaults.ts\nfunction _getDefaults() {\n  return {\n    async: false,\n    breaks: false,\n    extensions: null,\n    gfm: true,\n    hooks: null,\n    pedantic: false,\n    renderer: null,\n    silent: false,\n    tokenizer: null,\n    walkTokens: null\n  };\n}\nvar _defaults = _getDefaults();\nfunction changeDefaults(newDefaults) {\n  _defaults = newDefaults;\n}\n\n// src/rules.ts\nvar noopTest = { exec: () => null };\nfunction edit(regex, opt = \"\") {\n  let source = typeof regex === \"string\" ? regex : regex.source;\n  const obj = {\n    replace: (name, val) => {\n      let valSource = typeof val === \"string\" ? val : val.source;\n      valSource = valSource.replace(other.caret, \"$1\");\n      source = source.replace(name, valSource);\n      return obj;\n    },\n    getRegex: () => {\n      return new RegExp(source, opt);\n    }\n  };\n  return obj;\n}\nvar other = {\n  codeRemoveIndent: /^(?: {1,4}| {0,3}\\t)/gm,\n  outputLinkReplace: /\\\\([\\[\\]])/g,\n  indentCodeCompensation: /^(\\s+)(?:```)/,\n  beginningSpace: /^\\s+/,\n  endingHash: /#$/,\n  startingSpaceChar: /^ /,\n  endingSpaceChar: / $/,\n  nonSpaceChar: /[^ ]/,\n  newLineCharGlobal: /\\n/g,\n  tabCharGlobal: /\\t/g,\n  multipleSpaceGlobal: /\\s+/g,\n  blankLine: /^[ \\t]*$/,\n  doubleBlankLine: /\\n[ \\t]*\\n[ \\t]*$/,\n  blockquoteStart: /^ {0,3}>/,\n  blockquoteSetextReplace: /\\n {0,3}((?:=+|-+) *)(?=\\n|$)/g,\n  blockquoteSetextReplace2: /^ {0,3}>[ \\t]?/gm,\n  listReplaceTabs: /^\\t+/,\n  listReplaceNesting: /^ {1,4}(?=( {4})*[^ ])/g,\n  listIsTask: /^\\[[ xX]\\] /,\n  listReplaceTask: /^\\[[ xX]\\] +/,\n  anyLine: /\\n.*\\n/,\n  hrefBrackets: /^<(.*)>$/,\n  tableDelimiter: /[:|]/,\n  tableAlignChars: /^\\||\\| *$/g,\n  tableRowBlankLine: /\\n[ \\t]*$/,\n  tableAlignRight: /^ *-+: *$/,\n  tableAlignCenter: /^ *:-+: *$/,\n  tableAlignLeft: /^ *:-+ *$/,\n  startATag: /^<a /i,\n  endATag: /^<\\/a>/i,\n  startPreScriptTag: /^<(pre|code|kbd|script)(\\s|>)/i,\n  endPreScriptTag: /^<\\/(pre|code|kbd|script)(\\s|>)/i,\n  startAngleBracket: /^</,\n  endAngleBracket: />$/,\n  pedanticHrefTitle: /^([^'\"]*[^\\s])\\s+(['\"])(.*)\\2/,\n  unicodeAlphaNumeric: /[\\p{L}\\p{N}]/u,\n  escapeTest: /[&<>\"']/,\n  escapeReplace: /[&<>\"']/g,\n  escapeTestNoEncode: /[<>\"']|&(?!(#\\d{1,7}|#[Xx][a-fA-F0-9]{1,6}|\\w+);)/,\n  escapeReplaceNoEncode: /[<>\"']|&(?!(#\\d{1,7}|#[Xx][a-fA-F0-9]{1,6}|\\w+);)/g,\n  unescapeTest: /&(#(?:\\d+)|(?:#x[0-9A-Fa-f]+)|(?:\\w+));?/ig,\n  caret: /(^|[^\\[])\\^/g,\n  percentDecode: /%25/g,\n  findPipe: /\\|/g,\n  splitPipe: / \\|/,\n  slashPipe: /\\\\\\|/g,\n  carriageReturn: /\\r\\n|\\r/g,\n  spaceLine: /^ +$/gm,\n  notSpaceStart: /^\\S*/,\n  endingNewline: /\\n$/,\n  listItemRegex: (bull) => new RegExp(`^( {0,3}${bull})((?:[\t ][^\\\\n]*)?(?:\\\\n|$))`),\n  nextBulletRegex: (indent) => new RegExp(`^ {0,${Math.min(3, indent - 1)}}(?:[*+-]|\\\\d{1,9}[.)])((?:[ \t][^\\\\n]*)?(?:\\\\n|$))`),\n  hrRegex: (indent) => new RegExp(`^ {0,${Math.min(3, indent - 1)}}((?:- *){3,}|(?:_ *){3,}|(?:\\\\* *){3,})(?:\\\\n+|$)`),\n  fencesBeginRegex: (indent) => new RegExp(`^ {0,${Math.min(3, indent - 1)}}(?:\\`\\`\\`|~~~)`),\n  headingBeginRegex: (indent) => new RegExp(`^ {0,${Math.min(3, indent - 1)}}#`),\n  htmlBeginRegex: (indent) => new RegExp(`^ {0,${Math.min(3, indent - 1)}}<(?:[a-z].*>|!--)`, \"i\")\n};\nvar newline = /^(?:[ \\t]*(?:\\n|$))+/;\nvar blockCode = /^((?: {4}| {0,3}\\t)[^\\n]+(?:\\n(?:[ \\t]*(?:\\n|$))*)?)+/;\nvar fences = /^ {0,3}(`{3,}(?=[^`\\n]*(?:\\n|$))|~{3,})([^\\n]*)(?:\\n|$)(?:|([\\s\\S]*?)(?:\\n|$))(?: {0,3}\\1[~`]* *(?=\\n|$)|$)/;\nvar hr = /^ {0,3}((?:-[\\t ]*){3,}|(?:_[ \\t]*){3,}|(?:\\*[ \\t]*){3,})(?:\\n+|$)/;\nvar heading = /^ {0,3}(#{1,6})(?=\\s|$)(.*)(?:\\n+|$)/;\nvar bullet = /(?:[*+-]|\\d{1,9}[.)])/;\nvar lheadingCore = /^(?!bull |blockCode|fences|blockquote|heading|html|table)((?:.|\\n(?!\\s*?\\n|bull |blockCode|fences|blockquote|heading|html|table))+?)\\n {0,3}(=+|-+) *(?:\\n+|$)/;\nvar lheading = edit(lheadingCore).replace(/bull/g, bullet).replace(/blockCode/g, /(?: {4}| {0,3}\\t)/).replace(/fences/g, / {0,3}(?:`{3,}|~{3,})/).replace(/blockquote/g, / {0,3}>/).replace(/heading/g, / {0,3}#{1,6}/).replace(/html/g, / {0,3}<[^\\n>]+>\\n/).replace(/\\|table/g, \"\").getRegex();\nvar lheadingGfm = edit(lheadingCore).replace(/bull/g, bullet).replace(/blockCode/g, /(?: {4}| {0,3}\\t)/).replace(/fences/g, / {0,3}(?:`{3,}|~{3,})/).replace(/blockquote/g, / {0,3}>/).replace(/heading/g, / {0,3}#{1,6}/).replace(/html/g, / {0,3}<[^\\n>]+>\\n/).replace(/table/g, / {0,3}\\|?(?:[:\\- ]*\\|)+[\\:\\- ]*\\n/).getRegex();\nvar _paragraph = /^([^\\n]+(?:\\n(?!hr|heading|lheading|blockquote|fences|list|html|table| +\\n)[^\\n]+)*)/;\nvar blockText = /^[^\\n]+/;\nvar _blockLabel = /(?!\\s*\\])(?:\\\\.|[^\\[\\]\\\\])+/;\nvar def = edit(/^ {0,3}\\[(label",
  ")\\]: *(?:\\n[ \\t]*)?([^<\\s][^\\s]*|<.*?>)(?:(?: +(?:\\n[ \\t]*)?| *\\n[ \\t]*)(title))? *(?:\\n+|$)/).replace(\"label\", _blockLabel).replace(\"title\", /(?:\"(?:\\\\\"?|[^\"\\\\])*\"|'[^'\\n]*(?:\\n[^'\\n]+)*\\n?'|\\([^()]*\\))/).getRegex();\nvar list = edit(/^( {0,3}bull)([ \\t][^\\n]+?)?(?:\\n|$)/).replace(/bull/g, bullet).getRegex();\nvar _tag = \"address|article|aside|base|basefont|blockquote|body|caption|center|col|colgroup|dd|details|dialog|dir|div|dl|dt|fieldset|figcaption|figure|footer|form|frame|frameset|h[1-6]|head|header|hr|html|iframe|legend|li|link|main|menu|menuitem|meta|nav|noframes|ol|optgroup|option|p|param|search|section|summary|table|tbody|td|tfoot|th|thead|title|tr|track|ul\";\nvar _comment = /<!--(?:-?>|[\\s\\S]*?(?:-->|$))/;\nvar html = edit(\n  \"^ {0,3}(?:<(script|pre|style|textarea)[\\\\s>][\\\\s\\\\S]*?(?:</\\\\1>[^\\\\n]*\\\\n+|$)|comment[^\\\\n]*(\\\\n+|$)|<\\\\?[\\\\s\\\\S]*?(?:\\\\?>\\\\n*|$)|<![A-Z][\\\\s\\\\S]*?(?:>\\\\n*|$)|<!\\\\[CDATA\\\\[[\\\\s\\\\S]*?(?:\\\\]\\\\]>\\\\n*|$)|</?(tag)(?: +|\\\\n|/?>)[\\\\s\\\\S]*?(?:(?:\\\\n[ \t]*)+\\\\n|$)|<(?!script|pre|style|textarea)([a-z][\\\\w-]*)(?:attribute)*? */?>(?=[ \\\\t]*(?:\\\\n|$))[\\\\s\\\\S]*?(?:(?:\\\\n[ \t]*)+\\\\n|$)|</(?!script|pre|style|textarea)[a-z][\\\\w-]*\\\\s*>(?=[ \\\\t]*(?:\\\\n|$))[\\\\s\\\\S]*?(?:(?:\\\\n[ \t]*)+\\\\n|$))\",\n  \"i\"\n).replace(\"comment\", _comment).replace(\"tag\", _tag).replace(\"attribute\", / +[a-zA-Z:_][\\w.:-]*(?: *= *\"[^\"\\n]*\"| *= *'[^'\\n]*'| *= *[^\\s\"'=<>`]+)?/).getRegex();\nvar paragraph = edit(_paragraph).replace(\"hr\", hr).replace(\"heading\", \" {0,3}#{1,6}(?:\\\\s|$)\").replace(\"|lheading\", \"\").replace(\"|table\", \"\").replace(\"blockquote\", \" {0,3}>\").replace(\"fences\", \" {0,3}(?:`{3,}(?=[^`\\\\n]*\\\\n)|~{3,})[^\\\\n]*\\\\n\").replace(\"list\", \" {0,3}(?:[*+-]|1[.)]) \").replace(\"html\", \"</?(?:tag)(?: +|\\\\n|/?>)|<(?:script|pre|style|textarea|!--)\").replace(\"tag\", _tag).getRegex();\nvar blockquote = edit(/^( {0,3}> ?(paragraph|[^\\n]*)(?:\\n|$))+/).replace(\"paragraph\", paragraph).getRegex();\nvar blockNormal = {\n  blockquote,\n  code: blockCode,\n  def,\n  fences,\n  heading,\n  hr,\n  html,\n  lheading,\n  list,\n  newline,\n  paragraph,\n  table: noopTest,\n  text: blockText\n};\nvar gfmTable = edit(\n  \"^ *([^\\\\n ].*)\\\\n {0,3}((?:\\\\| *)?:?-+:? *(?:\\\\| *:?-+:? *)*(?:\\\\| *)?)(?:\\\\n((?:(?! *\\\\n|hr|heading|blockquote|code|fences|list|html).*(?:\\\\n|$))*)\\\\n*|$)\"\n).replace(\"hr\", hr).replace(\"heading\", \" {0,3}#{1,6}(?:\\\\s|$)\").replace(\"blockquote\", \" {0,3}>\").replace(\"code\", \"(?: {4}| {0,3}\t)[^\\\\n]\").replace(\"fences\", \" {0,3}(?:`{3,}(?=[^`\\\\n]*\\\\n)|~{3,})[^\\\\n]*\\\\n\").replace(\"list\", \" {0,3}(?:[*+-]|1[.)]) \").replace(\"html\", \"</?(?:tag)(?: +|\\\\n|/?>)|<(?:script|pre|style|textarea|!--)\").replace(\"tag\", _tag).getRegex();\nvar blockGfm = {\n  ...blockNormal,\n  lheading: lheadingGfm,\n  table: gfmTable,\n  paragraph: edit(_paragraph).replace(\"hr\", hr).replace(\"heading\", \" {0,3}#{1,6}(?:\\\\s|$)\").replace(\"|lheading\", \"\").replace(\"table\", gfmTable).replace(\"blockquote\", \" {0,3}>\").replace(\"fences\", \" {0,3}(?:`{3,}(?=[^`\\\\n]*\\\\n)|~{3,})[^\\\\n]*\\\\n\").replace(\"list\", \" {0,3}(?:[*+-]|1[.)]) \").replace(\"html\", \"</?(?:tag)(?: +|\\\\n|/?>)|<(?:script|pre|style|textarea|!--)\").replace(\"tag\", _tag).getRegex()\n};\nvar blockPedantic = {\n  ...blockNormal,\n  html: edit(\n    `^ *(?:comment *(?:\\\\n|\\\\s*$)|<(tag)[\\\\s\\\\S]+?</\\\\1> *(?:\\\\n{2,}|\\\\s*$)|<tag(?:\"[^\"]*\"|'[^']*'|\\\\s[^'\"/>\\\\s]*)*?/?> *(?:\\\\n{2,}|\\\\s*$))`\n  ).replace(\"comment\", _comment).replace(/tag/g, \"(?!(?:a|em|strong|small|s|cite|q|dfn|abbr|data|time|code|var|samp|kbd|sub|sup|i|b|u|mark|ruby|rt|rp|bdi|bdo|span|br|wbr|ins|del|img)\\\\b)\\\\w+(?!:|[^\\\\w\\\\s@]*@)\\\\b\").getRegex(),\n  def: /^ *\\[([^\\]]+)\\]: *<?([^\\s>]+)>?(?: +([\"(][^\\n]+[\")]))? *(?:\\n+|$)/,\n  heading: /^(#{1,6})(.*)(?:\\n+|$)/,\n  fences: noopTest,\n  // fences not supported\n  lheading: /^(.+?)\\n {0,3}(=+|-+) *(?:\\n+|$)/,\n  paragraph: edit(_paragraph).replace(\"hr\", hr).replace(\"heading\", \" *#{1,6} *[^\\n]\").replace(\"lheading\", lheading).replace(\"|table\", \"\").replace(\"blockquote\", \" {0,3}>\").replace(\"|fences\", \"\").replace(\"|list\", \"\").replace(\"|html\", \"\").replace(\"|tag\", \"\").getRegex()\n};\nvar escape = /^\\\\([!\"#$%&'()*+,\\-./:;<=>?@\\[\\]\\\\^_`{|}~])/;\nvar inlineCode = /^(`+)([^`]|[^`][\\s\\S]*?[^`])\\1(?!`)/;\nvar br = /^( {2,}|\\\\)\\n(?!\\s*$)/;\nvar inlineText = /^(`+|[^`])(?:(?= {2,}\\n)|[\\s\\S]*?(?:(?=[\\\\<!\\[`*_]|\\b_|$)|[^ ](?= {2,}\\n)))/;\nvar _punctuation = /[\\p{P}\\p{S}]/u;\nvar _punctuationOrSpace = /[\\s\\p{P}\\p{S}]/u;\nvar _notPunctuationOrSpace = /[^\\s\\p{P}\\p{S}]/u;\nvar punctuation = edit(/^((?![*_])punctSpace)/, \"u\").replace(/punctSpace/g, _punctuationOrSpace).getRegex();\nvar _punctuationGfmStrongEm = /(?!~)[\\p{P}\\p{S}]/u;\nvar _punctuationOrSpaceGfmStrongEm = /(?!~)[\\s\\p{P}\\p{S}]/u;\nvar _notPunctuationOrSpaceGfmStrongEm = /(?:[^\\s\\p{P}\\p{S}]|~)/u;\nvar blockSkip = /\\[[^[\\]]*?\\]\\((?:\\\\.|[^\\\\\\(\\)]|\\((?:\\\\.|[^\\\\\\(\\)])*\\))*\\)|`[^`]*?`|<[^<>]*?>/g;\nvar emStrongLDelimCore = /^(?:\\*+(?:((?!\\*)punct)|[^\\s*]))|^_+(?:((?!_)punct)|([^\\s_]))/;\nvar emStrongLDelim = edit(emStrongLDelimCore, \"u\").replace(/punct/g, _punctuation).getRegex();\nvar emStrongLDelimGfm = edit(emStrongLDelimCore, \"u\").replace(/punct/g, _punctuationGfmStrongEm).getRegex();\nvar emStrongRDelimAstCore = \"^[^_*]*?__[^_*]*?\\\\*[^_*]*?(?=__)|[^*]+(?=[^*])|(?!\\\\*)punct(\\\\*+)(?=[\\\\s]|$)|notPunctSpace(\\\\*+)(?!\\\\*)(?=punctSpace|$)|(?!\\\\*)punctSpace(\\\\*+)(?=notPunctSpace)|[\\\\s](\\\\*+)(?!\\\\*)(?=punct)|(?!\\\\*)punct(\\\\*+)(?!\\\\*)(?=punct)|notPunctSpace(\\\\*+)(?=notPunctSpace)\";\nvar emStrongRDelimAst = edit(emStrongRDelimAstCore, \"gu\").replace(/notPunctSpace/g, _notPunctuationOrSpace).replace(/punctSpace/g, _punctuationOrSpace).replace(/punct/g, _punctuation).getRegex();\nvar emStrongRDelimAstGfm = edit(emStrongRDelimAstCore, \"gu\").replace(/notPunctSpace/g, _notPunctuationOrSpaceGfmStrongEm).replace(/punctSpace/g, _punctuationOrSpaceGfmStrongEm).replace(/punct/g, _punctuationGfmStrongEm).getRegex();\nvar emStrongRDelimUnd = edit(\n  \"^[^_*]*?\\\\*\\\\*[^_*]*?_[^_*]*?(?=\\\\*\\\\*)|[^_]+(?=[^_])|(?!_)punct(_+)(?=[\\\\s]|$)|notPunctSpace(_+)(?!_)(?=punctSpace|$)|(?!_)punctSpace(_+)(?=notPunctSpace)|[\\\\s](_+)(?!_)(?=punct)|(?!_)punct(_+)(?!_)(",
  "?=punct)\",\n  \"gu\"\n).replace(/notPunctSpace/g, _notPunctuationOrSpace).replace(/punctSpace/g, _punctuationOrSpace).replace(/punct/g, _punctuation).getRegex();\nvar anyPunctuation = edit(/\\\\(punct)/, \"gu\").replace(/punct/g, _punctuation).getRegex();\nvar autolink = edit(/^<(scheme:[^\\s\\x00-\\x1f<>]*|email)>/).replace(\"scheme\", /[a-zA-Z][a-zA-Z0-9+.-]{1,31}/).replace(\"email\", /[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+(@)[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)+(?![-_])/).getRegex();\nvar _inlineComment = edit(_comment).replace(\"(?:-->|$)\", \"-->\").getRegex();\nvar tag = edit(\n  \"^comment|^</[a-zA-Z][\\\\w:-]*\\\\s*>|^<[a-zA-Z][\\\\w-]*(?:attribute)*?\\\\s*/?>|^<\\\\?[\\\\s\\\\S]*?\\\\?>|^<![a-zA-Z]+\\\\s[\\\\s\\\\S]*?>|^<!\\\\[CDATA\\\\[[\\\\s\\\\S]*?\\\\]\\\\]>\"\n).replace(\"comment\", _inlineComment).replace(\"attribute\", /\\s+[a-zA-Z:_][\\w.:-]*(?:\\s*=\\s*\"[^\"]*\"|\\s*=\\s*'[^']*'|\\s*=\\s*[^\\s\"'=<>`]+)?/).getRegex();\nvar _inlineLabel = /(?:\\[(?:\\\\.|[^\\[\\]\\\\])*\\]|\\\\.|`[^`]*`|[^\\[\\]\\\\`])*?/;\nvar link = edit(/^!?\\[(label)\\]\\(\\s*(href)(?:(?:[ \\t]*(?:\\n[ \\t]*)?)(title))?\\s*\\)/).replace(\"label\", _inlineLabel).replace(\"href\", /<(?:\\\\.|[^\\n<>\\\\])+>|[^ \\t\\n\\x00-\\x1f]*/).replace(\"title\", /\"(?:\\\\\"?|[^\"\\\\])*\"|'(?:\\\\'?|[^'\\\\])*'|\\((?:\\\\\\)?|[^)\\\\])*\\)/).getRegex();\nvar reflink = edit(/^!?\\[(label)\\]\\[(ref)\\]/).replace(\"label\", _inlineLabel).replace(\"ref\", _blockLabel).getRegex();\nvar nolink = edit(/^!?\\[(ref)\\](?:\\[\\])?/).replace(\"ref\", _blockLabel).getRegex();\nvar reflinkSearch = edit(\"reflink|nolink(?!\\\\()\", \"g\").replace(\"reflink\", reflink).replace(\"nolink\", nolink).getRegex();\nvar inlineNormal = {\n  _backpedal: noopTest,\n  // only used for GFM url\n  anyPunctuation,\n  autolink,\n  blockSkip,\n  br,\n  code: inlineCode,\n  del: noopTest,\n  emStrongLDelim,\n  emStrongRDelimAst,\n  emStrongRDelimUnd,\n  escape,\n  link,\n  nolink,\n  punctuation,\n  reflink,\n  reflinkSearch,\n  tag,\n  text: inlineText,\n  url: noopTest\n};\nvar inlinePedantic = {\n  ...inlineNormal,\n  link: edit(/^!?\\[(label)\\]\\((.*?)\\)/).replace(\"label\", _inlineLabel).getRegex(),\n  reflink: edit(/^!?\\[(label)\\]\\s*\\[([^\\]]*)\\]/).replace(\"label\", _inlineLabel).getRegex()\n};\nvar inlineGfm = {\n  ...inlineNormal,\n  emStrongRDelimAst: emStrongRDelimAstGfm,\n  emStrongLDelim: emStrongLDelimGfm,\n  url: edit(/^((?:ftp|https?):\\/\\/|www\\.)(?:[a-zA-Z0-9\\-]+\\.?)+[^\\s<]*|^email/, \"i\").replace(\"email\", /[A-Za-z0-9._+-]+(@)[a-zA-Z0-9-_]+(?:\\.[a-zA-Z0-9-_]*[a-zA-Z0-9])+(?![-_])/).getRegex(),\n  _backpedal: /(?:[^?!.,:;*_'\"~()&]+|\\([^)]*\\)|&(?![a-zA-Z0-9]+;$)|[?!.,:;*_'\"~)]+(?!$))+/,\n  del: /^(~~?)(?=[^\\s~])((?:\\\\.|[^\\\\])*?(?:\\\\.|[^\\s~\\\\]))\\1(?=[^~]|$)/,\n  text: /^([`~]+|[^`~])(?:(?= {2,}\\n)|(?=[a-zA-Z0-9.!#$%&'*+\\/=?_`{\\|}~-]+@)|[\\s\\S]*?(?:(?=[\\\\<!\\[`*~_]|\\b_|https?:\\/\\/|ftp:\\/\\/|www\\.|$)|[^ ](?= {2,}\\n)|[^a-zA-Z0-9.!#$%&'*+\\/=?_`{\\|}~-](?=[a-zA-Z0-9.!#$%&'*+\\/=?_`{\\|}~-]+@)))/\n};\nvar inlineBreaks = {\n  ...inlineGfm,\n  br: edit(br).replace(\"{2,}\", \"*\").getRegex(),\n  text: edit(inlineGfm.text).replace(\"\\\\b_\", \"\\\\b_| {2,}\\\\n\").replace(/\\{2,\\}/g, \"*\").getRegex()\n};\nvar block = {\n  normal: blockNormal,\n  gfm: blockGfm,\n  pedantic: blockPedantic\n};\nvar inline = {\n  normal: inlineNormal,\n  gfm: inlineGfm,\n  breaks: inlineBreaks,\n  pedantic: inlinePedantic\n};\n\n// src/helpers.ts\nvar escapeReplacements = {\n  \"&\": \"&amp;\",\n  \"<\": \"&lt;\",\n  \">\": \"&gt;\",\n  '\"': \"&quot;\",\n  \"'\": \"&#39;\"\n};\nvar getEscapeReplacement = (ch) => escapeReplacements[ch];\nfunction escape2(html2, encode) {\n  if (encode) {\n    if (other.escapeTest.test(html2)) {\n      return html2.replace(other.escapeReplace, getEscapeReplacement);\n    }\n  } else {\n    if (other.escapeTestNoEncode.test(html2)) {\n      return html2.replace(other.escapeReplaceNoEncode, getEscapeReplacement);\n    }\n  }\n  return html2;\n}\nfunction cleanUrl(href) {\n  try {\n    href = encodeURI(href).replace(other.percentDecode, \"%\");\n  } catch {\n    return null;\n  }\n  return href;\n}\nfunction splitCells(tableRow, count) {\n  const row = tableRow.replace(other.findPipe, (match, offset, str) => {\n    let escaped = false;\n    let curr = offset;\n    while (--curr >= 0 && str[curr] === \"\\\\\") escaped = !escaped;\n    if (escaped) {\n      return \"|\";\n    } else {\n      return \" |\";\n    }\n  }), cells = row.split(other.splitPipe);\n  let i = 0;\n  if (!cells[0].trim()) {\n    cells.shift();\n  }\n  if (cells.length > 0 && !cells.at(-1)?.trim()) {\n    cells.pop();\n  }\n  if (count) {\n    if (cells.length > count) {\n      cells.splice(count);\n    } else {\n      while (cells.length < count) cells.push(\"\");\n    }\n  }\n  for (; i < cells.length; i++) {\n    cells[i] = cells[i].trim().replace(other.slashPipe, \"|\");\n  }\n  return cells;\n}\nfunction rtrim(str, c, invert) {\n  const l = str.length;\n  if (l === 0) {\n    return \"\";\n  }\n  let suffLen = 0;\n  while (suffLen < l) {\n    const currChar = str.charAt(l - suffLen - 1);\n    if (currChar === c && !invert) {\n      suffLen++;\n    } else if (currChar !== c && invert) {\n      suffLen++;\n    } else {\n      break;\n    }\n  }\n  return str.slice(0, l - suffLen);\n}\nfunction findClosingBracket(str, b) {\n  if (str.indexOf(b[1]) === -1) {\n    return -1;\n  }\n  let level = 0;\n  for (let i = 0; i < str.length; i++) {\n    if (str[i] === \"\\\\\") {\n      i++;\n    } else if (str[i] === b[0]) {\n      level++;\n    } else if (str[i] === b[1]) {\n      level--;\n      if (level < 0) {\n        return i;\n      }\n    }\n  }\n  if (level > 0) {\n    return -2;\n  }\n  return -1;\n}\n\n// src/Tokenizer.ts\nfunction outputLink(cap, link2, raw, lexer2, rules) {\n  const href = link2.href;\n  const title = link2.title || null;\n  const text = cap[1].replace(rules.other.outputLinkReplace, \"$1\");\n  lexer2.state.inLink = true;\n  const token = {\n    type: cap[0].charAt(0) === \"!\" ? \"image\" : \"link\",\n    raw,\n    href,\n    title,\n    text,\n    tokens: lexer2.inlineTokens(text)\n  };\n  lexer2.state.inLink = false;\n  return token;\n}\nfunction indentCodeCompensation(raw, text, rules) {\n  const matchIndentToCode = raw.match(rules.other.indentCodeCompensation);\n  if (matchIndentToCode ==",
  "= null) {\n    return text;\n  }\n  const indentToCode = matchIndentToCode[1];\n  return text.split(\"\\n\").map((node) => {\n    const matchIndentInNode = node.match(rules.other.beginningSpace);\n    if (matchIndentInNode === null) {\n      return node;\n    }\n    const [indentInNode] = matchIndentInNode;\n    if (indentInNode.length >= indentToCode.length) {\n      return node.slice(indentToCode.length);\n    }\n    return node;\n  }).join(\"\\n\");\n}\nvar _Tokenizer = class {\n  options;\n  rules;\n  // set by the lexer\n  lexer;\n  // set by the lexer\n  constructor(options2) {\n    this.options = options2 || _defaults;\n  }\n  space(src) {\n    const cap = this.rules.block.newline.exec(src);\n    if (cap && cap[0].length > 0) {\n      return {\n        type: \"space\",\n        raw: cap[0]\n      };\n    }\n  }\n  code(src) {\n    const cap = this.rules.block.code.exec(src);\n    if (cap) {\n      const text = cap[0].replace(this.rules.other.codeRemoveIndent, \"\");\n      return {\n        type: \"code\",\n        raw: cap[0],\n        codeBlockStyle: \"indented\",\n        text: !this.options.pedantic ? rtrim(text, \"\\n\") : text\n      };\n    }\n  }\n  fences(src) {\n    const cap = this.rules.block.fences.exec(src);\n    if (cap) {\n      const raw = cap[0];\n      const text = indentCodeCompensation(raw, cap[3] || \"\", this.rules);\n      return {\n        type: \"code\",\n        raw,\n        lang: cap[2] ? cap[2].trim().replace(this.rules.inline.anyPunctuation, \"$1\") : cap[2],\n        text\n      };\n    }\n  }\n  heading(src) {\n    const cap = this.rules.block.heading.exec(src);\n    if (cap) {\n      let text = cap[2].trim();\n      if (this.rules.other.endingHash.test(text)) {\n        const trimmed = rtrim(text, \"#\");\n        if (this.options.pedantic) {\n          text = trimmed.trim();\n        } else if (!trimmed || this.rules.other.endingSpaceChar.test(trimmed)) {\n          text = trimmed.trim();\n        }\n      }\n      return {\n        type: \"heading\",\n        raw: cap[0],\n        depth: cap[1].length,\n        text,\n        tokens: this.lexer.inline(text)\n      };\n    }\n  }\n  hr(src) {\n    const cap = this.rules.block.hr.exec(src);\n    if (cap) {\n      return {\n        type: \"hr\",\n        raw: rtrim(cap[0], \"\\n\")\n      };\n    }\n  }\n  blockquote(src) {\n    const cap = this.rules.block.blockquote.exec(src);\n    if (cap) {\n      let lines = rtrim(cap[0], \"\\n\").split(\"\\n\");\n      let raw = \"\";\n      let text = \"\";\n      const tokens = [];\n      while (lines.length > 0) {\n        let inBlockquote = false;\n        const currentLines = [];\n        let i;\n        for (i = 0; i < lines.length; i++) {\n          if (this.rules.other.blockquoteStart.test(lines[i])) {\n            currentLines.push(lines[i]);\n            inBlockquote = true;\n          } else if (!inBlockquote) {\n            currentLines.push(lines[i]);\n          } else {\n            break;\n          }\n        }\n        lines = lines.slice(i);\n        const currentRaw = currentLines.join(\"\\n\");\n        const currentText = currentRaw.replace(this.rules.other.blockquoteSetextReplace, \"\\n    $1\").replace(this.rules.other.blockquoteSetextReplace2, \"\");\n        raw = raw ? `${raw}\n${currentRaw}` : currentRaw;\n        text = text ? `${text}\n${currentText}` : currentText;\n        const top = this.lexer.state.top;\n        this.lexer.state.top = true;\n        this.lexer.blockTokens(currentText, tokens, true);\n        this.lexer.state.top = top;\n        if (lines.length === 0) {\n          break;\n        }\n        const lastToken = tokens.at(-1);\n        if (lastToken?.type === \"code\") {\n          break;\n        } else if (lastToken?.type === \"blockquote\") {\n          const oldToken = lastToken;\n          const newText = oldToken.raw + \"\\n\" + lines.join(\"\\n\");\n          const newToken = this.blockquote(newText);\n          tokens[tokens.length - 1] = newToken;\n          raw = raw.substring(0, raw.length - oldToken.raw.length) + newToken.raw;\n          text = text.substring(0, text.length - oldToken.text.length) + newToken.text;\n          break;\n        } else if (lastToken?.type === \"list\") {\n          const oldToken = lastToken;\n          const newText = oldToken.raw + \"\\n\" + lines.join(\"\\n\");\n          const newToken = this.list(newText);\n          tokens[tokens.length - 1] = newToken;\n          raw = raw.substring(0, raw.length - lastToken.raw.length) + newToken.raw;\n          text = text.substring(0, text.length - oldToken.raw.length) + newToken.raw;\n          lines = newText.substring(tokens.at(-1).raw.length).split(\"\\n\");\n          continue;\n        }\n      }\n      return {\n        type: \"blockquote\",\n        raw,\n        tokens,\n        text\n      };\n    }\n  }\n  list(src) {\n    let cap = this.rules.block.list.exec(src);\n    if (cap) {\n      let bull = cap[1].trim();\n      const isordered = bull.length > 1;\n      const list2 = {\n        type: \"list\",\n        raw: \"\",\n        ordered: isordered,\n        start: isordered ? +bull.slice(0, -1) : \"\",\n        loose: false,\n        items: []\n      };\n      bull = isordered ? `\\\\d{1,9}\\\\${bull.slice(-1)}` : `\\\\${bull}`;\n      if (this.options.pedantic) {\n        bull = isordered ? bull : \"[*+-]\";\n      }\n      const itemRegex = this.rules.other.listItemRegex(bull);\n      let endsWithBlankLine = false;\n      while (src) {\n        let endEarly = false;\n        let raw = \"\";\n        let itemContents = \"\";\n        if (!(cap = itemRegex.exec(src))) {\n          break;\n        }\n        if (this.rules.block.hr.test(src)) {\n          break;\n        }\n        raw = cap[0];\n        src = src.substring(raw.length);\n        let line = cap[2].split(\"\\n\", 1)[0].replace(this.rules.other.listReplaceTabs, (t) => \" \".repeat(3 * t.length));\n        let nextLine = src.split(\"\\n\", 1)[0];\n        let blankLine = !line.trim();\n        let indent = 0;\n        if (this.options.pedantic) {\n          indent = 2;\n          itemContents = line.trimStart();\n        } else if (blankLine) {\n          indent = cap[1].length + 1;\n        } else {\n          indent = cap[2].search(this.rules",
  ".other.nonSpaceChar);\n          indent = indent > 4 ? 1 : indent;\n          itemContents = line.slice(indent);\n          indent += cap[1].length;\n        }\n        if (blankLine && this.rules.other.blankLine.test(nextLine)) {\n          raw += nextLine + \"\\n\";\n          src = src.substring(nextLine.length + 1);\n          endEarly = true;\n        }\n        if (!endEarly) {\n          const nextBulletRegex = this.rules.other.nextBulletRegex(indent);\n          const hrRegex = this.rules.other.hrRegex(indent);\n          const fencesBeginRegex = this.rules.other.fencesBeginRegex(indent);\n          const headingBeginRegex = this.rules.other.headingBeginRegex(indent);\n          const htmlBeginRegex = this.rules.other.htmlBeginRegex(indent);\n          while (src) {\n            const rawLine = src.split(\"\\n\", 1)[0];\n            let nextLineWithoutTabs;\n            nextLine = rawLine;\n            if (this.options.pedantic) {\n              nextLine = nextLine.replace(this.rules.other.listReplaceNesting, \"  \");\n              nextLineWithoutTabs = nextLine;\n            } else {\n              nextLineWithoutTabs = nextLine.replace(this.rules.other.tabCharGlobal, \"    \");\n            }\n            if (fencesBeginRegex.test(nextLine)) {\n              break;\n            }\n            if (headingBeginRegex.test(nextLine)) {\n              break;\n            }\n            if (htmlBeginRegex.test(nextLine)) {\n              break;\n            }\n            if (nextBulletRegex.test(nextLine)) {\n              break;\n            }\n            if (hrRegex.test(nextLine)) {\n              break;\n            }\n            if (nextLineWithoutTabs.search(this.rules.other.nonSpaceChar) >= indent || !nextLine.trim()) {\n              itemContents += \"\\n\" + nextLineWithoutTabs.slice(indent);\n            } else {\n              if (blankLine) {\n                break;\n              }\n              if (line.replace(this.rules.other.tabCharGlobal, \"    \").search(this.rules.other.nonSpaceChar) >= 4) {\n                break;\n              }\n              if (fencesBeginRegex.test(line)) {\n                break;\n              }\n              if (headingBeginRegex.test(line)) {\n                break;\n              }\n              if (hrRegex.test(line)) {\n                break;\n              }\n              itemContents += \"\\n\" + nextLine;\n            }\n            if (!blankLine && !nextLine.trim()) {\n              blankLine = true;\n            }\n            raw += rawLine + \"\\n\";\n            src = src.substring(rawLine.length + 1);\n            line = nextLineWithoutTabs.slice(indent);\n          }\n        }\n        if (!list2.loose) {\n          if (endsWithBlankLine) {\n            list2.loose = true;\n          } else if (this.rules.other.doubleBlankLine.test(raw)) {\n            endsWithBlankLine = true;\n          }\n        }\n        let istask = null;\n        let ischecked;\n        if (this.options.gfm) {\n          istask = this.rules.other.listIsTask.exec(itemContents);\n          if (istask) {\n            ischecked = istask[0] !== \"[ ] \";\n            itemContents = itemContents.replace(this.rules.other.listReplaceTask, \"\");\n          }\n        }\n        list2.items.push({\n          type: \"list_item\",\n          raw,\n          task: !!istask,\n          checked: ischecked,\n          loose: false,\n          text: itemContents,\n          tokens: []\n        });\n        list2.raw += raw;\n      }\n      const lastItem = list2.items.at(-1);\n      if (lastItem) {\n        lastItem.raw = lastItem.raw.trimEnd();\n        lastItem.text = lastItem.text.trimEnd();\n      } else {\n        return;\n      }\n      list2.raw = list2.raw.trimEnd();\n      for (let i = 0; i < list2.items.length; i++) {\n        this.lexer.state.top = false;\n        list2.items[i].tokens = this.lexer.blockTokens(list2.items[i].text, []);\n        if (!list2.loose) {\n          const spacers = list2.items[i].tokens.filter((t) => t.type === \"space\");\n          const hasMultipleLineBreaks = spacers.length > 0 && spacers.some((t) => this.rules.other.anyLine.test(t.raw));\n          list2.loose = hasMultipleLineBreaks;\n        }\n      }\n      if (list2.loose) {\n        for (let i = 0; i < list2.items.length; i++) {\n          list2.items[i].loose = true;\n        }\n      }\n      return list2;\n    }\n  }\n  html(src) {\n    const cap = this.rules.block.html.exec(src);\n    if (cap) {\n      const token = {\n        type: \"html\",\n        block: true,\n        raw: cap[0],\n        pre: cap[1] === \"pre\" || cap[1] === \"script\" || cap[1] === \"style\",\n        text: cap[0]\n      };\n      return token;\n    }\n  }\n  def(src) {\n    const cap = this.rules.block.def.exec(src);\n    if (cap) {\n      const tag2 = cap[1].toLowerCase().replace(this.rules.other.multipleSpaceGlobal, \" \");\n      const href = cap[2] ? cap[2].replace(this.rules.other.hrefBrackets, \"$1\").replace(this.rules.inline.anyPunctuation, \"$1\") : \"\";\n      const title = cap[3] ? cap[3].substring(1, cap[3].length - 1).replace(this.rules.inline.anyPunctuation, \"$1\") : cap[3];\n      return {\n        type: \"def\",\n        tag: tag2,\n        raw: cap[0],\n        href,\n        title\n      };\n    }\n  }\n  table(src) {\n    const cap = this.rules.block.table.exec(src);\n    if (!cap) {\n      return;\n    }\n    if (!this.rules.other.tableDelimiter.test(cap[2])) {\n      return;\n    }\n    const headers = splitCells(cap[1]);\n    const aligns = cap[2].replace(this.rules.other.tableAlignChars, \"\").split(\"|\");\n    const rows = cap[3]?.trim() ? cap[3].replace(this.rules.other.tableRowBlankLine, \"\").split(\"\\n\") : [];\n    const item = {\n      type: \"table\",\n      raw: cap[0],\n      header: [],\n      align: [],\n      rows: []\n    };\n    if (headers.length !== aligns.length) {\n      return;\n    }\n    for (const align of aligns) {\n      if (this.rules.other.tableAlignRight.test(align)) {\n        item.align.push(\"right\");\n      } else if (this.rules.other.tableAlignCenter.test(align)) {\n        item.align.push(\"center\");\n      } else if (this.rules.other.tableAlignLeft.t",
  "est(align)) {\n        item.align.push(\"left\");\n      } else {\n        item.align.push(null);\n      }\n    }\n    for (let i = 0; i < headers.length; i++) {\n      item.header.push({\n        text: headers[i],\n        tokens: this.lexer.inline(headers[i]),\n        header: true,\n        align: item.align[i]\n      });\n    }\n    for (const row of rows) {\n      item.rows.push(splitCells(row, item.header.length).map((cell, i) => {\n        return {\n          text: cell,\n          tokens: this.lexer.inline(cell),\n          header: false,\n          align: item.align[i]\n        };\n      }));\n    }\n    return item;\n  }\n  lheading(src) {\n    const cap = this.rules.block.lheading.exec(src);\n    if (cap) {\n      return {\n        type: \"heading\",\n        raw: cap[0],\n        depth: cap[2].charAt(0) === \"=\" ? 1 : 2,\n        text: cap[1],\n        tokens: this.lexer.inline(cap[1])\n      };\n    }\n  }\n  paragraph(src) {\n    const cap = this.rules.block.paragraph.exec(src);\n    if (cap) {\n      const text = cap[1].charAt(cap[1].length - 1) === \"\\n\" ? cap[1].slice(0, -1) : cap[1];\n      return {\n        type: \"paragraph\",\n        raw: cap[0],\n        text,\n        tokens: this.lexer.inline(text)\n      };\n    }\n  }\n  text(src) {\n    const cap = this.rules.block.text.exec(src);\n    if (cap) {\n      return {\n        type: \"text\",\n        raw: cap[0],\n        text: cap[0],\n        tokens: this.lexer.inline(cap[0])\n      };\n    }\n  }\n  escape(src) {\n    const cap = this.rules.inline.escape.exec(src);\n    if (cap) {\n      return {\n        type: \"escape\",\n        raw: cap[0],\n        text: cap[1]\n      };\n    }\n  }\n  tag(src) {\n    const cap = this.rules.inline.tag.exec(src);\n    if (cap) {\n      if (!this.lexer.state.inLink && this.rules.other.startATag.test(cap[0])) {\n        this.lexer.state.inLink = true;\n      } else if (this.lexer.state.inLink && this.rules.other.endATag.test(cap[0])) {\n        this.lexer.state.inLink = false;\n      }\n      if (!this.lexer.state.inRawBlock && this.rules.other.startPreScriptTag.test(cap[0])) {\n        this.lexer.state.inRawBlock = true;\n      } else if (this.lexer.state.inRawBlock && this.rules.other.endPreScriptTag.test(cap[0])) {\n        this.lexer.state.inRawBlock = false;\n      }\n      return {\n        type: \"html\",\n        raw: cap[0],\n        inLink: this.lexer.state.inLink,\n        inRawBlock: this.lexer.state.inRawBlock,\n        block: false,\n        text: cap[0]\n      };\n    }\n  }\n  link(src) {\n    const cap = this.rules.inline.link.exec(src);\n    if (cap) {\n      const trimmedUrl = cap[2].trim();\n      if (!this.options.pedantic && this.rules.other.startAngleBracket.test(trimmedUrl)) {\n        if (!this.rules.other.endAngleBracket.test(trimmedUrl)) {\n          return;\n        }\n        const rtrimSlash = rtrim(trimmedUrl.slice(0, -1), \"\\\\\");\n        if ((trimmedUrl.length - rtrimSlash.length) % 2 === 0) {\n          return;\n        }\n      } else {\n        const lastParenIndex = findClosingBracket(cap[2], \"()\");\n        if (lastParenIndex === -2) {\n          return;\n        }\n        if (lastParenIndex > -1) {\n          const start = cap[0].indexOf(\"!\") === 0 ? 5 : 4;\n          const linkLen = start + cap[1].length + lastParenIndex;\n          cap[2] = cap[2].substring(0, lastParenIndex);\n          cap[0] = cap[0].substring(0, linkLen).trim();\n          cap[3] = \"\";\n        }\n      }\n      let href = cap[2];\n      let title = \"\";\n      if (this.options.pedantic) {\n        const link2 = this.rules.other.pedanticHrefTitle.exec(href);\n        if (link2) {\n          href = link2[1];\n          title = link2[3];\n        }\n      } else {\n        title = cap[3] ? cap[3].slice(1, -1) : \"\";\n      }\n      href = href.trim();\n      if (this.rules.other.startAngleBracket.test(href)) {\n        if (this.options.pedantic && !this.rules.other.endAngleBracket.test(trimmedUrl)) {\n          href = href.slice(1);\n        } else {\n          href = href.slice(1, -1);\n        }\n      }\n      return outputLink(cap, {\n        href: href ? href.replace(this.rules.inline.anyPunctuation, \"$1\") : href,\n        title: title ? title.replace(this.rules.inline.anyPunctuation, \"$1\") : title\n      }, cap[0], this.lexer, this.rules);\n    }\n  }\n  reflink(src, links) {\n    let cap;\n    if ((cap = this.rules.inline.reflink.exec(src)) || (cap = this.rules.inline.nolink.exec(src))) {\n      const linkString = (cap[2] || cap[1]).replace(this.rules.other.multipleSpaceGlobal, \" \");\n      const link2 = links[linkString.toLowerCase()];\n      if (!link2) {\n        const text = cap[0].charAt(0);\n        return {\n          type: \"text\",\n          raw: text,\n          text\n        };\n      }\n      return outputLink(cap, link2, cap[0], this.lexer, this.rules);\n    }\n  }\n  emStrong(src, maskedSrc, prevChar = \"\") {\n    let match = this.rules.inline.emStrongLDelim.exec(src);\n    if (!match) return;\n    if (match[3] && prevChar.match(this.rules.other.unicodeAlphaNumeric)) return;\n    const nextChar = match[1] || match[2] || \"\";\n    if (!nextChar || !prevChar || this.rules.inline.punctuation.exec(prevChar)) {\n      const lLength = [...match[0]].length - 1;\n      let rDelim, rLength, delimTotal = lLength, midDelimTotal = 0;\n      const endReg = match[0][0] === \"*\" ? this.rules.inline.emStrongRDelimAst : this.rules.inline.emStrongRDelimUnd;\n      endReg.lastIndex = 0;\n      maskedSrc = maskedSrc.slice(-1 * src.length + lLength);\n      while ((match = endReg.exec(maskedSrc)) != null) {\n        rDelim = match[1] || match[2] || match[3] || match[4] || match[5] || match[6];\n        if (!rDelim) continue;\n        rLength = [...rDelim].length;\n        if (match[3] || match[4]) {\n          delimTotal += rLength;\n          continue;\n        } else if (match[5] || match[6]) {\n          if (lLength % 3 && !((lLength + rLength) % 3)) {\n            midDelimTotal += rLength;\n            continue;\n          }\n        }\n        delimTotal -= rLength;\n        if (delimTotal > 0) continue;\n        rLength = Math.min(rLength, rLength + delimTotal + m",
  "idDelimTotal);\n        const lastCharLength = [...match[0]][0].length;\n        const raw = src.slice(0, lLength + match.index + lastCharLength + rLength);\n        if (Math.min(lLength, rLength) % 2) {\n          const text2 = raw.slice(1, -1);\n          return {\n            type: \"em\",\n            raw,\n            text: text2,\n            tokens: this.lexer.inlineTokens(text2)\n          };\n        }\n        const text = raw.slice(2, -2);\n        return {\n          type: \"strong\",\n          raw,\n          text,\n          tokens: this.lexer.inlineTokens(text)\n        };\n      }\n    }\n  }\n  codespan(src) {\n    const cap = this.rules.inline.code.exec(src);\n    if (cap) {\n      let text = cap[2].replace(this.rules.other.newLineCharGlobal, \" \");\n      const hasNonSpaceChars = this.rules.other.nonSpaceChar.test(text);\n      const hasSpaceCharsOnBothEnds = this.rules.other.startingSpaceChar.test(text) && this.rules.other.endingSpaceChar.test(text);\n      if (hasNonSpaceChars && hasSpaceCharsOnBothEnds) {\n        text = text.substring(1, text.length - 1);\n      }\n      return {\n        type: \"codespan\",\n        raw: cap[0],\n        text\n      };\n    }\n  }\n  br(src) {\n    const cap = this.rules.inline.br.exec(src);\n    if (cap) {\n      return {\n        type: \"br\",\n        raw: cap[0]\n      };\n    }\n  }\n  del(src) {\n    const cap = this.rules.inline.del.exec(src);\n    if (cap) {\n      return {\n        type: \"del\",\n        raw: cap[0],\n        text: cap[2],\n        tokens: this.lexer.inlineTokens(cap[2])\n      };\n    }\n  }\n  autolink(src) {\n    const cap = this.rules.inline.autolink.exec(src);\n    if (cap) {\n      let text, href;\n      if (cap[2] === \"@\") {\n        text = cap[1];\n        href = \"mailto:\" + text;\n      } else {\n        text = cap[1];\n        href = text;\n      }\n      return {\n        type: \"link\",\n        raw: cap[0],\n        text,\n        href,\n        tokens: [\n          {\n            type: \"text\",\n            raw: text,\n            text\n          }\n        ]\n      };\n    }\n  }\n  url(src) {\n    let cap;\n    if (cap = this.rules.inline.url.exec(src)) {\n      let text, href;\n      if (cap[2] === \"@\") {\n        text = cap[0];\n        href = \"mailto:\" + text;\n      } else {\n        let prevCapZero;\n        do {\n          prevCapZero = cap[0];\n          cap[0] = this.rules.inline._backpedal.exec(cap[0])?.[0] ?? \"\";\n        } while (prevCapZero !== cap[0]);\n        text = cap[0];\n        if (cap[1] === \"www.\") {\n          href = \"http://\" + cap[0];\n        } else {\n          href = cap[0];\n        }\n      }\n      return {\n        type: \"link\",\n        raw: cap[0],\n        text,\n        href,\n        tokens: [\n          {\n            type: \"text\",\n            raw: text,\n            text\n          }\n        ]\n      };\n    }\n  }\n  inlineText(src) {\n    const cap = this.rules.inline.text.exec(src);\n    if (cap) {\n      const escaped = this.lexer.state.inRawBlock;\n      return {\n        type: \"text\",\n        raw: cap[0],\n        text: cap[0],\n        escaped\n      };\n    }\n  }\n};\n\n// src/Lexer.ts\nvar _Lexer = class __Lexer {\n  tokens;\n  options;\n  state;\n  tokenizer;\n  inlineQueue;\n  constructor(options2) {\n    this.tokens = [];\n    this.tokens.links = /* @__PURE__ */ Object.create(null);\n    this.options = options2 || _defaults;\n    this.options.tokenizer = this.options.tokenizer || new _Tokenizer();\n    this.tokenizer = this.options.tokenizer;\n    this.tokenizer.options = this.options;\n    this.tokenizer.lexer = this;\n    this.inlineQueue = [];\n    this.state = {\n      inLink: false,\n      inRawBlock: false,\n      top: true\n    };\n    const rules = {\n      other,\n      block: block.normal,\n      inline: inline.normal\n    };\n    if (this.options.pedantic) {\n      rules.block = block.pedantic;\n      rules.inline = inline.pedantic;\n    } else if (this.options.gfm) {\n      rules.block = block.gfm;\n      if (this.options.breaks) {\n        rules.inline = inline.breaks;\n      } else {\n        rules.inline = inline.gfm;\n      }\n    }\n    this.tokenizer.rules = rules;\n  }\n  /**\n   * Expose Rules\n   */\n  static get rules() {\n    return {\n      block,\n      inline\n    };\n  }\n  /**\n   * Static Lex Method\n   */\n  static lex(src, options2) {\n    const lexer2 = new __Lexer(options2);\n    return lexer2.lex(src);\n  }\n  /**\n   * Static Lex Inline Method\n   */\n  static lexInline(src, options2) {\n    const lexer2 = new __Lexer(options2);\n    return lexer2.inlineTokens(src);\n  }\n  /**\n   * Preprocessing\n   */\n  lex(src) {\n    src = src.replace(other.carriageReturn, \"\\n\");\n    this.blockTokens(src, this.tokens);\n    for (let i = 0; i < this.inlineQueue.length; i++) {\n      const next = this.inlineQueue[i];\n      this.inlineTokens(next.src, next.tokens);\n    }\n    this.inlineQueue = [];\n    return this.tokens;\n  }\n  blockTokens(src, tokens = [], lastParagraphClipped = false) {\n    if (this.options.pedantic) {\n      src = src.replace(other.tabCharGlobal, \"    \").replace(other.spaceLine, \"\");\n    }\n    while (src) {\n      let token;\n      if (this.options.extensions?.block?.some((extTokenizer) => {\n        if (token = extTokenizer.call({ lexer: this }, src, tokens)) {\n          src = src.substring(token.raw.length);\n          tokens.push(token);\n          return true;\n        }\n        return false;\n      })) {\n        continue;\n      }\n      if (token = this.tokenizer.space(src)) {\n        src = src.substring(token.raw.length);\n        const lastToken = tokens.at(-1);\n        if (token.raw.length === 1 && lastToken !== void 0) {\n          lastToken.raw += \"\\n\";\n        } else {\n          tokens.push(token);\n        }\n        continue;\n      }\n      if (token = this.tokenizer.code(src)) {\n        src = src.substring(token.raw.length);\n        const lastToken = tokens.at(-1);\n        if (lastToken?.type === \"paragraph\" || lastToken?.type === \"text\") {\n          lastToken.raw += \"\\n\" + token.raw;\n          lastToken.text += \"\\n\" + token.text;\n          this.inlineQueue.at(-1).src = lastToken.text;\n        } else {\n  ",
  "        tokens.push(token);\n        }\n        continue;\n      }\n      if (token = this.tokenizer.fences(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.heading(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.hr(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.blockquote(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.list(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.html(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.def(src)) {\n        src = src.substring(token.raw.length);\n        const lastToken = tokens.at(-1);\n        if (lastToken?.type === \"paragraph\" || lastToken?.type === \"text\") {\n          lastToken.raw += \"\\n\" + token.raw;\n          lastToken.text += \"\\n\" + token.raw;\n          this.inlineQueue.at(-1).src = lastToken.text;\n        } else if (!this.tokens.links[token.tag]) {\n          this.tokens.links[token.tag] = {\n            href: token.href,\n            title: token.title\n          };\n        }\n        continue;\n      }\n      if (token = this.tokenizer.table(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.lheading(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      let cutSrc = src;\n      if (this.options.extensions?.startBlock) {\n        let startIndex = Infinity;\n        const tempSrc = src.slice(1);\n        let tempStart;\n        this.options.extensions.startBlock.forEach((getStartIndex) => {\n          tempStart = getStartIndex.call({ lexer: this }, tempSrc);\n          if (typeof tempStart === \"number\" && tempStart >= 0) {\n            startIndex = Math.min(startIndex, tempStart);\n          }\n        });\n        if (startIndex < Infinity && startIndex >= 0) {\n          cutSrc = src.substring(0, startIndex + 1);\n        }\n      }\n      if (this.state.top && (token = this.tokenizer.paragraph(cutSrc))) {\n        const lastToken = tokens.at(-1);\n        if (lastParagraphClipped && lastToken?.type === \"paragraph\") {\n          lastToken.raw += \"\\n\" + token.raw;\n          lastToken.text += \"\\n\" + token.text;\n          this.inlineQueue.pop();\n          this.inlineQueue.at(-1).src = lastToken.text;\n        } else {\n          tokens.push(token);\n        }\n        lastParagraphClipped = cutSrc.length !== src.length;\n        src = src.substring(token.raw.length);\n        continue;\n      }\n      if (token = this.tokenizer.text(src)) {\n        src = src.substring(token.raw.length);\n        const lastToken = tokens.at(-1);\n        if (lastToken?.type === \"text\") {\n          lastToken.raw += \"\\n\" + token.raw;\n          lastToken.text += \"\\n\" + token.text;\n          this.inlineQueue.pop();\n          this.inlineQueue.at(-1).src = lastToken.text;\n        } else {\n          tokens.push(token);\n        }\n        continue;\n      }\n      if (src) {\n        const errMsg = \"Infinite loop on byte: \" + src.charCodeAt(0);\n        if (this.options.silent) {\n          console.error(errMsg);\n          break;\n        } else {\n          throw new Error(errMsg);\n        }\n      }\n    }\n    this.state.top = true;\n    return tokens;\n  }\n  inline(src, tokens = []) {\n    this.inlineQueue.push({ src, tokens });\n    return tokens;\n  }\n  /**\n   * Lexing/Compiling\n   */\n  inlineTokens(src, tokens = []) {\n    let maskedSrc = src;\n    let match = null;\n    if (this.tokens.links) {\n      const links = Object.keys(this.tokens.links);\n      if (links.length > 0) {\n        while ((match = this.tokenizer.rules.inline.reflinkSearch.exec(maskedSrc)) != null) {\n          if (links.includes(match[0].slice(match[0].lastIndexOf(\"[\") + 1, -1))) {\n            maskedSrc = maskedSrc.slice(0, match.index) + \"[\" + \"a\".repeat(match[0].length - 2) + \"]\" + maskedSrc.slice(this.tokenizer.rules.inline.reflinkSearch.lastIndex);\n          }\n        }\n      }\n    }\n    while ((match = this.tokenizer.rules.inline.anyPunctuation.exec(maskedSrc)) != null) {\n      maskedSrc = maskedSrc.slice(0, match.index) + \"++\" + maskedSrc.slice(this.tokenizer.rules.inline.anyPunctuation.lastIndex);\n    }\n    while ((match = this.tokenizer.rules.inline.blockSkip.exec(maskedSrc)) != null) {\n      maskedSrc = maskedSrc.slice(0, match.index) + \"[\" + \"a\".repeat(match[0].length - 2) + \"]\" + maskedSrc.slice(this.tokenizer.rules.inline.blockSkip.lastIndex);\n    }\n    let keepPrevChar = false;\n    let prevChar = \"\";\n    while (src) {\n      if (!keepPrevChar) {\n        prevChar = \"\";\n      }\n      keepPrevChar = false;\n      let token;\n      if (this.options.extensions?.inline?.some((extTokenizer) => {\n        if (token = extTokenizer.call({ lexer: this }, src, tokens)) {\n          src = src.substring(token.raw.length);\n          tokens.push(token);\n          return true;\n        }\n        return false;\n      })) {\n        continue;\n      }\n      if (token = this.tokenizer.escape(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.tag(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.link(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.reflink(src, this.tokens.links)) {\n        src = src.substring(token.raw.length);\n        const lastToken = tokens.at(-1);\n        if (token.type === ",
  "\"text\" && lastToken?.type === \"text\") {\n          lastToken.raw += token.raw;\n          lastToken.text += token.text;\n        } else {\n          tokens.push(token);\n        }\n        continue;\n      }\n      if (token = this.tokenizer.emStrong(src, maskedSrc, prevChar)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.codespan(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.br(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.del(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (token = this.tokenizer.autolink(src)) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      if (!this.state.inLink && (token = this.tokenizer.url(src))) {\n        src = src.substring(token.raw.length);\n        tokens.push(token);\n        continue;\n      }\n      let cutSrc = src;\n      if (this.options.extensions?.startInline) {\n        let startIndex = Infinity;\n        const tempSrc = src.slice(1);\n        let tempStart;\n        this.options.extensions.startInline.forEach((getStartIndex) => {\n          tempStart = getStartIndex.call({ lexer: this }, tempSrc);\n          if (typeof tempStart === \"number\" && tempStart >= 0) {\n            startIndex = Math.min(startIndex, tempStart);\n          }\n        });\n        if (startIndex < Infinity && startIndex >= 0) {\n          cutSrc = src.substring(0, startIndex + 1);\n        }\n      }\n      if (token = this.tokenizer.inlineText(cutSrc)) {\n        src = src.substring(token.raw.length);\n        if (token.raw.slice(-1) !== \"_\") {\n          prevChar = token.raw.slice(-1);\n        }\n        keepPrevChar = true;\n        const lastToken = tokens.at(-1);\n        if (lastToken?.type === \"text\") {\n          lastToken.raw += token.raw;\n          lastToken.text += token.text;\n        } else {\n          tokens.push(token);\n        }\n        continue;\n      }\n      if (src) {\n        const errMsg = \"Infinite loop on byte: \" + src.charCodeAt(0);\n        if (this.options.silent) {\n          console.error(errMsg);\n          break;\n        } else {\n          throw new Error(errMsg);\n        }\n      }\n    }\n    return tokens;\n  }\n};\n\n// src/Renderer.ts\nvar _Renderer = class {\n  options;\n  parser;\n  // set by the parser\n  constructor(options2) {\n    this.options = options2 || _defaults;\n  }\n  space(token) {\n    return \"\";\n  }\n  code({ text, lang, escaped }) {\n    const langString = (lang || \"\").match(other.notSpaceStart)?.[0];\n    const code = text.replace(other.endingNewline, \"\") + \"\\n\";\n    if (!langString) {\n      return \"<pre><code>\" + (escaped ? code : escape2(code, true)) + \"</code></pre>\\n\";\n    }\n    return '<pre><code class=\"language-' + escape2(langString) + '\">' + (escaped ? code : escape2(code, true)) + \"</code></pre>\\n\";\n  }\n  blockquote({ tokens }) {\n    const body = this.parser.parse(tokens);\n    return `<blockquote>\n${body}</blockquote>\n`;\n  }\n  html({ text }) {\n    return text;\n  }\n  heading({ tokens, depth }) {\n    return `<h${depth}>${this.parser.parseInline(tokens)}</h${depth}>\n`;\n  }\n  hr(token) {\n    return \"<hr>\\n\";\n  }\n  list(token) {\n    const ordered = token.ordered;\n    const start = token.start;\n    let body = \"\";\n    for (let j = 0; j < token.items.length; j++) {\n      const item = token.items[j];\n      body += this.listitem(item);\n    }\n    const type = ordered ? \"ol\" : \"ul\";\n    const startAttr = ordered && start !== 1 ? ' start=\"' + start + '\"' : \"\";\n    return \"<\" + type + startAttr + \">\\n\" + body + \"</\" + type + \">\\n\";\n  }\n  listitem(item) {\n    let itemBody = \"\";\n    if (item.task) {\n      const checkbox = this.checkbox({ checked: !!item.checked });\n      if (item.loose) {\n        if (item.tokens[0]?.type === \"paragraph\") {\n          item.tokens[0].text = checkbox + \" \" + item.tokens[0].text;\n          if (item.tokens[0].tokens && item.tokens[0].tokens.length > 0 && item.tokens[0].tokens[0].type === \"text\") {\n            item.tokens[0].tokens[0].text = checkbox + \" \" + escape2(item.tokens[0].tokens[0].text);\n            item.tokens[0].tokens[0].escaped = true;\n          }\n        } else {\n          item.tokens.unshift({\n            type: \"text\",\n            raw: checkbox + \" \",\n            text: checkbox + \" \",\n            escaped: true\n          });\n        }\n      } else {\n        itemBody += checkbox + \" \";\n      }\n    }\n    itemBody += this.parser.parse(item.tokens, !!item.loose);\n    return `<li>${itemBody}</li>\n`;\n  }\n  checkbox({ checked }) {\n    return \"<input \" + (checked ? 'checked=\"\" ' : \"\") + 'disabled=\"\" type=\"checkbox\">';\n  }\n  paragraph({ tokens }) {\n    return `<p>${this.parser.parseInline(tokens)}</p>\n`;\n  }\n  table(token) {\n    let header = \"\";\n    let cell = \"\";\n    for (let j = 0; j < token.header.length; j++) {\n      cell += this.tablecell(token.header[j]);\n    }\n    header += this.tablerow({ text: cell });\n    let body = \"\";\n    for (let j = 0; j < token.rows.length; j++) {\n      const row = token.rows[j];\n      cell = \"\";\n      for (let k = 0; k < row.length; k++) {\n        cell += this.tablecell(row[k]);\n      }\n      body += this.tablerow({ text: cell });\n    }\n    if (body) body = `<tbody>${body}</tbody>`;\n    return \"<table>\\n<thead>\\n\" + header + \"</thead>\\n\" + body + \"</table>\\n\";\n  }\n  tablerow({ text }) {\n    return `<tr>\n${text}</tr>\n`;\n  }\n  tablecell(token) {\n    const content = this.parser.parseInline(token.tokens);\n    const type = token.header ? \"th\" : \"td\";\n    const tag2 = token.align ? `<${type} align=\"${token.align}\">` : `<${type}>`;\n    return tag2 + content + `</${type}>\n`;\n  }\n  /**\n   * span level renderer\n   */\n  strong({ tokens }) {\n    return `<strong>${this.parser.parseInline(tokens)}</strong>`;\n  }\n  em({ tokens }) {",
  "\n    return `<em>${this.parser.parseInline(tokens)}</em>`;\n  }\n  codespan({ text }) {\n    return `<code>${escape2(text, true)}</code>`;\n  }\n  br(token) {\n    return \"<br>\";\n  }\n  del({ tokens }) {\n    return `<del>${this.parser.parseInline(tokens)}</del>`;\n  }\n  link({ href, title, tokens }) {\n    const text = this.parser.parseInline(tokens);\n    const cleanHref = cleanUrl(href);\n    if (cleanHref === null) {\n      return text;\n    }\n    href = cleanHref;\n    let out = '<a href=\"' + href + '\"';\n    if (title) {\n      out += ' title=\"' + escape2(title) + '\"';\n    }\n    out += \">\" + text + \"</a>\";\n    return out;\n  }\n  image({ href, title, text, tokens }) {\n    if (tokens) {\n      text = this.parser.parseInline(tokens, this.parser.textRenderer);\n    }\n    const cleanHref = cleanUrl(href);\n    if (cleanHref === null) {\n      return escape2(text);\n    }\n    href = cleanHref;\n    let out = `<img src=\"${href}\" alt=\"${text}\"`;\n    if (title) {\n      out += ` title=\"${escape2(title)}\"`;\n    }\n    out += \">\";\n    return out;\n  }\n  text(token) {\n    return \"tokens\" in token && token.tokens ? this.parser.parseInline(token.tokens) : \"escaped\" in token && token.escaped ? token.text : escape2(token.text);\n  }\n};\n\n// src/TextRenderer.ts\nvar _TextRenderer = class {\n  // no need for block level renderers\n  strong({ text }) {\n    return text;\n  }\n  em({ text }) {\n    return text;\n  }\n  codespan({ text }) {\n    return text;\n  }\n  del({ text }) {\n    return text;\n  }\n  html({ text }) {\n    return text;\n  }\n  text({ text }) {\n    return text;\n  }\n  link({ text }) {\n    return \"\" + text;\n  }\n  image({ text }) {\n    return \"\" + text;\n  }\n  br() {\n    return \"\";\n  }\n};\n\n// src/Parser.ts\nvar _Parser = class __Parser {\n  options;\n  renderer;\n  textRenderer;\n  constructor(options2) {\n    this.options = options2 || _defaults;\n    this.options.renderer = this.options.renderer || new _Renderer();\n    this.renderer = this.options.renderer;\n    this.renderer.options = this.options;\n    this.renderer.parser = this;\n    this.textRenderer = new _TextRenderer();\n  }\n  /**\n   * Static Parse Method\n   */\n  static parse(tokens, options2) {\n    const parser2 = new __Parser(options2);\n    return parser2.parse(tokens);\n  }\n  /**\n   * Static Parse Inline Method\n   */\n  static parseInline(tokens, options2) {\n    const parser2 = new __Parser(options2);\n    return parser2.parseInline(tokens);\n  }\n  /**\n   * Parse Loop\n   */\n  parse(tokens, top = true) {\n    let out = \"\";\n    for (let i = 0; i < tokens.length; i++) {\n      const anyToken = tokens[i];\n      if (this.options.extensions?.renderers?.[anyToken.type]) {\n        const genericToken = anyToken;\n        const ret = this.options.extensions.renderers[genericToken.type].call({ parser: this }, genericToken);\n        if (ret !== false || ![\"space\", \"hr\", \"heading\", \"code\", \"table\", \"blockquote\", \"list\", \"html\", \"paragraph\", \"text\"].includes(genericToken.type)) {\n          out += ret || \"\";\n          continue;\n        }\n      }\n      const token = anyToken;\n      switch (token.type) {\n        case \"space\": {\n          out += this.renderer.space(token);\n          continue;\n        }\n        case \"hr\": {\n          out += this.renderer.hr(token);\n          continue;\n        }\n        case \"heading\": {\n          out += this.renderer.heading(token);\n          continue;\n        }\n        case \"code\": {\n          out += this.renderer.code(token);\n          continue;\n        }\n        case \"table\": {\n          out += this.renderer.table(token);\n          continue;\n        }\n        case \"blockquote\": {\n          out += this.renderer.blockquote(token);\n          continue;\n        }\n        case \"list\": {\n          out += this.renderer.list(token);\n          continue;\n        }\n        case \"html\": {\n          out += this.renderer.html(token);\n          continue;\n        }\n        case \"paragraph\": {\n          out += this.renderer.paragraph(token);\n          continue;\n        }\n        case \"text\": {\n          let textToken = token;\n          let body = this.renderer.text(textToken);\n          while (i + 1 < tokens.length && tokens[i + 1].type === \"text\") {\n            textToken = tokens[++i];\n            body += \"\\n\" + this.renderer.text(textToken);\n          }\n          if (top) {\n            out += this.renderer.paragraph({\n              type: \"paragraph\",\n              raw: body,\n              text: body,\n              tokens: [{ type: \"text\", raw: body, text: body, escaped: true }]\n            });\n          } else {\n            out += body;\n          }\n          continue;\n        }\n        default: {\n          const errMsg = 'Token with \"' + token.type + '\" type was not found.';\n          if (this.options.silent) {\n            console.error(errMsg);\n            return \"\";\n          } else {\n            throw new Error(errMsg);\n          }\n        }\n      }\n    }\n    return out;\n  }\n  /**\n   * Parse Inline Tokens\n   */\n  parseInline(tokens, renderer = this.renderer) {\n    let out = \"\";\n    for (let i = 0; i < tokens.length; i++) {\n      const anyToken = tokens[i];\n      if (this.options.extensions?.renderers?.[anyToken.type]) {\n        const ret = this.options.extensions.renderers[anyToken.type].call({ parser: this }, anyToken);\n        if (ret !== false || ![\"escape\", \"html\", \"link\", \"image\", \"strong\", \"em\", \"codespan\", \"br\", \"del\", \"text\"].includes(anyToken.type)) {\n          out += ret || \"\";\n          continue;\n        }\n      }\n      const token = anyToken;\n      switch (token.type) {\n        case \"escape\": {\n          out += renderer.text(token);\n          break;\n        }\n        case \"html\": {\n          out += renderer.html(token);\n          break;\n        }\n        case \"link\": {\n          out += renderer.link(token);\n          break;\n        }\n        case \"image\": {\n          out += renderer.image(token);\n          break;\n        }\n        case \"strong\": {\n          out += renderer.strong(token);\n          break;\n        }\n        case \"em\": {\n          out += renderer.em(token",
  ");\n          break;\n        }\n        case \"codespan\": {\n          out += renderer.codespan(token);\n          break;\n        }\n        case \"br\": {\n          out += renderer.br(token);\n          break;\n        }\n        case \"del\": {\n          out += renderer.del(token);\n          break;\n        }\n        case \"text\": {\n          out += renderer.text(token);\n          break;\n        }\n        default: {\n          const errMsg = 'Token with \"' + token.type + '\" type was not found.';\n          if (this.options.silent) {\n            console.error(errMsg);\n            return \"\";\n          } else {\n            throw new Error(errMsg);\n          }\n        }\n      }\n    }\n    return out;\n  }\n};\n\n// src/Hooks.ts\nvar _Hooks = class {\n  options;\n  block;\n  constructor(options2) {\n    this.options = options2 || _defaults;\n  }\n  static passThroughHooks = /* @__PURE__ */ new Set([\n    \"preprocess\",\n    \"postprocess\",\n    \"processAllTokens\"\n  ]);\n  /**\n   * Process markdown before marked\n   */\n  preprocess(markdown) {\n    return markdown;\n  }\n  /**\n   * Process HTML after marked is finished\n   */\n  postprocess(html2) {\n    return html2;\n  }\n  /**\n   * Process all tokens before walk tokens\n   */\n  processAllTokens(tokens) {\n    return tokens;\n  }\n  /**\n   * Provide function to tokenize markdown\n   */\n  provideLexer() {\n    return this.block ? _Lexer.lex : _Lexer.lexInline;\n  }\n  /**\n   * Provide function to parse tokens\n   */\n  provideParser() {\n    return this.block ? _Parser.parse : _Parser.parseInline;\n  }\n};\n\n// src/Instance.ts\nvar Marked = class {\n  defaults = _getDefaults();\n  options = this.setOptions;\n  parse = this.parseMarkdown(true);\n  parseInline = this.parseMarkdown(false);\n  Parser = _Parser;\n  Renderer = _Renderer;\n  TextRenderer = _TextRenderer;\n  Lexer = _Lexer;\n  Tokenizer = _Tokenizer;\n  Hooks = _Hooks;\n  constructor(...args) {\n    this.use(...args);\n  }\n  /**\n   * Run callback for every token\n   */\n  walkTokens(tokens, callback) {\n    let values = [];\n    for (const token of tokens) {\n      values = values.concat(callback.call(this, token));\n      switch (token.type) {\n        case \"table\": {\n          const tableToken = token;\n          for (const cell of tableToken.header) {\n            values = values.concat(this.walkTokens(cell.tokens, callback));\n          }\n          for (const row of tableToken.rows) {\n            for (const cell of row) {\n              values = values.concat(this.walkTokens(cell.tokens, callback));\n            }\n          }\n          break;\n        }\n        case \"list\": {\n          const listToken = token;\n          values = values.concat(this.walkTokens(listToken.items, callback));\n          break;\n        }\n        default: {\n          const genericToken = token;\n          if (this.defaults.extensions?.childTokens?.[genericToken.type]) {\n            this.defaults.extensions.childTokens[genericToken.type].forEach((childTokens) => {\n              const tokens2 = genericToken[childTokens].flat(Infinity);\n              values = values.concat(this.walkTokens(tokens2, callback));\n            });\n          } else if (genericToken.tokens) {\n            values = values.concat(this.walkTokens(genericToken.tokens, callback));\n          }\n        }\n      }\n    }\n    return values;\n  }\n  use(...args) {\n    const extensions = this.defaults.extensions || { renderers: {}, childTokens: {} };\n    args.forEach((pack) => {\n      const opts = { ...pack };\n      opts.async = this.defaults.async || opts.async || false;\n      if (pack.extensions) {\n        pack.extensions.forEach((ext) => {\n          if (!ext.name) {\n            throw new Error(\"extension name required\");\n          }\n          if (\"renderer\" in ext) {\n            const prevRenderer = extensions.renderers[ext.name];\n            if (prevRenderer) {\n              extensions.renderers[ext.name] = function(...args2) {\n                let ret = ext.renderer.apply(this, args2);\n                if (ret === false) {\n                  ret = prevRenderer.apply(this, args2);\n                }\n                return ret;\n              };\n            } else {\n              extensions.renderers[ext.name] = ext.renderer;\n            }\n          }\n          if (\"tokenizer\" in ext) {\n            if (!ext.level || ext.level !== \"block\" && ext.level !== \"inline\") {\n              throw new Error(\"extension level must be 'block' or 'inline'\");\n            }\n            const extLevel = extensions[ext.level];\n            if (extLevel) {\n              extLevel.unshift(ext.tokenizer);\n            } else {\n              extensions[ext.level] = [ext.tokenizer];\n            }\n            if (ext.start) {\n              if (ext.level === \"block\") {\n                if (extensions.startBlock) {\n                  extensions.startBlock.push(ext.start);\n                } else {\n                  extensions.startBlock = [ext.start];\n                }\n              } else if (ext.level === \"inline\") {\n                if (extensions.startInline) {\n                  extensions.startInline.push(ext.start);\n                } else {\n                  extensions.startInline = [ext.start];\n                }\n              }\n            }\n          }\n          if (\"childTokens\" in ext && ext.childTokens) {\n            extensions.childTokens[ext.name] = ext.childTokens;\n          }\n        });\n        opts.extensions = extensions;\n      }\n      if (pack.renderer) {\n        const renderer = this.defaults.renderer || new _Renderer(this.defaults);\n        for (const prop in pack.renderer) {\n          if (!(prop in renderer)) {\n            throw new Error(`renderer '${prop}' does not exist`);\n          }\n          if ([\"options\", \"parser\"].includes(prop)) {\n            continue;\n          }\n          const rendererProp = prop;\n          const rendererFunc = pack.renderer[rendererProp];\n          const prevRenderer = renderer[rendererProp];\n          renderer[rendererProp] = (...args2) => {\n            let ret = rendererFunc.apply(renderer, args2);\n            if",
  " (ret === false) {\n              ret = prevRenderer.apply(renderer, args2);\n            }\n            return ret || \"\";\n          };\n        }\n        opts.renderer = renderer;\n      }\n      if (pack.tokenizer) {\n        const tokenizer = this.defaults.tokenizer || new _Tokenizer(this.defaults);\n        for (const prop in pack.tokenizer) {\n          if (!(prop in tokenizer)) {\n            throw new Error(`tokenizer '${prop}' does not exist`);\n          }\n          if ([\"options\", \"rules\", \"lexer\"].includes(prop)) {\n            continue;\n          }\n          const tokenizerProp = prop;\n          const tokenizerFunc = pack.tokenizer[tokenizerProp];\n          const prevTokenizer = tokenizer[tokenizerProp];\n          tokenizer[tokenizerProp] = (...args2) => {\n            let ret = tokenizerFunc.apply(tokenizer, args2);\n            if (ret === false) {\n              ret = prevTokenizer.apply(tokenizer, args2);\n            }\n            return ret;\n          };\n        }\n        opts.tokenizer = tokenizer;\n      }\n      if (pack.hooks) {\n        const hooks = this.defaults.hooks || new _Hooks();\n        for (const prop in pack.hooks) {\n          if (!(prop in hooks)) {\n            throw new Error(`hook '${prop}' does not exist`);\n          }\n          if ([\"options\", \"block\"].includes(prop)) {\n            continue;\n          }\n          const hooksProp = prop;\n          const hooksFunc = pack.hooks[hooksProp];\n          const prevHook = hooks[hooksProp];\n          if (_Hooks.passThroughHooks.has(prop)) {\n            hooks[hooksProp] = (arg) => {\n              if (this.defaults.async) {\n                return Promise.resolve(hooksFunc.call(hooks, arg)).then((ret2) => {\n                  return prevHook.call(hooks, ret2);\n                });\n              }\n              const ret = hooksFunc.call(hooks, arg);\n              return prevHook.call(hooks, ret);\n            };\n          } else {\n            hooks[hooksProp] = (...args2) => {\n              let ret = hooksFunc.apply(hooks, args2);\n              if (ret === false) {\n                ret = prevHook.apply(hooks, args2);\n              }\n              return ret;\n            };\n          }\n        }\n        opts.hooks = hooks;\n      }\n      if (pack.walkTokens) {\n        const walkTokens2 = this.defaults.walkTokens;\n        const packWalktokens = pack.walkTokens;\n        opts.walkTokens = function(token) {\n          let values = [];\n          values.push(packWalktokens.call(this, token));\n          if (walkTokens2) {\n            values = values.concat(walkTokens2.call(this, token));\n          }\n          return values;\n        };\n      }\n      this.defaults = { ...this.defaults, ...opts };\n    });\n    return this;\n  }\n  setOptions(opt) {\n    this.defaults = { ...this.defaults, ...opt };\n    return this;\n  }\n  lexer(src, options2) {\n    return _Lexer.lex(src, options2 ?? this.defaults);\n  }\n  parser(tokens, options2) {\n    return _Parser.parse(tokens, options2 ?? this.defaults);\n  }\n  parseMarkdown(blockType) {\n    const parse2 = (src, options2) => {\n      const origOpt = { ...options2 };\n      const opt = { ...this.defaults, ...origOpt };\n      const throwError = this.onError(!!opt.silent, !!opt.async);\n      if (this.defaults.async === true && origOpt.async === false) {\n        return throwError(new Error(\"marked(): The async option was set to true by an extension. Remove async: false from the parse options object to return a Promise.\"));\n      }\n      if (typeof src === \"undefined\" || src === null) {\n        return throwError(new Error(\"marked(): input parameter is undefined or null\"));\n      }\n      if (typeof src !== \"string\") {\n        return throwError(new Error(\"marked(): input parameter is of type \" + Object.prototype.toString.call(src) + \", string expected\"));\n      }\n      if (opt.hooks) {\n        opt.hooks.options = opt;\n        opt.hooks.block = blockType;\n      }\n      const lexer2 = opt.hooks ? opt.hooks.provideLexer() : blockType ? _Lexer.lex : _Lexer.lexInline;\n      const parser2 = opt.hooks ? opt.hooks.provideParser() : blockType ? _Parser.parse : _Parser.parseInline;\n      if (opt.async) {\n        return Promise.resolve(opt.hooks ? opt.hooks.preprocess(src) : src).then((src2) => lexer2(src2, opt)).then((tokens) => opt.hooks ? opt.hooks.processAllTokens(tokens) : tokens).then((tokens) => opt.walkTokens ? Promise.all(this.walkTokens(tokens, opt.walkTokens)).then(() => tokens) : tokens).then((tokens) => parser2(tokens, opt)).then((html2) => opt.hooks ? opt.hooks.postprocess(html2) : html2).catch(throwError);\n      }\n      try {\n        if (opt.hooks) {\n          src = opt.hooks.preprocess(src);\n        }\n        let tokens = lexer2(src, opt);\n        if (opt.hooks) {\n          tokens = opt.hooks.processAllTokens(tokens);\n        }\n        if (opt.walkTokens) {\n          this.walkTokens(tokens, opt.walkTokens);\n        }\n        let html2 = parser2(tokens, opt);\n        if (opt.hooks) {\n          html2 = opt.hooks.postprocess(html2);\n        }\n        return html2;\n      } catch (e) {\n        return throwError(e);\n      }\n    };\n    return parse2;\n  }\n  onError(silent, async) {\n    return (e) => {\n      e.message += \"\\nPlease report this to https://github.com/markedjs/marked.\";\n      if (silent) {\n        const msg = \"<p>An error occurred:</p><pre>\" + escape2(e.message + \"\", true) + \"</pre>\";\n        if (async) {\n          return Promise.resolve(msg);\n        }\n        return msg;\n      }\n      if (async) {\n        return Promise.reject(e);\n      }\n      throw e;\n    };\n  }\n};\n\n// src/marked.ts\nvar markedInstance = new Marked();\nfunction marked(src, opt) {\n  return markedInstance.parse(src, opt);\n}\nmarked.options = marked.setOptions = function(options2) {\n  markedInstance.setOptions(options2);\n  marked.defaults = markedInstance.defaults;\n  changeDefaults(marked.defaults);\n  return marked;\n};\nmarked.getDefaults = _getDefaults;\nmarked.defaults = _defaults;\nmarked.use = function(...args) {\n  markedInstance.use(...args);\n  marked",
  ".defaults = markedInstance.defaults;\n  changeDefaults(marked.defaults);\n  return marked;\n};\nmarked.walkTokens = function(tokens, callback) {\n  return markedInstance.walkTokens(tokens, callback);\n};\nmarked.parseInline = markedInstance.parseInline;\nmarked.Parser = _Parser;\nmarked.parser = _Parser.parse;\nmarked.Renderer = _Renderer;\nmarked.TextRenderer = _TextRenderer;\nmarked.Lexer = _Lexer;\nmarked.lexer = _Lexer.lex;\nmarked.Tokenizer = _Tokenizer;\nmarked.Hooks = _Hooks;\nmarked.parse = marked;\nvar options = marked.options;\nvar setOptions = marked.setOptions;\nvar use = marked.use;\nvar walkTokens = marked.walkTokens;\nvar parseInline = marked.parseInline;\nvar parse = marked;\nvar parser = _Parser.parse;\nvar lexer = _Lexer.lex;\n\nif(__exports != exports)module.exports = exports;return module.exports}));\n//# sourceMappingURL=marked.umd.js.map\n\n(function () {\n  /**\n   * Figma UI host bridge — only __harborHost (injected in ui.shell.html).\n   * IDE webview host APIs and JetBrains polyfills are excluded from this bundle.\n   */\n  const host =\n    typeof globalThis !== \"undefined\" && globalThis.__harborHost\n      ? globalThis.__harborHost\n      : {\n          postMessage: function () {},\n          getState: function () {\n            return {};\n          },\n          setState: function () {},\n        };\n\n  function cryptoToken(len) {\n    try {\n      if (typeof crypto !== \"undefined\" && crypto.getRandomValues) {\n        const bytes = new Uint8Array(len);\n        crypto.getRandomValues(bytes);\n        let out = \"\";\n        for (let i = 0; i < bytes.length; i++) {\n          out += bytes[i].toString(36).padStart(2, \"0\");\n        }\n        return out.slice(0, len);\n      }\n    } catch (_e) {\n      /* fall through */\n    }\n    return (\n      Date.now().toString(36) + Math.random().toString(36).slice(2)\n    ).slice(0, len);\n  }\n\n  const STORAGE_SETTINGS = \"harbor.figma.settings\";\n  const STORAGE_SESSION = \"harbor.figma.session\";\n  const STORAGE_UI = \"harbor.figma.uiState\";\n\n  const pendingStorage = new Map();\n  let storageReq = 0;\n\n  function storageGet(key) {\n    return new Promise(function (resolve) {\n      const requestId = \"s\" + ++storageReq;\n      pendingStorage.set(requestId, resolve);\n      host.postMessage({\n        type: \"figmaClientStorageGet\",\n        key: key,\n        requestId: requestId,\n      });\n      setTimeout(function () {\n        if (pendingStorage.has(requestId)) {\n          pendingStorage.delete(requestId);\n          resolve(null);\n        }\n      }, 3000);\n    });\n  }\n\n  function storageSet(key, value) {\n    return new Promise(function (resolve) {\n      const requestId = \"s\" + ++storageReq;\n      pendingStorage.set(requestId, function () {\n        resolve();\n      });\n      host.postMessage({\n        type: \"figmaClientStorageSet\",\n        key: key,\n        value: value,\n        requestId: requestId,\n      });\n      setTimeout(function () {\n        if (pendingStorage.has(requestId)) {\n          pendingStorage.delete(requestId);\n          resolve();\n        }\n      }, 3000);\n    });\n  }\n\n  const pendingTools = new Map();\n  let toolReq = 0;\n  const pendingSelection = new Map();\n  let selReq = 0;\n\n  function invokeTool(name, args) {\n    return new Promise(function (resolve, reject) {\n      const requestId = \"t\" + ++toolReq;\n      pendingTools.set(requestId, { resolve: resolve, reject: reject });\n      host.postMessage({\n        type: \"figmaInvokeTool\",\n        requestId: requestId,\n        name: name,\n        args: args || {},\n      });\n      setTimeout(function () {\n        if (pendingTools.has(requestId)) {\n          pendingTools.delete(requestId);\n          reject(new Error(\"Tool timed out: \" + name));\n        }\n      }, 30000);\n    });\n  }\n\n  function refreshSelection() {\n    return new Promise(function (resolve) {\n      const requestId = \"sel\" + ++selReq;\n      pendingSelection.set(requestId, resolve);\n      host.postMessage({\n        type: \"figmaGetSelection\",\n        requestId: requestId,\n      });\n      setTimeout(function () {\n        if (pendingSelection.has(requestId)) {\n          pendingSelection.delete(requestId);\n          resolve(null);\n        }\n      }, 5000);\n    });\n  }\n\n  function focusNode(nodeId) {\n    host.postMessage({ type: \"figmaFocusNode\", nodeId: nodeId });\n  }\n\n  window.__harborFigma = {\n    host: host,\n    cryptoToken: cryptoToken,\n    STORAGE_SETTINGS: STORAGE_SETTINGS,\n    STORAGE_SESSION: STORAGE_SESSION,\n    STORAGE_UI: STORAGE_UI,\n    storageGet: storageGet,\n    storageSet: storageSet,\n    pendingStorage: pendingStorage,\n    pendingTools: pendingTools,\n    pendingSelection: pendingSelection,\n    invokeTool: invokeTool,\n    refreshSelection: refreshSelection,\n    focusNode: focusNode,\n  };\n})();\n\n(function () {\n  /** Slim OpenAI-compatible turn — Figma host only (no Cline / IDE tools). */\n\n  var MAX_TOOL_ROUNDS = 8;\n\n  function trimSlash(url) {\n    return String(url || \"\").replace(/\\/+$/, \"\");\n  }\n\n  function designSystemPrompt(mode) {\n    var base =\n      \"You are Harbor Agents for Figma — a design assistant inside Figma. \" +\n      \"You help designers critique, name, structure, and hand off UI. \" +\n      \"Ground answers in the current selection JSON and screenshot when provided. \" +\n      \"Use tools to inspect or focus nodes by real ids from the selection — never invent node ids. \" +\n      \"Be concise. Do not invent layers that are not in the selection.\";\n    if (mode === \"agent\") {\n      return (\n        base +\n        \" Mode: Agent. You may edit the canvas via write tools \" +\n        \"(rename, set text, solid fills, auto-layout padding/gap). \" +\n        \"Do not create or delete nodes. Prefer small targeted edits. \" +\n        \"If a write fails (e.g. Dev Mode), explain and continue with advice only.\"\n      );\n    }\n    if (mode === \"plan\") {\n      return (\n        base +\n        \" Mode: Plan. Produce a clear implementation / design plan with Goal and numbered Steps. \" +\n        \"You may inspect/focus nodes with read tools. Do not edit the canvas. \" +\n       ",
  " \"End with a short handoff brief a developer can paste into an IDE agent.\"\n      );\n    }\n    return (\n      base +\n      \" Mode: Ask. Answer the design question; do not invent a full build plan unless asked. \" +\n      \"You may inspect/focus nodes with read tools. Do not edit the canvas.\"\n    );\n  }\n\n  function buildSelectionContextText(selection) {\n    if (!selection || !selection.nodes || selection.nodes.length === 0) {\n      return \"Current Figma selection: (none — ask the user to select a frame or node).\";\n    }\n    var header = [\n      selection.fileName ? \"File: \" + selection.fileName : null,\n      selection.pageName ? \"Page: \" + selection.pageName : null,\n      selection.nodeUrl ? \"Link: \" + selection.nodeUrl : null,\n      selection.canWrite === false\n        ? \"Canvas writes: unavailable (Dev Mode)\"\n        : \"Canvas writes: available in Agent mode\",\n    ]\n      .filter(Boolean)\n      .join(\"\\n\");\n    return (\n      header +\n      \"\\nSelection JSON:\\n\" +\n      JSON.stringify(selection.nodes, null, 2)\n    );\n  }\n\n  function buildHandoffBrief(planText, selection) {\n    var link = (selection && selection.nodeUrl) || \"(no node link)\";\n    var name =\n      (selection &&\n        selection.nodes &&\n        selection.nodes[0] &&\n        selection.nodes[0].name) ||\n      (selection && selection.fileName) ||\n      \"selection\";\n    return (\n      \"# Harbor handoff — \" +\n      name +\n      \"\\n\\nFigma: \" +\n      link +\n      \"\\n\\n\" +\n      String(planText || \"\").trim() +\n      \"\\n\"\n    );\n  }\n\n  function modelIdForApi(modelId) {\n    var id = String(modelId || \"\");\n    if (id.indexOf(\"/\") === -1) return id;\n    var parts = id.split(\"/\");\n    return parts.slice(1).join(\"/\") || id;\n  }\n\n  function readTools() {\n    return [\n      {\n        type: \"function\",\n        function: {\n          name: \"figma_get_selection\",\n          description:\n            \"Get the current Figma selection tree (structure, text, fills, auto-layout).\",\n          parameters: { type: \"object\", properties: {}, additionalProperties: false },\n        },\n      },\n      {\n        type: \"function\",\n        function: {\n          name: \"figma_inspect_node\",\n          description: \"Serialize one node by id (including children summary).\",\n          parameters: {\n            type: \"object\",\n            properties: {\n              nodeId: { type: \"string\", description: \"Figma node id\" },\n            },\n            required: [\"nodeId\"],\n            additionalProperties: false,\n          },\n        },\n      },\n      {\n        type: \"function\",\n        function: {\n          name: \"figma_focus_node\",\n          description: \"Select a node on the canvas and zoom the viewport to it.\",\n          parameters: {\n            type: \"object\",\n            properties: {\n              nodeId: { type: \"string\" },\n            },\n            required: [\"nodeId\"],\n            additionalProperties: false,\n          },\n        },\n      },\n    ];\n  }\n\n  function writeTools() {\n    return [\n      {\n        type: \"function\",\n        function: {\n          name: \"figma_set_name\",\n          description: \"Rename an existing node.\",\n          parameters: {\n            type: \"object\",\n            properties: {\n              nodeId: { type: \"string\" },\n              name: { type: \"string\" },\n            },\n            required: [\"nodeId\", \"name\"],\n            additionalProperties: false,\n          },\n        },\n      },\n      {\n        type: \"function\",\n        function: {\n          name: \"figma_set_text\",\n          description: \"Set characters on a TEXT node (loads font first).\",\n          parameters: {\n            type: \"object\",\n            properties: {\n              nodeId: { type: \"string\" },\n              characters: { type: \"string\" },\n            },\n            required: [\"nodeId\", \"characters\"],\n            additionalProperties: false,\n          },\n        },\n      },\n      {\n        type: \"function\",\n        function: {\n          name: \"figma_set_fills\",\n          description: \"Set a single solid fill on a node (r/g/b/a in 0–1).\",\n          parameters: {\n            type: \"object\",\n            properties: {\n              nodeId: { type: \"string\" },\n              color: {\n                type: \"object\",\n                properties: {\n                  r: { type: \"number\" },\n                  g: { type: \"number\" },\n                  b: { type: \"number\" },\n                  a: { type: \"number\" },\n                },\n                required: [\"r\", \"g\", \"b\"],\n              },\n            },\n            required: [\"nodeId\", \"color\"],\n            additionalProperties: false,\n          },\n        },\n      },\n      {\n        type: \"function\",\n        function: {\n          name: \"figma_set_auto_layout\",\n          description:\n            \"Update auto-layout on a frame/component (layoutMode, padding, itemSpacing).\",\n          parameters: {\n            type: \"object\",\n            properties: {\n              nodeId: { type: \"string\" },\n              layoutMode: {\n                type: \"string\",\n                enum: [\"NONE\", \"HORIZONTAL\", \"VERTICAL\"],\n              },\n              padding: { type: \"number\" },\n              paddingTop: { type: \"number\" },\n              paddingRight: { type: \"number\" },\n              paddingBottom: { type: \"number\" },\n              paddingLeft: { type: \"number\" },\n              itemSpacing: { type: \"number\" },\n            },\n            required: [\"nodeId\"],\n            additionalProperties: false,\n          },\n        },\n      },\n    ];\n  }\n\n  function toolsForMode(mode) {\n    var tools = readTools();\n    if (mode === \"agent\") {\n      tools = tools.concat(writeTools());\n    }\n    return tools;\n  }\n\n  function toolLabel(name) {\n    var map = {\n      figma_get_selection: \"Get selection\",\n      figma_inspect_node: \"Inspect node\",\n      figma_focus_node: \"Focus node\",\n      figma_set_name: \"Rename\",\n      figma_set_text: \"Set text\",\n      figma_set_fills: \"Set fill\",\n      figma_set_auto_layout: \"Auto-layout\",\n    };\n    return map[name] ||",
  " name;\n  }\n\n  function parseArgs(raw) {\n    if (raw == null || raw === \"\") return {};\n    if (typeof raw === \"object\") return raw;\n    try {\n      return JSON.parse(String(raw));\n    } catch (_e) {\n      return {};\n    }\n  }\n\n  async function streamCompletion(opts) {\n    var headers = { \"Content-Type\": \"application/json\" };\n    if (opts.provider.apiKey) {\n      headers.Authorization = \"Bearer \" + opts.provider.apiKey;\n    }\n    var body = {\n      model: modelIdForApi(opts.model.id),\n      messages: opts.messages,\n      stream: true,\n    };\n    if (opts.tools && opts.tools.length) {\n      body.tools = opts.tools;\n      body.tool_choice = \"auto\";\n    }\n\n    var res = await fetch(trimSlash(opts.provider.baseUrl) + \"/chat/completions\", {\n      method: \"POST\",\n      headers: headers,\n      body: JSON.stringify(body),\n      signal: opts.signal,\n    });\n\n    if (!res.ok) {\n      var errBody = await res.text().catch(function () {\n        return \"\";\n      });\n      throw new Error(\n        \"Provider error \" +\n          res.status +\n          \": \" +\n          (errBody.slice(0, 400) || res.statusText)\n      );\n    }\n\n    var reader = res.body && res.body.getReader();\n    if (!reader) throw new Error(\"No response body from provider\");\n\n    var decoder = new TextDecoder();\n    var buffer = \"\";\n    var fullText = \"\";\n    var finishReason = null;\n    /** @type {Record<number, { id: string, name: string, arguments: string }>} */\n    var toolAcc = {};\n\n    while (true) {\n      var step = await reader.read();\n      if (step.done) break;\n      buffer += decoder.decode(step.value, { stream: true });\n      var lines = buffer.split(\"\\n\");\n      buffer = lines.pop() || \"\";\n      for (var i = 0; i < lines.length; i++) {\n        var trimmed = lines[i].trim();\n        if (!trimmed.startsWith(\"data:\")) continue;\n        var data = trimmed.slice(5).trim();\n        if (data === \"[DONE]\") continue;\n        try {\n          var json = JSON.parse(data);\n          var choice = json.choices && json.choices[0];\n          if (!choice) continue;\n          if (choice.finish_reason) finishReason = choice.finish_reason;\n          var delta = choice.delta || {};\n          if (delta.content) {\n            fullText += delta.content;\n            if (opts.onDelta) opts.onDelta(delta.content);\n          }\n          var tcs = delta.tool_calls;\n          if (tcs && tcs.length) {\n            for (var t = 0; t < tcs.length; t++) {\n              var tc = tcs[t];\n              var idx = typeof tc.index === \"number\" ? tc.index : t;\n              if (!toolAcc[idx]) {\n                toolAcc[idx] = { id: \"\", name: \"\", arguments: \"\" };\n              }\n              if (tc.id) toolAcc[idx].id = tc.id;\n              if (tc.function && tc.function.name) {\n                toolAcc[idx].name = tc.function.name;\n              }\n              if (tc.function && tc.function.arguments) {\n                toolAcc[idx].arguments += tc.function.arguments;\n              }\n            }\n          }\n        } catch (_e) {\n          /* ignore */\n        }\n      }\n    }\n\n    var toolCalls = Object.keys(toolAcc)\n      .sort(function (a, b) {\n        return Number(a) - Number(b);\n      })\n      .map(function (k) {\n        return toolAcc[k];\n      })\n      .filter(function (tc) {\n        return tc && tc.name;\n      });\n\n    return {\n      text: fullText,\n      finishReason: finishReason,\n      toolCalls: toolCalls,\n    };\n  }\n\n  async function runFigmaAgentTurn(opts) {\n    var provider = opts.provider;\n    var model = opts.model;\n    var baseUrl = trimSlash(provider && provider.baseUrl);\n    if (!baseUrl) {\n      throw new Error(\n        \"Provider base URL is empty — open Settings and add a provider.\"\n      );\n    }\n    if (!model || !model.id) {\n      throw new Error(\"No model selected — open Settings and add a model.\");\n    }\n\n    var mode =\n      opts.mode === \"plan\"\n        ? \"plan\"\n        : opts.mode === \"agent\"\n          ? \"agent\"\n          : \"ask\";\n    var system = designSystemPrompt(mode);\n    var selectionText = buildSelectionContextText(opts.selection);\n    var useVision =\n      !!model.supportsVision &&\n      opts.selection &&\n      opts.selection.previewPngDataUrl;\n    var tools = toolsForMode(mode);\n    var invokeTool =\n      typeof opts.invokeTool === \"function\" ? opts.invokeTool : null;\n    var onToolStep =\n      typeof opts.onToolStep === \"function\" ? opts.onToolStep : null;\n\n    var messages = [{ role: \"system\", content: system }];\n    (opts.history || []).forEach(function (m) {\n      messages.push({ role: m.role, content: m.text });\n    });\n\n    if (useVision) {\n      messages.push({\n        role: \"user\",\n        content: [\n          { type: \"text\", text: selectionText + \"\\n\\n\" + opts.userText },\n          {\n            type: \"image_url\",\n            image_url: { url: opts.selection.previewPngDataUrl },\n          },\n        ],\n      });\n    } else {\n      messages.push({\n        role: \"user\",\n        content: selectionText + \"\\n\\n\" + opts.userText,\n      });\n    }\n\n    var assistantText = \"\";\n    for (var round = 0; round < MAX_TOOL_ROUNDS; round++) {\n      var result = await streamCompletion({\n        provider: provider,\n        model: model,\n        messages: messages,\n        tools: tools,\n        signal: opts.signal,\n        onDelta: function (piece) {\n          assistantText += piece;\n          if (opts.onDelta) opts.onDelta(piece);\n        },\n      });\n\n      if (!result.toolCalls || !result.toolCalls.length || !invokeTool) {\n        return assistantText || result.text;\n      }\n\n      var assistantMsg = {\n        role: \"assistant\",\n        content: result.text || null,\n        tool_calls: result.toolCalls.map(function (tc, i) {\n          return {\n            id: tc.id || \"call_\" + round + \"_\" + i,\n            type: \"function\",\n            function: {\n              name: tc.name,\n              arguments: tc.arguments || \"{}\",\n            },\n          };\n        }),\n      };\n      messages.push(assistantMsg);\n\n      for (var j = 0; j < result",
  ".toolCalls.length; j++) {\n        var call = result.toolCalls[j];\n        var callId = call.id || \"call_\" + round + \"_\" + j;\n        var args = parseArgs(call.arguments);\n        if (onToolStep) {\n          onToolStep({\n            name: call.name,\n            label: toolLabel(call.name),\n            status: \"running\",\n            args: args,\n          });\n        }\n        var toolResult;\n        var toolError = null;\n        try {\n          toolResult = await invokeTool(call.name, args);\n          if (onToolStep) {\n            onToolStep({\n              name: call.name,\n              label: toolLabel(call.name),\n              status: \"ok\",\n              args: args,\n            });\n          }\n        } catch (err) {\n          toolError = (err && err.message) || String(err);\n          toolResult = { error: toolError };\n          if (onToolStep) {\n            onToolStep({\n              name: call.name,\n              label: toolLabel(call.name),\n              status: \"error\",\n              error: toolError,\n              args: args,\n            });\n          }\n        }\n        messages.push({\n          role: \"tool\",\n          tool_call_id: callId,\n          content: JSON.stringify(toolResult),\n        });\n      }\n    }\n\n    return assistantText || \"(tool round limit reached)\";\n  }\n\n  window.__harborFigmaTurn = {\n    runFigmaAgentTurn: runFigmaAgentTurn,\n    buildHandoffBrief: buildHandoffBrief,\n    buildSelectionContextText: buildSelectionContextText,\n    toolLabel: toolLabel,\n  };\n})();\n\n(function () {\n  /**\n   * Figma markdown — same marked.js as the VS Code webview (media/marked.js),\n   * with a slim renderer (no IDE file paths / plan cards / citation fences).\n   */\n\n  function escapeHtml(s) {\n    return String(s)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\");\n  }\n\n  function getMarkedApi() {\n    if (typeof marked === \"undefined\") return null;\n    if (marked && typeof marked.parse === \"function\" && marked.Renderer) {\n      return marked;\n    }\n    if (marked && typeof marked.marked === \"function\") {\n      return {\n        parse: marked.marked.parse || marked.marked,\n        parseInline: marked.marked.parseInline || marked.parseInline,\n        Renderer: marked.Renderer || marked.marked.Renderer,\n        use: marked.use || marked.marked.use,\n      };\n    }\n    return null;\n  }\n\n  var markdownReady = false;\n\n  function ensureMarkdownRenderer() {\n    var api = getMarkedApi();\n    if (markdownReady) return !!api;\n    if (!api || !api.Renderer) return false;\n    markdownReady = true;\n\n    var renderer = new api.Renderer();\n\n    renderer.code = function (token) {\n      var text = token && token.text != null ? token.text : \"\";\n      var lang = token && token.lang ? String(token.lang) : \"\";\n      var langClass = lang\n        ? ' class=\"language-' + escapeHtml(lang.split(/\\s+/)[0]) + '\"'\n        : \"\";\n      return (\n        '<div class=\"md-pre-wrap\">' +\n        '<pre class=\"md-pre\"><code' +\n        langClass +\n        \">\" +\n        escapeHtml(text) +\n        \"</code></pre></div>\\n\"\n      );\n    };\n\n    renderer.codespan = function (token) {\n      return (\n        '<code class=\"md-code\">' +\n        escapeHtml(token && token.text != null ? token.text : \"\") +\n        \"</code>\"\n      );\n    };\n\n    renderer.heading = function (token) {\n      var level = Math.min(3, Math.max(1, (token && token.depth) || 1));\n      return (\n        '<div class=\"md-h md-h' +\n        level +\n        '\">' +\n        this.parser.parseInline(token.tokens) +\n        \"</div>\\n\"\n      );\n    };\n\n    renderer.paragraph = function (token) {\n      return (\n        '<div class=\"md-p\">' + this.parser.parseInline(token.tokens) + \"</div>\\n\"\n      );\n    };\n\n    renderer.blockquote = function (token) {\n      return (\n        '<blockquote class=\"md-quote\">' +\n        this.parser.parse(token.tokens) +\n        \"</blockquote>\\n\"\n      );\n    };\n\n    renderer.list = function (token) {\n      var tag = token.ordered ? \"ol\" : \"ul\";\n      var startAttr =\n        token.ordered && token.start !== 1 ? ' start=\"' + token.start + '\"' : \"\";\n      var body = \"\";\n      for (var i = 0; i < token.items.length; i++) {\n        body += this.listitem(token.items[i]);\n      }\n      return (\n        \"<\" +\n        tag +\n        ' class=\"md-list md-' +\n        tag +\n        '\"' +\n        startAttr +\n        \">\" +\n        body +\n        \"</\" +\n        tag +\n        \">\\n\"\n      );\n    };\n\n    renderer.listitem = function (item) {\n      var body = \"\";\n      if (item.task) {\n        body +=\n          '<input class=\"md-task\" type=\"checkbox\" disabled' +\n          (item.checked ? \" checked\" : \"\") +\n          \" /> \";\n      }\n      body += this.parser.parse(item.tokens, !!item.loose);\n      return '<li class=\"md-li\">' + body + \"</li>\\n\";\n    };\n\n    renderer.checkbox = function () {\n      return \"\";\n    };\n\n    renderer.strong = function (token) {\n      return (\n        '<strong class=\"md-strong\">' +\n        this.parser.parseInline(token.tokens) +\n        \"</strong>\"\n      );\n    };\n\n    renderer.em = function (token) {\n      return (\n        '<em class=\"md-em\">' + this.parser.parseInline(token.tokens) + \"</em>\"\n      );\n    };\n\n    renderer.del = function (token) {\n      return (\n        '<del class=\"md-del\">' + this.parser.parseInline(token.tokens) + \"</del>\"\n      );\n    };\n\n    renderer.link = function (token) {\n      var label = this.parser.parseInline(token.tokens);\n      var href = String(token.href || \"\");\n      if (!/^https?:\\/\\//i.test(href)) {\n        return label;\n      }\n      var title = token.title\n        ? ' title=\"' + escapeHtml(token.title) + '\"'\n        : \"\";\n      return (\n        '<a class=\"md-link\" href=\"' +\n        escapeHtml(href) +\n        '\" target=\"_blank\" rel=\"noopener noreferrer\"' +\n        title +\n        \">\" +\n        label +\n        \"</a>\"\n      );\n    };\n\n    renderer.image = function (token) {\n      return escapeHtml((token && (token.text || token.href)) || \"\");\n",
  "    };\n\n    renderer.html = function (token) {\n      return escapeHtml((token && token.text) || \"\");\n    };\n\n    renderer.hr = function () {\n      return '<hr class=\"md-hr\" />\\n';\n    };\n\n    renderer.br = function () {\n      return \"<br />\";\n    };\n\n    renderer.table = function (token) {\n      var header = \"\";\n      for (var h = 0; h < token.header.length; h++) {\n        header += this.tablecell(token.header[h]);\n      }\n      var body = \"\";\n      for (var r = 0; r < token.rows.length; r++) {\n        var cells = \"\";\n        for (var c = 0; c < token.rows[r].length; c++) {\n          cells += this.tablecell(token.rows[r][c]);\n        }\n        body += this.tablerow({ text: cells });\n      }\n      return (\n        '<div class=\"md-table-wrap\"><table class=\"md-table\"><thead>' +\n        this.tablerow({ text: header }) +\n        \"</thead><tbody>\" +\n        body +\n        \"</tbody></table></div>\\n\"\n      );\n    };\n\n    renderer.tablerow = function (token) {\n      return \"<tr>\" + token.text + \"</tr>\\n\";\n    };\n\n    renderer.tablecell = function (cell) {\n      var tag = cell.header ? \"th\" : \"td\";\n      var align = cell.align ? ' style=\"text-align:' + cell.align + '\"' : \"\";\n      return (\n        \"<\" +\n        tag +\n        ' class=\"md-td\"' +\n        align +\n        \">\" +\n        this.parser.parseInline(cell.tokens) +\n        \"</\" +\n        tag +\n        \">\"\n      );\n    };\n\n    api.use({\n      renderer: renderer,\n      gfm: true,\n      breaks: true,\n      pedantic: false,\n    });\n    return true;\n  }\n\n  function renderMarkdown(text) {\n    var raw = String(text || \"\");\n    if (!raw) {\n      return '<div class=\"msg-body\"></div>';\n    }\n    var api = getMarkedApi();\n    if (ensureMarkdownRenderer() && api) {\n      try {\n        var html = api.parse(raw, { async: false });\n        return '<div class=\"msg-body\">' + html + \"</div>\";\n      } catch (_e) {\n        /* fall through */\n      }\n    }\n    return (\n      '<div class=\"msg-body\"><div class=\"md-p\">' +\n      escapeHtml(raw).replace(/\\n/g, \"<br />\") +\n      \"</div></div>\"\n    );\n  }\n\n  window.__harborFigmaMd = {\n    renderMarkdown: renderMarkdown,\n    escapeHtml: escapeHtml,\n  };\n})();\n\n(function () {\n  try {\n  const F = window.__harborFigma;\n  const Turn = window.__harborFigmaTurn;\n  const Md = window.__harborFigmaMd;\n  if (!F || !Turn || !Md) {\n    document.body.innerHTML =\n      \"<pre style='padding:16px;color:#c00;font:13px monospace'>Harbor UI failed to init (bridge/turn/markdown missing). Rebuild: npm run build:figma</pre>\";\n    return;\n  }\n  const host = F.host;\n\n  const ICON = {\n    settings:\n      '<span class=\"material-symbols-outlined\" aria-hidden=\"true\">settings</span>',\n    delete:\n      '<span class=\"material-symbols-outlined\" aria-hidden=\"true\">delete</span>',\n  };\n\n  const els = {\n    workspaceShell: document.getElementById(\"workspaceShell\"),\n    agentsRailBackdrop: document.getElementById(\"agentsRailBackdrop\"),\n    agentsScreen: document.getElementById(\"agentsScreen\"),\n    chatScreen: document.getElementById(\"chatScreen\"),\n    settingsScreen: document.getElementById(\"settingsScreen\"),\n    agentsList: document.getElementById(\"agentsList\"),\n    messages: document.getElementById(\"messages\"),\n    prompt: document.getElementById(\"prompt\"),\n    sendBtn: document.getElementById(\"sendBtn\"),\n    modeLabel: document.getElementById(\"modeLabel\"),\n    modeMenu: document.getElementById(\"modeMenu\"),\n    modeTrigger: document.getElementById(\"modeTrigger\"),\n    modePicker: document.getElementById(\"modePicker\"),\n    modelLabel: document.getElementById(\"modelLabel\"),\n    modelMenu: document.getElementById(\"modelMenu\"),\n    modelTrigger: document.getElementById(\"modelTrigger\"),\n    chatAgentName: document.getElementById(\"chatAgentName\"),\n    selectionPreview: document.getElementById(\"selectionPreview\"),\n    selectionThumb: document.getElementById(\"selectionThumb\"),\n    selectionMeta: document.getElementById(\"selectionMeta\"),\n    copyBriefBtn: document.getElementById(\"copyBriefBtn\"),\n    openSettingsBtn: document.getElementById(\"openSettingsBtn\"),\n    closeSettingsBtn: document.getElementById(\"closeSettingsBtn\"),\n    toggleAgentsRailBtn: document.getElementById(\"toggleAgentsRailBtn\"),\n    newAgentBtn: document.getElementById(\"newAgentBtn\"),\n    settingsProvidersModelsList: document.getElementById(\n      \"settingsProvidersModelsList\"\n    ),\n    settingsNav: document.getElementById(\"settingsNav\"),\n    settingsSaveStatus: document.getElementById(\"settingsSaveStatus\"),\n    addProviderBtn: document.getElementById(\"addProviderBtn\"),\n    addModelBtn: document.getElementById(\"addModelBtn\"),\n    langSelect: document.getElementById(\"langSelect\"),\n    fontSizeRange: document.getElementById(\"fontSizeRange\"),\n    fontSizeValue: document.getElementById(\"fontSizeValue\"),\n    fontPreview: document.getElementById(\"fontPreview\"),\n    statusLine: document.getElementById(\"statusLine\"),\n    providerEditModal: document.getElementById(\"providerEditModal\"),\n    providerEditTitle: document.getElementById(\"providerEditTitle\"),\n    providerEditName: document.getElementById(\"providerEditName\"),\n    providerEditBaseUrl: document.getElementById(\"providerEditBaseUrl\"),\n    providerEditApiKey: document.getElementById(\"providerEditApiKey\"),\n    providerEditCloseBtn: document.getElementById(\"providerEditCloseBtn\"),\n    providerEditCancelBtn: document.getElementById(\"providerEditCancelBtn\"),\n    providerEditDoneBtn: document.getElementById(\"providerEditDoneBtn\"),\n    modelEditModal: document.getElementById(\"modelEditModal\"),\n    modelEditTitle: document.getElementById(\"modelEditTitle\"),\n    modelEditProvider: document.getElementById(\"modelEditProvider\"),\n    modelEditId: document.getElementById(\"modelEditId\"),\n    modelEditLabel: document.getElementById(\"modelEditLabel\"),\n    modelEditVision: document.getElementById(\"modelEditVision\"),\n    modelEditCloseBtn: document.getElementById(\"modelEditCloseBtn\"),\n    modelEditCancelBtn: document.getElementById(\"modelEditCancelBtn\"),\n    modelEdit",
  "DoneBtn: document.getElementById(\"modelEditDoneBtn\"),\n  };\n\n  /** @type {{ providers: any[], models: any[], language: string, fontSize: number }} */\n  let settings = {\n    providers: [],\n    models: [],\n    language: \"en\",\n    fontSize: 13,\n  };\n\n  /** @type {{ agents: any[], activeAgentId: string|null }} */\n  let session = { agents: [], activeAgentId: null };\n\n  /** @type {any} */\n  let selection = null;\n  let mode = \"agent\";\n  let selectedModelId = \"\";\n  let busy = false;\n  /** Agent id currently running a turn (cube on agents list). */\n  let busyAgentId = null;\n  /** @type {AbortController|null} */\n  let abortCtrl = null;\n  let lastAssistantText = \"\";\n  /** @type {number|null} */\n  let providerEditIndex = null;\n  /** @type {number|null} */\n  let modelEditIndex = null;\n  let saveStatusTimer = 0;\n  let agentsRailOpen = false;\n  let currentScreen = \"chat\";\n\n  const i18n = {\n    en: {\n      agents: \"Agents\",\n      settings: \"Settings\",\n      newAgent: \"New chat\",\n      agent: \"Agent\",\n      ask: \"Ask\",\n      plan: \"Plan\",\n      send: \"Send\",\n      stop: \"Stop\",\n      copyBrief: \"Copy brief\",\n      focusSelection: \"Focus on canvas\",\n      toolRunning: \"Running…\",\n      runWorking: \"Running\",\n      toolOk: \"Done\",\n      toolError: \"Failed\",\n      canvasReadOnly: \"Dev Mode — canvas writes unavailable\",\n      showAgentsList: \"Show agents\",\n      hideAgentsList: \"Hide agents\",\n      modelsProviders: \"Models & providers\",\n      providersNote:\n        \"Base URL and API key for each OpenAI-compatible API. Models are grouped under their provider.\",\n      language: \"Language\",\n      appearance: \"Appearance\",\n      appearanceNote: \"Chat text and composer size in the panel.\",\n      pluginUiLanguage: \"Plugin UI language\",\n      fontSize: \"Font size\",\n      fontSizeHint: \"Applies to messages and the input field.\",\n      fontPreview: \"The agent will reply at this size.\",\n      cancel: \"Cancel\",\n      done: \"Done\",\n      addProvider: \"+ Provider\",\n      addModel: \"+ Model\",\n      noSelection: \"Select a frame or node\",\n      placeholder: \"Task for the design agent…\",\n      placeholderAgent: \"Task for the design agent…\",\n      placeholderPlan: \"Describe the task — draft a plan without canvas edits…\",\n      placeholderAsk: \"Ask about the selection…\",\n      emptyChat: \"Select a frame, then ask a design question.\",\n      saved: \"Saved\",\n      briefCopied: \"Brief copied\",\n      needSettings: \"Add a provider and model in Settings.\",\n      openSettingsCta: \"Open Settings\",\n      setupHint: \"Add an OpenAI-compatible provider and a model to start.\",\n      noProvidersOrModels: \"No providers or models yet.\",\n      otherProvider: \"Other\",\n      providerTitle: \"Provider\",\n      newProvider: \"New provider\",\n      modelTitle: \"Model\",\n      newModel: \"New model\",\n      delete: \"Delete\",\n      noAgentsYet: \"No chats yet.\",\n      supportsVision: \"Supports images (vision)\",\n    },\n    ru: {\n      agents: \"Агенты\",\n      settings: \"Настройки\",\n      newAgent: \"Новый чат\",\n      agent: \"Агент\",\n      ask: \"Спросить\",\n      plan: \"План\",\n      send: \"Отправить\",\n      stop: \"Стоп\",\n      copyBrief: \"Копировать бриф\",\n      focusSelection: \"Показать на макете\",\n      toolRunning: \"Выполняется…\",\n      runWorking: \"выполняю\",\n      toolOk: \"Готово\",\n      toolError: \"Ошибка\",\n      canvasReadOnly: \"Dev Mode — правки canvas недоступны\",\n      showAgentsList: \"Показать агентов\",\n      hideAgentsList: \"Скрыть агентов\",\n      modelsProviders: \"Модели и провайдеры\",\n      providersNote:\n        \"Base URL и API-ключ для каждого OpenAI-compatible API. Модели сгруппированы под провайдером.\",\n      language: \"Язык\",\n      appearance: \"Оформление\",\n      appearanceNote: \"Размер текста чата и поля ввода в панели.\",\n      pluginUiLanguage: \"Язык интерфейса плагина\",\n      fontSize: \"Размер шрифта\",\n      fontSizeHint: \"Действует на сообщения и поле ввода.\",\n      fontPreview: \"Агент ответит таким размером.\",\n      cancel: \"Отмена\",\n      done: \"Готово\",\n      addProvider: \"+ Провайдер\",\n      addModel: \"+ Модель\",\n      noSelection: \"Выберите фрейм или ноду\",\n      placeholder: \"Задача для дизайн-агента…\",\n      placeholderAgent: \"Задача для дизайн-агента…\",\n      placeholderPlan: \"Опишите задачу — составим план без правок canvas…\",\n      placeholderAsk: \"Вопрос по выделению…\",\n      emptyChat: \"Выберите фрейм и задайте вопрос по дизайну.\",\n      saved: \"Сохранено\",\n      briefCopied: \"Бриф скопирован\",\n      needSettings: \"Добавьте провайдера и модель в Настройках.\",\n      openSettingsCta: \"Открыть настройки\",\n      setupHint: \"Добавьте OpenAI-compatible провайдера и модель, чтобы начать.\",\n      noProvidersOrModels: \"Пока нет провайдеров и моделей.\",\n      otherProvider: \"Другое\",\n      providerTitle: \"Провайдер\",\n      newProvider: \"Новый провайдер\",\n      modelTitle: \"Модель\",\n      newModel: \"Новая модель\",\n      delete: \"Удалить\",\n      noAgentsYet: \"Пока нет чатов.\",\n      supportsVision: \"Поддержка изображений (vision)\",\n    },\n  };\n\n  function t(key) {\n    const lang = settings.language === \"ru\" ? \"ru\" : \"en\";\n    return (i18n[lang] && i18n[lang][key]) || i18n.en[key] || key;\n  }\n\n  function normalizeMode(value) {\n    if (value === \"plan\" || value === \"ask\" || value === \"agent\") return value;\n    return \"agent\";\n  }\n\n  function modeLabelText(m) {\n    if (m === \"plan\") return t(\"plan\");\n    if (m === \"ask\") return t(\"ask\");\n    return t(\"agent\");\n  }\n\n  function modePlaceholder(m) {\n    if (m === \"plan\") return t(\"placeholderPlan\");\n    if (m === \"ask\") return t(\"placeholderAsk\");\n    return t(\"placeholderAgent\");\n  }\n\n  function setStatus(text) {\n    if (els.statusLine) els.statusLine.textContent = text || \"\";\n  }\n\n  function flashSaved() {\n    if (!els.settingsSaveStatus) return;\n    els.settingsSaveStatus.textContent = t(\"saved\");\n    els.settingsSaveStatus.hidden = false;\n    window.clearTimeout(saveStatusTimer);\n    saveStatusTimer = window.setTimeout(function () {\n      if (els.settingsSaveStatus) els.settingsSaveStatus.hidde",
  "n = true;\n    }, 1600);\n  }\n\n  function applyChrome() {\n    document.documentElement.style.setProperty(\n      \"--harbor-font-size\",\n      (settings.fontSize || 13) + \"px\"\n    );\n    if (els.prompt) els.prompt.placeholder = modePlaceholder(mode);\n    if (els.modeLabel) {\n      els.modeLabel.textContent = modeLabelText(mode);\n    }\n    if (els.modePicker) els.modePicker.setAttribute(\"data-mode\", mode);\n    const composer = document.getElementById(\"composer\");\n    if (composer) composer.setAttribute(\"data-mode\", mode);\n    document.querySelectorAll(\"[data-i18n]\").forEach(function (node) {\n      const key = node.getAttribute(\"data-i18n\");\n      if (key) node.textContent = t(key);\n    });\n    if (els.fontSizeValue) {\n      els.fontSizeValue.textContent = (settings.fontSize || 13) + \" px\";\n    }\n    if (els.fontPreview) {\n      els.fontPreview.style.fontSize = (settings.fontSize || 13) + \"px\";\n    }\n    if (els.copyBriefBtn) {\n      els.copyBriefBtn.title = t(\"copyBrief\");\n      els.copyBriefBtn.setAttribute(\"aria-label\", t(\"copyBrief\"));\n    }\n    syncAgentsRailChrome();\n    updateSendButton();\n    renderModeMenu();\n  }\n\n  function clearMenuPlacement(menu) {\n    if (!menu) return;\n    menu.classList.remove(\"is-fixed\", \"opens-down\");\n    menu.style.position = \"\";\n    menu.style.left = \"\";\n    menu.style.top = \"\";\n    menu.style.right = \"\";\n    menu.style.bottom = \"\";\n    menu.style.minWidth = \"\";\n    menu.style.maxHeight = \"\";\n    menu.style.visibility = \"\";\n    menu.style.zIndex = \"\";\n    if (\n      menu._homeParent &&\n      menu.parentElement !== menu._homeParent\n    ) {\n      if (menu._homeNext && menu._homeNext.parentNode === menu._homeParent) {\n        menu._homeParent.insertBefore(menu, menu._homeNext);\n      } else {\n        menu._homeParent.appendChild(menu);\n      }\n    }\n  }\n\n  function placeMenu(picker, menu) {\n    if (!picker || !menu || menu.hidden) return;\n    if (!menu._homeParent) {\n      menu._homeParent = menu.parentElement;\n      menu._homeNext = menu.nextSibling;\n    }\n    const trigger = picker.querySelector(\".model-trigger\") || picker;\n    const triggerRect = trigger.getBoundingClientRect();\n    const gap = 6;\n    const edgePad = 8;\n    const cssMax = 240;\n    if (menu.parentElement !== document.body) {\n      document.body.appendChild(menu);\n    }\n    menu.classList.add(\"is-fixed\");\n    menu.style.position = \"fixed\";\n    menu.style.visibility = \"hidden\";\n    menu.style.left = \"0px\";\n    menu.style.top = \"0px\";\n    menu.style.right = \"auto\";\n    menu.style.bottom = \"auto\";\n    menu.style.zIndex = \"10000\";\n    menu.style.minWidth =\n      Math.round(Math.max(160, triggerRect.width)) + \"px\";\n\n    const naturalHeight = Math.min(Math.max(menu.scrollHeight, 1), cssMax);\n    const menuWidth = Math.min(\n      280,\n      Math.max(160, triggerRect.width, menu.offsetWidth || 0)\n    );\n    const availAbove = triggerRect.top - gap - edgePad;\n    const availBelow = window.innerHeight - triggerRect.bottom - gap - edgePad;\n    const openDown = availAbove < naturalHeight && availBelow > availAbove;\n\n    let left = triggerRect.left;\n    const maxLeft = window.innerWidth - menuWidth - edgePad;\n    if (left > maxLeft) left = Math.max(edgePad, maxLeft);\n    if (left < edgePad) left = edgePad;\n\n    menu.style.visibility = \"\";\n    menu.style.left = Math.round(left) + \"px\";\n    menu.style.minWidth = Math.round(menuWidth) + \"px\";\n    if (openDown) {\n      menu.classList.add(\"opens-down\");\n      picker.classList.add(\"opens-down\");\n      const maxH = Math.max(80, Math.min(cssMax, availBelow));\n      menu.style.maxHeight = Math.round(maxH) + \"px\";\n      menu.style.top = Math.round(triggerRect.bottom + gap) + \"px\";\n      menu.style.bottom = \"auto\";\n    } else {\n      menu.classList.remove(\"opens-down\");\n      picker.classList.remove(\"opens-down\");\n      const maxH = Math.max(80, Math.min(cssMax, availAbove));\n      const h = Math.min(naturalHeight, maxH);\n      menu.style.maxHeight = Math.round(maxH) + \"px\";\n      menu.style.top = Math.round(triggerRect.top - gap - h) + \"px\";\n      menu.style.bottom = \"auto\";\n    }\n  }\n\n  function closeModeMenu() {\n    if (els.modePicker) {\n      els.modePicker.classList.remove(\"is-open\", \"opens-down\");\n    }\n    if (els.modeTrigger) {\n      els.modeTrigger.setAttribute(\"aria-expanded\", \"false\");\n    }\n    if (els.modeMenu) {\n      els.modeMenu.hidden = true;\n      clearMenuPlacement(els.modeMenu);\n    }\n  }\n\n  function closeModelMenu() {\n    const modelPicker = document.getElementById(\"modelPicker\");\n    if (modelPicker) {\n      modelPicker.classList.remove(\"is-open\", \"opens-down\");\n    }\n    if (els.modelTrigger) {\n      els.modelTrigger.setAttribute(\"aria-expanded\", \"false\");\n    }\n    if (els.modelMenu) {\n      els.modelMenu.hidden = true;\n      clearMenuPlacement(els.modelMenu);\n    }\n  }\n\n  function closeAllMenus() {\n    closeModeMenu();\n    closeModelMenu();\n  }\n\n  function openModeMenu() {\n    closeModelMenu();\n    renderModeMenu();\n    if (els.modePicker) els.modePicker.classList.add(\"is-open\");\n    if (els.modeTrigger) {\n      els.modeTrigger.setAttribute(\"aria-expanded\", \"true\");\n    }\n    if (els.modeMenu) {\n      els.modeMenu.hidden = false;\n      placeMenu(els.modePicker, els.modeMenu);\n    }\n  }\n\n  function openModelMenu() {\n    closeModeMenu();\n    renderModelMenu();\n    const modelPicker = document.getElementById(\"modelPicker\");\n    if (modelPicker) modelPicker.classList.add(\"is-open\");\n    if (els.modelTrigger) {\n      els.modelTrigger.setAttribute(\"aria-expanded\", \"true\");\n    }\n    if (els.modelMenu) {\n      els.modelMenu.hidden = false;\n      placeMenu(modelPicker, els.modelMenu);\n    }\n  }\n\n  function renderModeMenu() {\n    if (!els.modeMenu) return;\n    const wasOpen = !els.modeMenu.hidden;\n    els.modeMenu.innerHTML = \"\";\n    [\"agent\", \"plan\", \"ask\"].forEach(function (m) {\n      const btn = document.createElement(\"button\");\n      btn.type = \"button\";\n      btn.className = \"model-option\" + (m === mode ? \" is-active\" : \"\");\n      btn.setAttribute",
  "(\"data-mode\", m);\n      btn.setAttribute(\"role\", \"option\");\n      btn.innerHTML = '<span class=\"model-option-label\"></span>';\n      btn.querySelector(\".model-option-label\").textContent = modeLabelText(m);\n      btn.addEventListener(\"click\", function (e) {\n        e.preventDefault();\n        e.stopPropagation();\n        mode = normalizeMode(m);\n        const agent = activeAgent();\n        if (agent) agent.mode = mode;\n        closeModeMenu();\n        applyChrome();\n        renderChat();\n        void persistSession();\n      });\n      els.modeMenu.appendChild(btn);\n    });\n    if (wasOpen && els.modePicker) {\n      placeMenu(els.modePicker, els.modeMenu);\n    }\n  }\n\n  function syncAgentsRailChrome() {\n    if (els.workspaceShell) {\n      els.workspaceShell.classList.toggle(\"is-rail-open\", agentsRailOpen);\n      els.workspaceShell.classList.add(\"is-narrow\");\n    }\n    if (els.agentsScreen) {\n      els.agentsScreen.hidden = !(currentScreen === \"chat\" && agentsRailOpen);\n    }\n    if (els.agentsRailBackdrop) {\n      els.agentsRailBackdrop.hidden = !(\n        currentScreen === \"chat\" && agentsRailOpen\n      );\n    }\n    if (els.toggleAgentsRailBtn) {\n      const label = agentsRailOpen\n        ? t(\"hideAgentsList\")\n        : t(\"showAgentsList\");\n      els.toggleAgentsRailBtn.title = label;\n      els.toggleAgentsRailBtn.setAttribute(\"aria-label\", label);\n      els.toggleAgentsRailBtn.setAttribute(\n        \"aria-pressed\",\n        agentsRailOpen ? \"true\" : \"false\"\n      );\n      const icon = els.toggleAgentsRailBtn.querySelector(\n        \".material-symbols-outlined\"\n      );\n      if (icon) icon.textContent = agentsRailOpen ? \"menu_open\" : \"menu\";\n    }\n  }\n\n  function setAgentsRailOpen(next) {\n    agentsRailOpen = !!next;\n    if (agentsRailOpen) renderAgents();\n    syncAgentsRailChrome();\n  }\n\n  function showScreen(name) {\n    currentScreen = name === \"settings\" ? \"settings\" : \"chat\";\n    if (els.workspaceShell) els.workspaceShell.hidden = currentScreen !== \"chat\";\n    if (els.chatScreen) els.chatScreen.hidden = false;\n    if (els.settingsScreen) {\n      els.settingsScreen.hidden = currentScreen !== \"settings\";\n    }\n    if (currentScreen !== \"chat\") {\n      agentsRailOpen = false;\n    }\n    syncAgentsRailChrome();\n  }\n\n  function updateSendButton() {\n    if (!els.sendBtn) return;\n    els.sendBtn.classList.remove(\"is-stop\", \"is-queue\");\n    if (!busy) {\n      els.sendBtn.dataset.mode = \"send\";\n      els.sendBtn.title = t(\"send\");\n      els.sendBtn.setAttribute(\"aria-label\", t(\"send\"));\n      return;\n    }\n    els.sendBtn.dataset.mode = \"stop\";\n    els.sendBtn.classList.add(\"is-stop\");\n    els.sendBtn.title = t(\"stop\");\n    els.sendBtn.setAttribute(\"aria-label\", t(\"stop\"));\n  }\n\n  function showSettingsCategory(category) {\n    const allowed = [\"models\", \"language\", \"appearance\"];\n    const cat = allowed.indexOf(category) >= 0 ? category : \"models\";\n    if (els.settingsNav) {\n      els.settingsNav.querySelectorAll(\".settings-nav-item\").forEach(function (btn) {\n        btn.classList.toggle(\n          \"is-active\",\n          btn.getAttribute(\"data-settings-cat\") === cat\n        );\n      });\n    }\n    document.querySelectorAll(\"[data-settings-panel]\").forEach(function (panel) {\n      panel.hidden = panel.getAttribute(\"data-settings-panel\") !== cat;\n    });\n  }\n\n  function activeAgent() {\n    return (\n      session.agents.find(function (a) {\n        return a.id === session.activeAgentId;\n      }) || null\n    );\n  }\n\n  function ensureAgent() {\n    if (activeAgent()) return activeAgent();\n    const agent = {\n      id: \"a_\" + F.cryptoToken(10),\n      name: \"Chat\",\n      messages: [],\n      mode: mode,\n      modelId: selectedModelId,\n      createdAt: Date.now(),\n    };\n    session.agents.unshift(agent);\n    session.activeAgentId = agent.id;\n    return agent;\n  }\n\n  function persistSession() {\n    return F.storageSet(F.STORAGE_SESSION, session);\n  }\n\n  function persistSettings() {\n    return F.storageSet(F.STORAGE_SETTINGS, settings);\n  }\n\n  function shortModelChip(raw) {\n    const s = String(raw || \"\").trim();\n    if (!s) return \"\";\n    const last = s.includes(\"/\") ? s.slice(s.lastIndexOf(\"/\") + 1) : s;\n    if (/^gpt-/i.test(last)) return last.replace(/^gpt-/i, \"GPT-\");\n    if (/^glm-/i.test(last)) return last.replace(/^glm-/i, \"GLM \");\n    if (/^claude-/i.test(last)) return last.replace(/^claude-/i, \"\");\n    return last.length > 18 ? last.slice(0, 16) + \"…\" : last;\n  }\n\n  function formatAgentTime(ts) {\n    if (!ts) return \"\";\n    try {\n      const d = new Date(ts);\n      return d.toLocaleTimeString([], { hour: \"2-digit\", minute: \"2-digit\" });\n    } catch (_e) {\n      return \"\";\n    }\n  }\n\n  function agentPreview(a) {\n    const msgs = a.messages || [];\n    for (let i = msgs.length - 1; i >= 0; i--) {\n      const text = String((msgs[i] && msgs[i].text) || \"\").trim();\n      if (text) return text.slice(0, 120);\n    }\n    return \"\";\n  }\n\n  function deleteAgent(agentId) {\n    const idx = session.agents.findIndex(function (a) {\n      return a.id === agentId;\n    });\n    if (idx < 0) return;\n    const wasActive = session.activeAgentId === agentId;\n    session.agents.splice(idx, 1);\n    if (wasActive) {\n      session.activeAgentId = session.agents[0] ? session.agents[0].id : null;\n      lastAssistantText = \"\";\n      if (session.activeAgentId) {\n        const next = activeAgent();\n        if (next) {\n          mode = normalizeMode(next.mode);\n          selectedModelId = next.modelId || selectedModelId;\n        }\n      } else {\n        ensureAgent();\n      }\n    }\n    renderAgents();\n    renderChat();\n    applyChrome();\n    void persistSession();\n  }\n\n  function renderAgents() {\n    if (!els.agentsList) return;\n    els.agentsList.innerHTML = \"\";\n    if (!session.agents.length) {\n      els.agentsList.innerHTML =\n        '<div class=\"agents-empty\">' + escapeHtml(t(\"noAgentsYet\")) + \"</div>\";\n      return;\n    }\n    session.agents.forEach(function (a) {\n      const wrap = document.createElement(\"div\");\n      wrap.className",
  " =\n        \"agent-block\" + (a.id === session.activeAgentId ? \" is-active\" : \"\");\n      wrap.dataset.agent = a.id;\n      const model =\n        a.modelId ||\n        ((settings.models || []).find(function (m) {\n          return m.id === selectedModelId;\n        }) || {}).id ||\n        \"\";\n      const runMode = normalizeMode(a.mode || mode);\n      const isRunning = busy && busyAgentId === a.id;\n      const statusHtml = isRunning\n        ? '<span class=\"agent-run-status agent-run-status-running\" data-mode=\"' +\n          escapeHtml(runMode) +\n          '\" aria-label=\"' +\n          escapeHtml(t(\"runWorking\")) +\n          '\"><span class=\"cube-bit cube-bit-1\"></span><span class=\"cube-bit cube-bit-2\"></span><span class=\"cube-bit cube-bit-3\"></span><span class=\"cube-bit cube-bit-4\"></span></span>'\n        : '<span class=\"agent-run-status agent-run-status-empty\" aria-hidden=\"true\"></span>';\n      wrap.innerHTML =\n        '<div class=\"agent-row-wrap\">' +\n        '<div class=\"agent-row flat\" role=\"button\" tabindex=\"0\">' +\n        '<div class=\"agent-main\">' +\n        statusHtml +\n        '<div class=\"agent-name-row\">' +\n        '<div class=\"agent-name-wrap\"><div class=\"agent-name\"></div></div>' +\n        '<div class=\"agent-trailing\">' +\n        '<span class=\"agent-time\"></span>' +\n        '<div class=\"row-actions\">' +\n        '<button type=\"button\" class=\"row-action row-delete\" data-delete-agent=\"' +\n        escapeHtml(a.id) +\n        '\" title=\"' +\n        escapeHtml(t(\"delete\")) +\n        '\" aria-label=\"' +\n        escapeHtml(t(\"delete\")) +\n        '\">' +\n        ICON.delete +\n        \"</button>\" +\n        \"</div></div></div>\" +\n        '<div class=\"agent-preview\"></div>' +\n        '<span class=\"agent-chip\"></span>' +\n        \"</div></div></div>\";\n      wrap.querySelector(\".agent-name\").textContent = a.name || \"Chat\";\n      wrap.querySelector(\".agent-time\").textContent = formatAgentTime(\n        a.createdAt || a.updatedAt\n      );\n      wrap.querySelector(\".agent-preview\").textContent = agentPreview(a);\n      const chip = wrap.querySelector(\".agent-chip\");\n      const chipText = shortModelChip(model);\n      chip.textContent = chipText;\n      if (chipText && model) chip.title = String(model);\n      wrap.querySelector(\".agent-row\").addEventListener(\"click\", function (e) {\n        if (e.target.closest(\".row-action\")) return;\n        session.activeAgentId = a.id;\n        mode = normalizeMode(a.mode);\n        selectedModelId = a.modelId || selectedModelId;\n        renderChat();\n        renderAgents();\n        setAgentsRailOpen(false);\n        showScreen(\"chat\");\n        applyChrome();\n        void persistSession();\n      });\n      wrap\n        .querySelector(\".row-delete\")\n        .addEventListener(\"click\", function (e) {\n          e.preventDefault();\n          e.stopPropagation();\n          deleteAgent(a.id);\n        });\n      els.agentsList.appendChild(wrap);\n    });\n  }\n\n  function escapeHtml(s) {\n    return Md.escapeHtml(s);\n  }\n\n  function renderMarkdown(text) {\n    return Md.renderMarkdown(text);\n  }\n\n  function openSettings() {\n    ensureDefaultProviderForm();\n    renderSettingsCatalog();\n    if (els.langSelect) els.langSelect.value = settings.language || \"en\";\n    if (els.fontSizeRange) {\n      els.fontSizeRange.value = String(settings.fontSize || 13);\n    }\n    showSettingsCategory(\"models\");\n    showScreen(\"settings\");\n    applyChrome();\n  }\n\n  function ensureDefaultProviderForm() {\n    if (!settings.providers) settings.providers = [];\n    if (!settings.models) settings.models = [];\n    if (settings.providers.length === 0) {\n      settings.providers.push({\n        id: \"p_\" + F.cryptoToken(6),\n        name: \"Provider\",\n        baseUrl: \"https://api.openai.com/v1\",\n        apiKey: \"\",\n      });\n    }\n    if (settings.models.length === 0) {\n      settings.models.push({\n        id: \"gpt-4o-mini\",\n        label: \"gpt-4o-mini\",\n        providerId: settings.providers[0].id,\n        supportsVision: false,\n        enabled: true,\n      });\n    }\n  }\n\n  function hasUsableSetup() {\n    return (\n      (settings.providers || []).some(function (p) {\n        return p && p.baseUrl;\n      }) &&\n      (settings.models || []).some(function (m) {\n        return m && m.id && m.providerId && m.enabled !== false;\n      })\n    );\n  }\n\n  function renderChat() {\n    const agent = ensureAgent();\n    if (els.chatAgentName) els.chatAgentName.textContent = agent.name || \"Chat\";\n    if (!els.messages) return;\n    els.messages.innerHTML = \"\";\n    if (!agent.messages.length) {\n      const empty = document.createElement(\"div\");\n      empty.className = \"empty-hint\";\n      if (!hasUsableSetup()) {\n        const p = document.createElement(\"p\");\n        p.textContent = t(\"setupHint\");\n        empty.appendChild(p);\n        const btn = document.createElement(\"button\");\n        btn.type = \"button\";\n        btn.className = \"text-btn\";\n        btn.textContent = t(\"openSettingsCta\");\n        btn.addEventListener(\"click\", openSettings);\n        empty.appendChild(btn);\n      } else {\n        empty.textContent = t(\"emptyChat\");\n      }\n      els.messages.appendChild(empty);\n      return;\n    }\n    agent.messages.forEach(function (m) {\n      const wrap = document.createElement(\"div\");\n      wrap.className =\n        \"msg-wrap \" +\n        (m.role === \"user\" ? \"msg-wrap-user\" : \"msg-wrap-assistant\");\n      const bubble = document.createElement(\"div\");\n      bubble.className = \"msg \" + (m.role === \"user\" ? \"user\" : \"assistant\");\n      if (m.role === \"user\") {\n        const msgMode = normalizeMode(m.mode || mode);\n        bubble.setAttribute(\"data-mode\", msgMode);\n      }\n      if (m.role === \"assistant\") {\n        if (m.steps && m.steps.length) {\n          const stepsEl = document.createElement(\"div\");\n          stepsEl.className = \"figma-tool-steps\";\n          m.steps.forEach(function (step) {\n            const row = document.createElement(\"div\");\n            row.className =\n              \"figma-tool-step is-\" + (step.status || \"running\");\n       ",
  "     const label = document.createElement(\"span\");\n            label.className = \"figma-tool-step-label\";\n            label.textContent = step.label || step.name || \"tool\";\n            const status = document.createElement(\"span\");\n            status.className = \"figma-tool-step-status\";\n            status.textContent =\n              step.status === \"ok\"\n                ? t(\"toolOk\")\n                : step.status === \"error\"\n                  ? t(\"toolError\")\n                  : t(\"toolRunning\");\n            row.appendChild(label);\n            row.appendChild(status);\n            if (step.error) {\n              row.title = step.error;\n            }\n            stepsEl.appendChild(row);\n          });\n          bubble.appendChild(stepsEl);\n        }\n        const md = document.createElement(\"div\");\n        md.className = \"msg-md\";\n        md.innerHTML = renderMarkdown(m.text || \"\");\n        bubble.appendChild(md);\n      } else {\n        bubble.innerHTML =\n          '<div class=\"msg-body\"><div class=\"msg-text\"></div></div>';\n        bubble.querySelector(\".msg-text\").textContent = m.text || \"\";\n      }\n      wrap.appendChild(bubble);\n      els.messages.appendChild(wrap);\n    });\n    if (busy) {\n      const runEl = document.createElement(\"div\");\n      const liveUser =\n        agent.messages.length >= 2\n          ? agent.messages[agent.messages.length - 2]\n          : null;\n      const runMode = normalizeMode(\n        (liveUser && liveUser.role === \"user\" && liveUser.mode) || mode\n      );\n      runEl.className = \"figma-run-working\";\n      runEl.setAttribute(\"data-mode\", runMode);\n      runEl.setAttribute(\"aria-live\", \"polite\");\n      runEl.innerHTML =\n        '<span class=\"figma-run-working-dot\" aria-hidden=\"true\"></span>' +\n        '<span class=\"figma-run-working-label\"></span>';\n      runEl.querySelector(\".figma-run-working-label\").textContent =\n        t(\"runWorking\");\n      const last = els.messages.lastElementChild;\n      if (\n        last &&\n        last.classList.contains(\"msg-wrap-assistant\") &&\n        last.parentNode\n      ) {\n        last.parentNode.insertBefore(runEl, last);\n      } else {\n        els.messages.appendChild(runEl);\n      }\n    }\n    els.messages.scrollTop = els.messages.scrollHeight;\n    if (els.copyBriefBtn) {\n      els.copyBriefBtn.hidden = mode !== \"plan\" || !lastAssistantText;\n    }\n  }\n\n  function renderSelection() {\n    if (!els.selectionPreview) return;\n    const has = selection && selection.nodes && selection.nodes.length > 0;\n    els.selectionPreview.hidden = !has;\n    if (!has) {\n      if (els.selectionMeta) els.selectionMeta.textContent = t(\"noSelection\");\n      els.selectionPreview.classList.remove(\"is-clickable\");\n      els.selectionPreview.removeAttribute(\"title\");\n      els.selectionPreview.removeAttribute(\"role\");\n      els.selectionPreview.removeAttribute(\"tabindex\");\n      return;\n    }\n    const n = selection.nodes[0];\n    let meta =\n      n.name +\n      \" · \" +\n      n.type +\n      (n.width && n.height ? \" · \" + n.width + \"×\" + n.height : \"\");\n    if (selection.canWrite === false) {\n      meta += \" · \" + t(\"canvasReadOnly\");\n    }\n    if (els.selectionMeta) {\n      els.selectionMeta.textContent = meta;\n    }\n    if (els.selectionThumb) {\n      if (selection.previewPngDataUrl) {\n        els.selectionThumb.src = selection.previewPngDataUrl;\n        els.selectionThumb.hidden = false;\n      } else {\n        els.selectionThumb.hidden = true;\n      }\n    }\n    els.selectionPreview.classList.add(\"is-clickable\");\n    els.selectionPreview.title = t(\"focusSelection\");\n    els.selectionPreview.setAttribute(\"role\", \"button\");\n    els.selectionPreview.setAttribute(\"tabindex\", \"0\");\n  }\n\n  function renderModelMenu() {\n    if (!els.modelMenu || !els.modelLabel) return;\n    els.modelMenu.innerHTML = \"\";\n    const models = (settings.models || []).filter(function (m) {\n      return m && m.enabled !== false;\n    });\n    if (!selectedModelId && models[0]) selectedModelId = models[0].id;\n    const current = models.find(function (m) {\n      return m.id === selectedModelId;\n    });\n    els.modelLabel.textContent = current\n      ? current.label || current.id\n      : t(\"needSettings\");\n    models.forEach(function (m) {\n      const btn = document.createElement(\"button\");\n      btn.type = \"button\";\n      btn.className =\n        \"model-option\" + (m.id === selectedModelId ? \" is-active\" : \"\");\n      btn.role = \"option\";\n      btn.innerHTML = '<span class=\"model-option-label\"></span>';\n      btn.querySelector(\".model-option-label\").textContent = m.label || m.id;\n      btn.addEventListener(\"click\", function (e) {\n        e.preventDefault();\n        e.stopPropagation();\n        selectedModelId = m.id;\n        const agent = activeAgent();\n        if (agent) agent.modelId = m.id;\n        closeModelMenu();\n        renderModelMenu();\n        void persistSession();\n      });\n      els.modelMenu.appendChild(btn);\n    });\n  }\n\n  function appendProviderHead(listEl, provider, index) {\n    const row = document.createElement(\"div\");\n    row.className = \"settings-provider-head\";\n    row.dataset.providerIndex = String(index);\n    const title = provider.name || provider.id || t(\"providerTitle\");\n    row.innerHTML =\n      '<div class=\"settings-model-info\">' +\n      '<div class=\"settings-model-name\">' +\n      '<span class=\"provider-status-title\"></span>' +\n      \"</div>\" +\n      '<div class=\"settings-model-id\"></div>' +\n      \"</div>\" +\n      '<button type=\"button\" class=\"icon-btn settings-provider-edit\" data-index=\"' +\n      index +\n      '\" title=\"' +\n      t(\"settings\") +\n      '\" aria-label=\"' +\n      t(\"settings\") +\n      '\">' +\n      ICON.settings +\n      \"</button>\" +\n      '<button type=\"button\" class=\"icon-btn settings-provider-remove\" data-index=\"' +\n      index +\n      '\" title=\"' +\n      t(\"delete\") +\n      '\" aria-label=\"' +\n      t(\"delete\") +\n      '\">' +\n      ICON.delete +\n      \"</button>\";\n    row.querySelector(\".provider-status-title\").textContent = title;\n    row.querySele",
  "ctor(\".settings-model-id\").textContent =\n      provider.baseUrl || provider.id || \"\";\n    listEl.appendChild(row);\n  }\n\n  function appendModelRow(listEl, model, index, nested) {\n    const row = document.createElement(\"div\");\n    const enabled = model.enabled !== false;\n    row.className =\n      \"settings-model-row\" +\n      (enabled ? \"\" : \" is-disabled\") +\n      (nested ? \" is-under-provider\" : \"\");\n    row.dataset.index = String(index);\n    const title = model.label || model.id || \"\";\n    const parts = [];\n    if (model.label && model.id && model.label !== model.id) {\n      parts.push(model.id);\n    }\n    if (!nested) {\n      const p = (settings.providers || []).find(function (x) {\n        return x.id === model.providerId;\n      });\n      if (p) parts.push(p.name || p.id);\n    }\n    row.innerHTML =\n      '<label class=\"settings-model-switch\">' +\n      '<input type=\"checkbox\" class=\"settings-model-toggle\" data-index=\"' +\n      index +\n      '\"' +\n      (enabled ? \" checked\" : \"\") +\n      \" />\" +\n      '<span class=\"settings-model-switch-ui\" aria-hidden=\"true\"></span>' +\n      \"</label>\" +\n      '<div class=\"settings-model-info\">' +\n      '<div class=\"settings-model-name\"></div>' +\n      '<div class=\"settings-model-id\"></div>' +\n      \"</div>\" +\n      '<button type=\"button\" class=\"icon-btn settings-model-edit\" data-index=\"' +\n      index +\n      '\" title=\"' +\n      t(\"settings\") +\n      '\" aria-label=\"' +\n      t(\"settings\") +\n      '\">' +\n      ICON.settings +\n      \"</button>\" +\n      '<button type=\"button\" class=\"icon-btn settings-model-remove\" data-index=\"' +\n      index +\n      '\" title=\"' +\n      t(\"delete\") +\n      '\" aria-label=\"' +\n      t(\"delete\") +\n      '\">' +\n      ICON.delete +\n      \"</button>\";\n    row.querySelector(\".settings-model-name\").textContent = title;\n    row.querySelector(\".settings-model-id\").textContent = parts.join(\" · \");\n    listEl.appendChild(row);\n  }\n\n  function renderSettingsCatalog() {\n    const list = els.settingsProvidersModelsList;\n    if (!list) return;\n    list.innerHTML = \"\";\n    const providers = settings.providers || [];\n    const models = settings.models || [];\n    if (!providers.length && !models.length) {\n      list.innerHTML =\n        '<div class=\"settings-models-empty\">' +\n        escapeHtml(t(\"noProvidersOrModels\")) +\n        \"</div>\";\n      return;\n    }\n    const used = new Set();\n    providers.forEach(function (provider, providerIndex) {\n      const group = document.createElement(\"div\");\n      group.className = \"settings-provider-group\";\n      appendProviderHead(group, provider, providerIndex);\n      const pid = String(provider.id || \"\").trim();\n      models.forEach(function (model, index) {\n        if (String(model.providerId || \"\").trim() === pid) {\n          used.add(index);\n          appendModelRow(group, model, index, true);\n        }\n      });\n      list.appendChild(group);\n    });\n    const orphans = [];\n    models.forEach(function (model, index) {\n      if (!used.has(index)) orphans.push({ model: model, index: index });\n    });\n    if (orphans.length) {\n      if (providers.length) {\n        const group = document.createElement(\"div\");\n        group.className = \"settings-provider-group\";\n        const orphanHead = document.createElement(\"div\");\n        orphanHead.className = \"settings-provider-head\";\n        orphanHead.innerHTML =\n          '<div class=\"settings-model-info\"><div class=\"settings-model-name\"></div></div>';\n        orphanHead.querySelector(\".settings-model-name\").textContent =\n          t(\"otherProvider\");\n        group.appendChild(orphanHead);\n        orphans.forEach(function (entry) {\n          appendModelRow(group, entry.model, entry.index, true);\n        });\n        list.appendChild(group);\n      } else {\n        orphans.forEach(function (entry) {\n          appendModelRow(list, entry.model, entry.index, false);\n        });\n      }\n    }\n  }\n\n  function closeProviderEdit() {\n    if (els.providerEditModal) els.providerEditModal.hidden = true;\n    providerEditIndex = null;\n  }\n\n  function closeModelEdit() {\n    if (els.modelEditModal) els.modelEditModal.hidden = true;\n    modelEditIndex = null;\n  }\n\n  function openProviderEdit(index) {\n    ensureDefaultProviderForm();\n    providerEditIndex = index;\n    const isNew = index < 0 || index >= settings.providers.length;\n    const provider = isNew\n      ? {\n          id: \"p_\" + F.cryptoToken(6),\n          name: \"\",\n          baseUrl: \"https://api.openai.com/v1\",\n          apiKey: \"\",\n        }\n      : settings.providers[index];\n    if (isNew) {\n      settings.providers.push(provider);\n      providerEditIndex = settings.providers.length - 1;\n    }\n    if (els.providerEditTitle) {\n      els.providerEditTitle.textContent = isNew\n        ? t(\"newProvider\")\n        : t(\"providerTitle\");\n    }\n    if (els.providerEditName) els.providerEditName.value = provider.name || \"\";\n    if (els.providerEditBaseUrl) {\n      els.providerEditBaseUrl.value = provider.baseUrl || \"\";\n    }\n    if (els.providerEditApiKey) {\n      els.providerEditApiKey.value = provider.apiKey || \"\";\n    }\n    if (els.providerEditModal) els.providerEditModal.hidden = false;\n    if (els.providerEditName) els.providerEditName.focus();\n  }\n\n  function saveProviderEdit() {\n    if (providerEditIndex == null || !settings.providers[providerEditIndex]) {\n      closeProviderEdit();\n      return;\n    }\n    const p = settings.providers[providerEditIndex];\n    p.name = (els.providerEditName && els.providerEditName.value.trim()) || \"Provider\";\n    p.baseUrl =\n      (els.providerEditBaseUrl && els.providerEditBaseUrl.value.trim()) || \"\";\n    p.apiKey =\n      (els.providerEditApiKey && els.providerEditApiKey.value) || \"\";\n    closeProviderEdit();\n    renderSettingsCatalog();\n    void persistSettings().then(flashSaved);\n  }\n\n  function fillModelProviderSelect(selectedId) {\n    if (!els.modelEditProvider) return;\n    els.modelEditProvider.innerHTML = \"\";\n    (settings.providers || []).forEach(function (p) {\n      const opt = doc",
  "ument.createElement(\"option\");\n      opt.value = p.id;\n      opt.textContent = p.name || p.id;\n      els.modelEditProvider.appendChild(opt);\n    });\n    if (selectedId) els.modelEditProvider.value = selectedId;\n  }\n\n  function openModelEdit(index) {\n    ensureDefaultProviderForm();\n    modelEditIndex = index;\n    const isNew = index < 0 || index >= settings.models.length;\n    const model = isNew\n      ? {\n          id: \"\",\n          label: \"\",\n          providerId: settings.providers[0] ? settings.providers[0].id : \"\",\n          supportsVision: false,\n          enabled: true,\n        }\n      : settings.models[index];\n    if (isNew) {\n      settings.models.push(model);\n      modelEditIndex = settings.models.length - 1;\n    }\n    if (els.modelEditTitle) {\n      els.modelEditTitle.textContent = isNew ? t(\"newModel\") : t(\"modelTitle\");\n    }\n    fillModelProviderSelect(model.providerId);\n    if (els.modelEditId) els.modelEditId.value = model.id || \"\";\n    if (els.modelEditLabel) els.modelEditLabel.value = model.label || \"\";\n    if (els.modelEditVision) {\n      els.modelEditVision.checked = !!model.supportsVision;\n    }\n    if (els.modelEditModal) els.modelEditModal.hidden = false;\n    if (els.modelEditId) els.modelEditId.focus();\n  }\n\n  function saveModelEdit() {\n    if (modelEditIndex == null || !settings.models[modelEditIndex]) {\n      closeModelEdit();\n      return;\n    }\n    const m = settings.models[modelEditIndex];\n    m.id = (els.modelEditId && els.modelEditId.value.trim()) || \"\";\n    m.label =\n      (els.modelEditLabel && els.modelEditLabel.value.trim()) || m.id;\n    m.providerId =\n      (els.modelEditProvider && els.modelEditProvider.value) || \"\";\n    m.supportsVision = !!(els.modelEditVision && els.modelEditVision.checked);\n    if (m.enabled === undefined) m.enabled = true;\n    closeModelEdit();\n    renderSettingsCatalog();\n    renderModelMenu();\n    void persistSettings().then(flashSaved);\n  }\n\n  async function sendMessage() {\n    if (busy) return;\n    const text = (els.prompt && els.prompt.value.trim()) || \"\";\n    if (!text) return;\n    const model = (settings.models || []).find(function (m) {\n      return m.id === selectedModelId && m.enabled !== false;\n    });\n    const provider =\n      model &&\n      (settings.providers || []).find(function (p) {\n        return p.id === model.providerId;\n      });\n    if (!model || !provider) {\n      setStatus(t(\"needSettings\"));\n      openSettings();\n      return;\n    }\n\n    const agent = ensureAgent();\n    agent.messages.push({ role: \"user\", text: text, mode: mode });\n    if (agent.name === \"Chat\" && text.length > 0) {\n      agent.name = text.slice(0, 40);\n    }\n    agent.mode = mode;\n    agent.modelId = selectedModelId;\n    if (els.prompt) els.prompt.value = \"\";\n    renderChat();\n    renderAgents();\n\n    busy = true;\n    busyAgentId = agent.id;\n    abortCtrl = new AbortController();\n    updateSendButton();\n    setStatus(t(\"runWorking\"));\n    renderAgents();\n    renderChat();\n\n    if (typeof F.refreshSelection === \"function\") {\n      const fresh = await F.refreshSelection();\n      if (fresh) selection = fresh;\n    }\n\n    const history = agent.messages.slice(0, -1).map(function (m) {\n      return { role: m.role, text: m.text };\n    });\n    const assistant = { role: \"assistant\", text: \"\", steps: [] };\n    agent.messages.push(assistant);\n    agent.updatedAt = Date.now();\n    renderChat();\n    renderAgents();\n\n    try {\n      const full = await Turn.runFigmaAgentTurn({\n        provider: provider,\n        model: model,\n        mode: mode,\n        userText: text,\n        history: history,\n        selection: selection,\n        signal: abortCtrl.signal,\n        invokeTool: function (name, args) {\n          return F.invokeTool(name, args);\n        },\n        onToolStep: function (step) {\n          if (!assistant.steps) assistant.steps = [];\n          const prev = assistant.steps[assistant.steps.length - 1];\n          if (\n            prev &&\n            prev.name === step.name &&\n            prev.status === \"running\" &&\n            step.status !== \"running\"\n          ) {\n            prev.status = step.status;\n            prev.error = step.error;\n          } else {\n            assistant.steps.push({\n              name: step.name,\n              label: step.label,\n              status: step.status,\n              error: step.error,\n            });\n          }\n          renderChat();\n        },\n        onDelta: function (piece) {\n          assistant.text += piece;\n          lastAssistantText = assistant.text;\n          renderChat();\n        },\n      });\n      assistant.text = full || assistant.text;\n      lastAssistantText = assistant.text;\n      setStatus(\"\");\n    } catch (err) {\n      if (err && err.name === \"AbortError\") {\n        setStatus(\"Stopped\");\n      } else {\n        assistant.text =\n          assistant.text ||\n          \"Error: \" + ((err && err.message) || String(err));\n        setStatus(assistant.text.slice(0, 120));\n      }\n      lastAssistantText = assistant.text;\n    }\n\n    busy = false;\n    busyAgentId = null;\n    abortCtrl = null;\n    updateSendButton();\n    renderChat();\n    renderAgents();\n    void persistSession();\n  }\n\n  function onHostMessage(msg) {\n    if (!msg || typeof msg !== \"object\") return;\n    if (msg.type === \"figmaSelectionChanged\") {\n      selection = msg.selection || null;\n      renderSelection();\n      if (msg.requestId && F.pendingSelection) {\n        const resolve = F.pendingSelection.get(msg.requestId);\n        if (resolve) {\n          F.pendingSelection.delete(msg.requestId);\n          resolve(selection);\n        }\n      }\n      return;\n    }\n    if (msg.type === \"figmaToolResult\") {\n      if (F.pendingTools) {\n        const pending = F.pendingTools.get(msg.requestId);\n        if (pending) {\n          F.pendingTools.delete(msg.requestId);\n          if (msg.ok) {\n            pending.resolve(msg.result);\n          } else {\n            pending.reject(new Error(msg.error || \"Tool failed\"));\n          }\n        }\n      }",
  "\n      return;\n    }\n    if (msg.type === \"figmaClientStorageValue\") {\n      const resolve = F.pendingStorage.get(msg.requestId);\n      if (resolve) {\n        F.pendingStorage.delete(msg.requestId);\n        resolve(msg.value);\n      }\n      return;\n    }\n    if (msg.type === \"figmaClientStorageSaved\") {\n      const resolve = F.pendingStorage.get(msg.requestId);\n      if (resolve) {\n        F.pendingStorage.delete(msg.requestId);\n        resolve();\n      }\n      return;\n    }\n    if (msg.type === \"copied\") {\n      setStatus(t(\"briefCopied\"));\n    }\n  }\n\n  window.onmessage = function (event) {\n    const data = event.data;\n    const msg =\n      data && typeof data === \"object\" && \"pluginMessage\" in data\n        ? data.pluginMessage\n        : data;\n    onHostMessage(msg);\n  };\n\n  if (els.sendBtn) {\n    els.sendBtn.addEventListener(\"click\", function () {\n      if (busy) {\n        if (abortCtrl) abortCtrl.abort();\n        return;\n      }\n      void sendMessage();\n    });\n  }\n  if (els.selectionPreview) {\n    els.selectionPreview.addEventListener(\"click\", function () {\n      const id =\n        selection &&\n        selection.nodes &&\n        selection.nodes[0] &&\n        selection.nodes[0].id;\n      if (id && typeof F.focusNode === \"function\") F.focusNode(id);\n    });\n    els.selectionPreview.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" || e.key === \" \") {\n        e.preventDefault();\n        els.selectionPreview.click();\n      }\n    });\n  }\n  if (els.prompt) {\n    els.prompt.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        if (!busy) void sendMessage();\n      }\n    });\n  }\n  if (els.modeTrigger && els.modeMenu) {\n    els.modeTrigger.addEventListener(\"click\", function (e) {\n      e.preventDefault();\n      e.stopPropagation();\n      if (els.modeMenu.hidden) {\n        openModeMenu();\n      } else {\n        closeModeMenu();\n      }\n    });\n  }\n  if (els.modelTrigger && els.modelMenu) {\n    els.modelTrigger.addEventListener(\"click\", function (e) {\n      e.preventDefault();\n      e.stopPropagation();\n      if (!(settings.models || []).length) {\n        openSettings();\n        return;\n      }\n      if (els.modelMenu.hidden) {\n        openModelMenu();\n      } else {\n        closeModelMenu();\n      }\n    });\n  }\n  document.addEventListener(\"click\", function (e) {\n    const target = e.target;\n    if (!(target instanceof Element)) {\n      closeAllMenus();\n      return;\n    }\n    if (\n      target.closest(\"#modePicker\") ||\n      target.closest(\"#modelPicker\") ||\n      target.closest(\"#modeMenu\") ||\n      target.closest(\"#modelMenu\")\n    ) {\n      return;\n    }\n    closeAllMenus();\n  });\n  window.addEventListener(\"resize\", function () {\n    closeAllMenus();\n  });\n  if (els.toggleAgentsRailBtn) {\n    els.toggleAgentsRailBtn.addEventListener(\"click\", function () {\n      setAgentsRailOpen(!agentsRailOpen);\n    });\n  }\n  if (els.agentsRailBackdrop) {\n    els.agentsRailBackdrop.addEventListener(\"click\", function () {\n      setAgentsRailOpen(false);\n    });\n  }\n  if (els.newAgentBtn) {\n    els.newAgentBtn.addEventListener(\"click\", function () {\n      session.activeAgentId = null;\n      ensureAgent();\n      lastAssistantText = \"\";\n      renderAgents();\n      renderChat();\n      setAgentsRailOpen(false);\n      showScreen(\"chat\");\n      void persistSession();\n    });\n  }\n  if (els.openSettingsBtn) {\n    els.openSettingsBtn.addEventListener(\"click\", openSettings);\n  }\n  if (els.closeSettingsBtn) {\n    els.closeSettingsBtn.addEventListener(\"click\", function () {\n      closeProviderEdit();\n      closeModelEdit();\n      showScreen(\"chat\");\n      renderModelMenu();\n    });\n  }\n  if (els.settingsNav) {\n    els.settingsNav.addEventListener(\"click\", function (event) {\n      const btn = event.target.closest(\".settings-nav-item\");\n      if (!btn) return;\n      showSettingsCategory(btn.getAttribute(\"data-settings-cat\") || \"models\");\n    });\n  }\n  if (els.addProviderBtn) {\n    els.addProviderBtn.addEventListener(\"click\", function () {\n      openProviderEdit(-1);\n    });\n  }\n  if (els.addModelBtn) {\n    els.addModelBtn.addEventListener(\"click\", function () {\n      openModelEdit(-1);\n    });\n  }\n  if (els.settingsProvidersModelsList) {\n    els.settingsProvidersModelsList.addEventListener(\"click\", function (e) {\n      const target = e.target;\n      if (!(target instanceof Element)) return;\n      const editP = target.closest(\".settings-provider-edit\");\n      if (editP) {\n        openProviderEdit(Number(editP.getAttribute(\"data-index\")));\n        return;\n      }\n      const remP = target.closest(\".settings-provider-remove\");\n      if (remP) {\n        const idx = Number(remP.getAttribute(\"data-index\"));\n        settings.providers.splice(idx, 1);\n        renderSettingsCatalog();\n        void persistSettings().then(flashSaved);\n        return;\n      }\n      const editM = target.closest(\".settings-model-edit\");\n      if (editM) {\n        openModelEdit(Number(editM.getAttribute(\"data-index\")));\n        return;\n      }\n      const remM = target.closest(\".settings-model-remove\");\n      if (remM) {\n        const idx = Number(remM.getAttribute(\"data-index\"));\n        settings.models.splice(idx, 1);\n        renderSettingsCatalog();\n        renderModelMenu();\n        void persistSettings().then(flashSaved);\n        return;\n      }\n    });\n    els.settingsProvidersModelsList.addEventListener(\"change\", function (e) {\n      const target = e.target;\n      if (!(target instanceof HTMLInputElement)) return;\n      if (!target.classList.contains(\"settings-model-toggle\")) return;\n      const idx = Number(target.getAttribute(\"data-index\"));\n      if (!settings.models[idx]) return;\n      settings.models[idx].enabled = target.checked;\n      renderSettingsCatalog();\n      renderModelMenu();\n      void persistSettings().then(flashSaved);\n    });\n  }\n\n  function bindModalDismiss(modal, closeFn, attr) {\n    if (!modal) return;\n    modal.addEventListener(\"click",
  "\", function (e) {\n      const el = e.target;\n      if (!(el instanceof Element)) return;\n      if (el.getAttribute(attr) === \"1\") closeFn();\n    });\n  }\n  bindModalDismiss(els.providerEditModal, closeProviderEdit, \"data-provider-dismiss\");\n  bindModalDismiss(els.modelEditModal, closeModelEdit, \"data-modal-dismiss\");\n  if (els.providerEditCloseBtn) {\n    els.providerEditCloseBtn.addEventListener(\"click\", closeProviderEdit);\n  }\n  if (els.providerEditCancelBtn) {\n    els.providerEditCancelBtn.addEventListener(\"click\", closeProviderEdit);\n  }\n  if (els.providerEditDoneBtn) {\n    els.providerEditDoneBtn.addEventListener(\"click\", saveProviderEdit);\n  }\n  if (els.modelEditCloseBtn) {\n    els.modelEditCloseBtn.addEventListener(\"click\", closeModelEdit);\n  }\n  if (els.modelEditCancelBtn) {\n    els.modelEditCancelBtn.addEventListener(\"click\", closeModelEdit);\n  }\n  if (els.modelEditDoneBtn) {\n    els.modelEditDoneBtn.addEventListener(\"click\", saveModelEdit);\n  }\n\n  if (els.langSelect) {\n    els.langSelect.addEventListener(\"change\", function () {\n      settings.language = els.langSelect.value || \"en\";\n      applyChrome();\n      void persistSettings().then(flashSaved);\n    });\n  }\n  if (els.fontSizeRange) {\n    els.fontSizeRange.addEventListener(\"input\", function () {\n      settings.fontSize = Number(els.fontSizeRange.value) || 13;\n      applyChrome();\n    });\n    els.fontSizeRange.addEventListener(\"change\", function () {\n      void persistSettings().then(flashSaved);\n    });\n  }\n  if (els.copyBriefBtn) {\n    els.copyBriefBtn.addEventListener(\"click\", function () {\n      const brief = Turn.buildHandoffBrief(lastAssistantText, selection);\n      void navigator.clipboard.writeText(brief).then(\n        function () {\n          host.postMessage({ type: \"copyBrief\", text: brief });\n          setStatus(t(\"briefCopied\"));\n        },\n        function () {\n          host.postMessage({ type: \"copyBrief\", text: brief });\n        }\n      );\n    });\n  }\n\n  async function boot() {\n    const storedSettings = await F.storageGet(F.STORAGE_SETTINGS);\n    if (storedSettings && typeof storedSettings === \"object\") {\n      settings = Object.assign(settings, storedSettings);\n      if (!Array.isArray(settings.providers)) settings.providers = [];\n      if (!Array.isArray(settings.models)) settings.models = [];\n    }\n    const storedSession = await F.storageGet(F.STORAGE_SESSION);\n    if (storedSession && typeof storedSession === \"object\") {\n      session = Object.assign(session, storedSession);\n      if (!Array.isArray(session.agents)) session.agents = [];\n    }\n    ensureAgent();\n    const agent = activeAgent();\n    if (agent) {\n      mode = normalizeMode(agent.mode);\n      selectedModelId = agent.modelId || selectedModelId;\n    }\n    applyChrome();\n    renderModelMenu();\n    renderAgents();\n    renderChat();\n    renderSelection();\n    showScreen(\"chat\");\n    if (!hasUsableSetup()) {\n      // Soft nudge: empty state CTA already points to Settings.\n    }\n    host.postMessage({ type: \"ready\", surface: \"figma\" });\n    host.postMessage({ type: \"figmaGetSelection\" });\n  }\n\n  void boot();\n  } catch (err) {\n    var msg = err && err.stack ? err.stack : String(err);\n    document.body.innerHTML =\n      \"<pre style='padding:16px;color:#c00;white-space:pre-wrap;font:12px monospace'>Harbor UI crash:\\n\" +\n      msg +\n      \"</pre>\";\n  }\n})();\n\n</script>\n  </body>\n</html>\n"
].join("");
/**
 * Harbor Agents - Figma main logic (source).
 * Built into code.js: `npm run build:figma`
 * Prefer manifest `__html__` (ui.html). In Dev Mode it is often empty for large UI -
 * then use chunked `__HTML_UI__` injected by the build (avoids one giant string literal).
 * Dev Mode: manifest needs capabilities: ["inspect"]. Writes require Design mode.
 */
/* global __html__, __HTML_UI__ */

figma.notify("Harbor Agents");

try {
  var html =
    typeof __html__ === "string" && __html__.length > 32 ? __html__ : null;
  if (
    !html &&
    typeof __HTML_UI__ === "string" &&
    __HTML_UI__.length > 32
  ) {
    html = __HTML_UI__;
  }
  if (!html) {
    figma.notify("UI missing - run npm run build:figma", { error: true });
    html =
      "<html><body style=\"margin:0;font:14px -apple-system,sans-serif;padding:16px;background:#1e1e1e;color:#fff\">" +
      "<h2 style=\"margin:0 0 8px\">Harbor Agents</h2>" +
      "<p style=\"margin:0;opacity:.85\">Rebuild the plugin (build:figma) and re-import manifest.</p>" +
      "</body></html>";
  }
  figma.showUI(html, {
    width: 420,
    height: 640,
    title: "Harbor Agents",
  });
} catch (err) {
  figma.notify(
    "showUI failed: " + (err && err.message ? err.message : String(err)),
    { error: true }
  );
}

var FIGMA_STORAGE_SETTINGS = "harbor.figma.settings";
var FIGMA_STORAGE_SESSION = "harbor.figma.session";
var FIGMA_STORAGE_UI_STATE = "harbor.figma.uiState";
var MAX_DEPTH = 4;
var MAX_CHILDREN = 24;
var TEXT_CHARS_MAX = 500;

function postToUi(msg) {
  try {
    figma.ui.postMessage(msg);
  } catch (_e) {
    /* ui not ready */
  }
}

function bytesToBase64(bytes) {
  var chars =
    "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
  var out = "";
  for (var i = 0; i < bytes.length; i += 3) {
    var a = bytes[i];
    var b = i + 1 < bytes.length ? bytes[i + 1] : 0;
    var c = i + 2 < bytes.length ? bytes[i + 2] : 0;
    var triple = (a << 16) | (b << 8) | c;
    out += chars[(triple >> 18) & 63];
    out += chars[(triple >> 12) & 63];
    out += i + 1 < bytes.length ? chars[(triple >> 6) & 63] : "=";
    out += i + 2 < bytes.length ? chars[triple & 63] : "=";
  }
  return out;
}

function canWriteCanvas() {
  try {
    if (typeof figma.editorType === "string" && figma.editorType === "dev") {
      return false;
    }
  } catch (_e) {
    /* ignore */
  }
  return true;
}

function paintToSimple(paint) {
  if (!paint || paint.type !== "SOLID" || !paint.color) return null;
  var c = paint.color;
  return {
    type: "SOLID",
    r: Math.round(c.r * 1000) / 1000,
    g: Math.round(c.g * 1000) / 1000,
    b: Math.round(c.b * 1000) / 1000,
    a:
      paint.opacity != null
        ? Math.round(paint.opacity * 1000) / 1000
        : 1,
  };
}

function paintsToSimple(paints) {
  if (!paints || paints === figma.mixed) return undefined;
  var out = [];
  for (var i = 0; i < paints.length && i < 8; i++) {
    var p = paintToSimple(paints[i]);
    if (p) out.push(p);
  }
  return out.length ? out : undefined;
}

function enrichNodeDetails(node, out, depth) {
  if (depth > 1) return;
  try {
    if ("fills" in node) {
      var fills = paintsToSimple(node.fills);
      if (fills) out.fills = fills;
    }
    if ("strokes" in node) {
      var strokes = paintsToSimple(node.strokes);
      if (strokes) out.strokes = strokes;
    }
  } catch (_e) {
    /* mixed / remote */
  }
  if (node.type === "TEXT") {
    try {
      var chars = String(node.characters || "");
      out.characters =
        chars.length > TEXT_CHARS_MAX
          ? chars.slice(0, TEXT_CHARS_MAX) + "..."
          : chars;
      if (node.fontSize !== figma.mixed && typeof node.fontSize === "number") {
        out.fontSize = node.fontSize;
      }
      if (node.fontName !== figma.mixed && node.fontName) {
        out.fontName = {
          family: node.fontName.family,
          style: node.fontName.style,
        };
      }
    } catch (_e2) {
      /* font not loaded */
    }
  }
  if (
    node.type === "FRAME" ||
    node.type === "COMPONENT" ||
    node.type === "INSTANCE" ||
    node.type === "COMPONENT_SET"
  ) {
    try {
      if (node.layoutMode && node.layoutMode !== "NONE") {
        out.layoutMode = node.layoutMode;
        out.paddingTop = node.paddingTop;
        out.paddingRight = node.paddingRight;
        out.paddingBottom = node.paddingBottom;
        out.paddingLeft = node.paddingLeft;
        out.itemSpacing = node.itemSpacing;
        if (node.primaryAxisAlignItems) {
          out.primaryAxisAlignItems = node.primaryAxisAlignItems;
        }
        if (node.counterAxisAlignItems) {
          out.counterAxisAlignItems = node.counterAxisAlignItems;
        }
      }
    } catch (_e3) {
      /* ignore */
    }
  }
  if (node.type === "INSTANCE") {
    try {
      out.componentId = node.componentId || undefined;
      if (node.mainComponent && node.mainComponent.name) {
        out.mainComponentName = node.mainComponent.name;
      }
    } catch (_e4) {
      /* remote mainComponent */
    }
  }
}

function serializeNode(node, depth) {
  var box = node.absoluteBoundingBox;
  var out = { id: node.id, name: node.name, type: node.type };
  if (box) {
    out.width = Math.round(box.width);
    out.height = Math.round(box.height);
  }
  enrichNodeDetails(node, out, depth);
  if (depth < MAX_DEPTH && node.children && node.children.length) {
    var kids = node.children;
    out.children = [];
    for (var i = 0; i < kids.length && i < MAX_CHILDREN; i++) {
      out.children.push(serializeNode(kids[i], depth + 1));
    }
    if (kids.length > MAX_CHILDREN) {
      out.children.push({
        id: node.id + "__more",
        name: "... +" + (kids.length - MAX_CHILDREN) + " more",
        type: "TRUNCATED",
      });
    }
  }
  return out;
}

function nodeDeepLink(fileKey, nodeId) {
  if (!fileKey || !nodeId) return undefined;
  return (
    "https://www.figma.com/design/" +
    fileKey +
    "?node-id=" +
    encodeURIComponent(String(nodeId).replace(/:/g, "-"))
  );
}

function captureSelection() {
  var selection = figma.currentPage.selection;
  var fileKey = figma.fileKey;
  var nodes = [];
  for (var i = 0; i < selection.length; i++) {
    nodes.push(serializeNode(selection[i], 0));
  }
  var primary = selection[0];
  var done = function (preview) {
    return {
      fileKey: fileKey || undefined,
      fileName: figma.root.name,
      pageName: figma.currentPage.name,
      nodes: nodes,
      previewPngDataUrl: preview,
      nodeUrl: primary ? nodeDeepLink(fileKey, primary.id) : undefined,
      canWrite: canWriteCanvas(),
    };
  };
  if (!primary) {
    return Promise.resolve(done(undefined));
  }
  return primary
    .exportAsync({ format: "PNG", constraint: { type: "SCALE", value: 1 } })
    .then(function (bytes) {
      return done("data:image/png;base64," + bytesToBase64(bytes));
    })
    .catch(function () {
      return done(undefined);
    });
}

function pushSelection(requestId) {
  return captureSelection()
    .then(function (selection) {
      postToUi({
        type: "figmaSelectionChanged",
        selection: selection,
        requestId: requestId,
      });
    })
    .catch(function (err) {
      figma.notify(
        "Selection sync failed: " + (err && err.message ? err.message : err),
        { error: true }
      );
    });
}

function resolveNode(nodeId) {
  if (!nodeId || typeof nodeId !== "string") {
    return Promise.reject(new Error("nodeId is required"));
  }
  if (typeof figma.getNodeByIdAsync === "function") {
    return figma.getNodeByIdAsync(nodeId).then(function (node) {
      if (!node) throw new Error("Node not found: " + nodeId);
      return node;
    });
  }
  var sync = figma.getNodeById(nodeId);
  if (!sync) return Promise.reject(new Error("Node not found: " + nodeId));
  return Promise.resolve(sync);
}

function focusNode(nodeId) {
  return resolveNode(nodeId).then(function (node) {
    if (!("type" in node) || node.type === "PAGE" || node.type === "DOCUMENT") {
      throw new Error("Cannot focus page/document node");
    }
    var page = node.parent;
    while (page && page.type !== "PAGE") {
      page = page.parent;
    }
    if (page && page.type === "PAGE" && page !== figma.currentPage) {
      if (typeof figma.setCurrentPageAsync === "function") {
        return figma.setCurrentPageAsync(page).then(function () {
          figma.currentPage.selection = [node];
          figma.viewport.scrollAndZoomIntoView([node]);
          return { id: node.id, name: node.name, type: node.type };
        });
      }
    }
    figma.currentPage.selection = [node];
    figma.viewport.scrollAndZoomIntoView([node]);
    return { id: node.id, name: node.name, type: node.type };
  });
}

function inspectNode(nodeId) {
  return resolveNode(nodeId).then(function (node) {
    if (!("absoluteBoundingBox" in node) && node.type !== "TEXT") {
      return serializeNode(node, 0);
    }
    return serializeNode(node, 0);
  });
}

function requireWrite() {
  if (!canWriteCanvas()) {
    throw new Error(
      "Canvas writes are unavailable in Dev Mode - open the file in Design mode."
    );
  }
}

function solidPaintFromArgs(color) {
  if (!color || typeof color !== "object") {
    throw new Error("fills requires { r, g, b, a? } with 0-1 channels");
  }
  var a = color.a != null ? Number(color.a) : 1;
  return {
    type: "SOLID",
    color: {
      r: Number(color.r) || 0,
      g: Number(color.g) || 0,
      b: Number(color.b) || 0,
    },
    opacity: isNaN(a) ? 1 : a,
  };
}

function setNodeName(nodeId, name) {
  requireWrite();
  return resolveNode(nodeId).then(function (node) {
    if (!("name" in node)) throw new Error("Node has no name");
    node.name = String(name || "");
    return { id: node.id, name: node.name };
  });
}

function setNodeText(nodeId, characters) {
  requireWrite();
  return resolveNode(nodeId).then(function (node) {
    if (node.type !== "TEXT") {
      throw new Error("figma_set_text requires a TEXT node");
    }
    var font =
      node.fontName !== figma.mixed
        ? node.fontName
        : { family: "Inter", style: "Regular" };
    return figma.loadFontAsync(font).then(function () {
      node.characters = String(characters != null ? characters : "");
      return {
        id: node.id,
        characters: node.characters.slice(0, TEXT_CHARS_MAX),
      };
    });
  });
}

function setNodeFills(nodeId, color) {
  requireWrite();
  return resolveNode(nodeId).then(function (node) {
    if (!("fills" in node)) throw new Error("Node has no fills");
    node.fills = [solidPaintFromArgs(color)];
    return { id: node.id, fills: paintsToSimple(node.fills) };
  });
}

function setAutoLayout(nodeId, args) {
  requireWrite();
  return resolveNode(nodeId).then(function (node) {
    if (!("layoutMode" in node)) {
      throw new Error("Node does not support auto-layout");
    }
    if (args.layoutMode != null) {
      var lm = String(args.layoutMode).toUpperCase();
      if (lm === "NONE" || lm === "HORIZONTAL" || lm === "VERTICAL") {
        node.layoutMode = lm;
      }
    }
    if (args.paddingTop != null) node.paddingTop = Number(args.paddingTop);
    if (args.paddingRight != null) node.paddingRight = Number(args.paddingRight);
    if (args.paddingBottom != null)
      node.paddingBottom = Number(args.paddingBottom);
    if (args.paddingLeft != null) node.paddingLeft = Number(args.paddingLeft);
    if (args.itemSpacing != null) node.itemSpacing = Number(args.itemSpacing);
    if (args.padding != null) {
      var p = Number(args.padding);
      node.paddingTop = p;
      node.paddingRight = p;
      node.paddingBottom = p;
      node.paddingLeft = p;
    }
    return {
      id: node.id,
      layoutMode: node.layoutMode,
      paddingTop: node.paddingTop,
      paddingRight: node.paddingRight,
      paddingBottom: node.paddingBottom,
      paddingLeft: node.paddingLeft,
      itemSpacing: node.itemSpacing,
    };
  });
}

function invokeTool(name, args) {
  args = args && typeof args === "object" ? args : {};
  if (name === "figma_get_selection") {
    return captureSelection().then(function (sel) {
      var slim = {
        fileName: sel.fileName,
        pageName: sel.pageName,
        nodeUrl: sel.nodeUrl,
        canWrite: sel.canWrite,
        nodes: sel.nodes,
      };
      return slim;
    });
  }
  if (name === "figma_inspect_node") {
    return inspectNode(args.nodeId);
  }
  if (name === "figma_focus_node") {
    return focusNode(args.nodeId);
  }
  if (name === "figma_set_name") {
    return setNodeName(args.nodeId, args.name).then(function (r) {
      pushSelection();
      return r;
    });
  }
  if (name === "figma_set_text") {
    return setNodeText(args.nodeId, args.characters).then(function (r) {
      pushSelection();
      return r;
    });
  }
  if (name === "figma_set_fills") {
    return setNodeFills(args.nodeId, args.color || args).then(function (r) {
      pushSelection();
      return r;
    });
  }
  if (name === "figma_set_auto_layout") {
    return setAutoLayout(args.nodeId, args).then(function (r) {
      pushSelection();
      return r;
    });
  }
  return Promise.reject(new Error("Unknown tool: " + name));
}

function replyTool(requestId, ok, result, error) {
  postToUi({
    type: "figmaToolResult",
    requestId: requestId,
    ok: !!ok,
    result: result,
    error: error ? String(error) : undefined,
  });
}

figma.ui.onmessage = function (msg) {
  if (!msg || typeof msg !== "object") return;
  var type = msg.type;
  if (type === "ready" || type === "figmaGetSelection") {
    pushSelection(msg.requestId);
    return;
  }
  if (type === "figmaFocusNode") {
    focusNode(msg.nodeId)
      .then(function () {
        pushSelection();
      })
      .catch(function (err) {
        figma.notify(
          "Focus failed: " + (err && err.message ? err.message : err),
          { error: true }
        );
      });
    return;
  }
  if (type === "figmaInvokeTool") {
    var requestId = msg.requestId;
    invokeTool(msg.name, msg.args)
      .then(function (result) {
        replyTool(requestId, true, result, null);
      })
      .catch(function (err) {
        replyTool(
          requestId,
          false,
          null,
          err && err.message ? err.message : String(err)
        );
      });
    return;
  }
  if (type === "figmaClientStorageGet") {
    figma.clientStorage.getAsync(msg.key).then(function (value) {
      postToUi({
        type: "figmaClientStorageValue",
        key: msg.key,
        value: value == null ? null : value,
        requestId: msg.requestId,
      });
    });
    return;
  }
  if (type === "figmaClientStorageSet") {
    var key = msg.key;
    var allowed =
      key === FIGMA_STORAGE_SETTINGS ||
      key === FIGMA_STORAGE_SESSION ||
      key === FIGMA_STORAGE_UI_STATE ||
      (typeof key === "string" && key.indexOf("harbor.figma.") === 0);
    if (!allowed) {
      figma.notify("Blocked storage key", { error: true });
      return;
    }
    figma.clientStorage.setAsync(key, msg.value).then(function () {
      postToUi({
        type: "figmaClientStorageSaved",
        key: key,
        requestId: msg.requestId,
      });
    });
    return;
  }
  if (type === "copyBrief" || type === "copyText") {
    postToUi({ type: "copied" });
    figma.notify(type === "copyBrief" ? "Brief copied" : "Copied");
    return;
  }
  if (type === "notify") {
    figma.notify(msg.message || "", { error: !!msg.error });
    return;
  }
  if (type === "resize") {
    figma.ui.resize(
      Math.max(320, Math.min(900, msg.width | 0)),
      Math.max(400, Math.min(900, msg.height | 0))
    );
  }
};

figma.on("selectionchange", function () {
  pushSelection();
});
figma.on("currentpagechange", function () {
  pushSelection();
});
