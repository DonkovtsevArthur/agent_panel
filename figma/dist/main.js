var __HTML_UI__ = "<!DOCTYPE html>\n<html lang=\"en\" data-surface=\"figma\">\n  <head>\n    <meta charset=\"UTF-8\" />\n    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\" />\n    <title>Harbor Agents</title>\n    <style>\n:root {\n  color-scheme: light dark;\n  --radius-sm: 8px;\n  --radius-md: 10px;\n  --radius-pill: 999px;\n  --harbor-font-family: -apple-system, BlinkMacSystemFont, \"Segoe UI\", system-ui,\n    sans-serif;\n  --harbor-font-size: 13px;\n  --harbor-bg: var(--figma-color-bg, #ffffff);\n  --harbor-fg: var(--figma-color-text, #1e1e1e);\n  --harbor-muted: var(--figma-color-text-secondary, #6b6b6b);\n  --harbor-border: var(--figma-color-border, #e5e5e5);\n  --harbor-bubble: var(--figma-color-bg-secondary, #f5f5f5);\n  --harbor-accent: var(--figma-color-bg-brand, #0d99ff);\n  --harbor-user: color-mix(in srgb, var(--harbor-accent) 18%, var(--harbor-bubble));\n  --mode-ask: #2d6a4f;\n  --mode-plan: #a67c00;\n}\n\n* {\n  box-sizing: border-box;\n}\n\nhtml,\nbody {\n  margin: 0;\n  padding: 0;\n  height: 100%;\n  font-family: var(--harbor-font-family);\n  font-size: var(--harbor-font-size);\n  color: var(--harbor-fg);\n  background: var(--harbor-bg);\n}\n\nbody {\n  display: flex;\n  flex-direction: column;\n  overflow: hidden;\n}\n\n.workspace-shell {\n  display: flex;\n  flex-direction: column;\n  height: 100%;\n  min-height: 0;\n}\n\n.screen {\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\n\n.screen[hidden] {\n  display: none !important;\n}\n\n.chat-top,\n.agents-top,\n.settings-top {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  padding: 8px 10px;\n  border-bottom: 1px solid var(--harbor-border);\n  flex-shrink: 0;\n}\n\n.chat-agent-name,\n.agents-title,\n.settings-title {\n  font-weight: 600;\n  flex: 1;\n  min-width: 0;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n\n.icon-btn,\n.primary-btn,\n.ghost-btn,\n.link-btn,\n.menu-item,\n.agent-row {\n  font: inherit;\n  color: inherit;\n  background: transparent;\n  border: none;\n  cursor: pointer;\n}\n\n.icon-btn {\n  min-width: 32px;\n  height: 32px;\n  padding: 0 8px;\n  border-radius: var(--radius-sm);\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  font-size: 12px;\n  font-weight: 600;\n}\n\n.icon-btn:hover,\n.agent-row:hover,\n.menu-item:hover {\n  background: var(--harbor-bubble);\n}\n\n.primary-btn {\n  background: var(--harbor-accent);\n  color: #fff;\n  border-radius: var(--radius-sm);\n  padding: 6px 12px;\n  font-weight: 600;\n}\n\n.ghost-btn {\n  border: 1px solid var(--harbor-border);\n  border-radius: var(--radius-sm);\n  padding: 6px 10px;\n}\n\n.link-btn {\n  color: var(--harbor-accent);\n  padding: 4px 0;\n  text-align: left;\n}\n\n.agents-list {\n  overflow: auto;\n  padding: 8px;\n  flex: 1;\n}\n\n.agent-row {\n  display: block;\n  width: 100%;\n  text-align: left;\n  padding: 10px 12px;\n  border-radius: var(--radius-sm);\n  margin-bottom: 4px;\n}\n\n.agent-row.active {\n  background: var(--harbor-bubble);\n  font-weight: 600;\n}\n\n#messages {\n  flex: 1;\n  overflow: auto;\n  padding: 12px;\n  display: flex;\n  flex-direction: column;\n  gap: 10px;\n}\n\n.empty-hint {\n  color: var(--harbor-muted);\n  text-align: center;\n  margin: auto;\n  padding: 24px;\n}\n\n.msg {\n  max-width: 100%;\n  padding: 10px 12px;\n  border-radius: var(--radius-md);\n  line-height: 1.45;\n  word-break: break-word;\n}\n\n.msg-user {\n  align-self: flex-end;\n  background: var(--harbor-user);\n}\n\n.msg-assistant {\n  align-self: stretch;\n  background: var(--harbor-bubble);\n}\n\n.msg-assistant h1,\n.msg-assistant h2,\n.msg-assistant h3 {\n  font-size: 1em;\n  margin: 0.4em 0;\n}\n\n.msg-assistant p {\n  margin: 0.35em 0;\n}\n\n.msg-assistant code {\n  font-family: ui-monospace, Menlo, Consolas, monospace;\n  font-size: 0.92em;\n}\n\n.composer-wrap {\n  border-top: 1px solid var(--harbor-border);\n  padding: 8px 10px 10px;\n  flex-shrink: 0;\n  background: var(--harbor-bg);\n}\n\n.selection-preview {\n  display: flex;\n  gap: 8px;\n  align-items: center;\n  margin-bottom: 8px;\n  padding: 6px 8px;\n  border-radius: var(--radius-sm);\n  background: var(--harbor-bubble);\n  font-size: 12px;\n  color: var(--harbor-muted);\n}\n\n.selection-preview img {\n  width: 40px;\n  height: 40px;\n  object-fit: contain;\n  border-radius: 4px;\n  background: #fff;\n}\n\n.composer {\n  border: 1px solid var(--harbor-border);\n  border-radius: var(--radius-md);\n  padding: 8px;\n}\n\n.composer[data-mode=\"ask\"] {\n  border-color: color-mix(in srgb, var(--mode-ask) 35%, var(--harbor-border));\n}\n\n.composer[data-mode=\"plan\"] {\n  border-color: color-mix(in srgb, var(--mode-plan) 35%, var(--harbor-border));\n}\n\n#prompt {\n  width: 100%;\n  border: none;\n  resize: none;\n  outline: none;\n  background: transparent;\n  color: inherit;\n  font: inherit;\n  min-height: 56px;\n}\n\n.composer-footer {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 8px;\n  margin-top: 6px;\n}\n\n.composer-footer-left,\n.composer-footer-right {\n  display: flex;\n  align-items: center;\n  gap: 6px;\n}\n\n.model-picker {\n  position: relative;\n}\n\n.model-trigger {\n  display: inline-flex;\n  align-items: center;\n  gap: 2px;\n  border: 1px solid var(--harbor-border);\n  border-radius: var(--radius-pill);\n  padding: 4px 8px;\n  background: var(--harbor-bg);\n  font: inherit;\n  color: inherit;\n  cursor: pointer;\n  max-width: 140px;\n}\n\n.model-label {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n\n.model-menu {\n  position: absolute;\n  bottom: calc(100% + 4px);\n  left: 0;\n  min-width: 160px;\n  max-width: 240px;\n  background: var(--harbor-bg);\n  border: 1px solid var(--harbor-border);\n  border-radius: var(--radius-sm);\n  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);\n  z-index: 20;\n  padding: 4px;\n}\n\n.model-menu[hidden] {\n  display: none !important;\n}\n\n.menu-item {\n  display: block;\n  width: 100%;\n  text-align: left;\n  padding: 8px 10px;\n  border-radius: 6px;\n}\n\n.status-line {\n  min-height: 16px;\n  font-size: 11px;\n  color: var(--harbor-muted);\n  margin-top: 6px;\n}\n\n.settings-body {\n  flex: 1;\n  overflow: auto;\n  padding: 12px;\n  display: flex;\n  flex-direction: column;\n  gap: 16px;\n}\n\n.settings-section h2 {\n  font-size: 13px;\n  margin: 0 0 8px;\n}\n\n.settings-card {\n  border: 1px solid var(--harbor-border);\n  border-radius: var(--radius-sm);\n  padding: 10px;\n  margin-bottom: 8px;\n  display: flex;\n  flex-direction: column;\n  gap: 8px;\n}\n\n.settings-card label {\n  display: flex;\n  flex-direction: column;\n  gap: 4px;\n  font-size: 12px;\n  color: var(--harbor-muted);\n}\n\n.settings-card input,\n.settings-card select,\n#langSelect {\n  font: inherit;\n  color: inherit;\n  background: var(--harbor-bg);\n  border: 1px solid var(--harbor-border);\n  border-radius: 6px;\n  padding: 6px 8px;\n}\n\n.settings-card .check {\n  flex-direction: row;\n  align-items: center;\n  gap: 8px;\n}\n\n.settings-actions {\n  display: flex;\n  gap: 8px;\n  padding: 10px 12px;\n  border-top: 1px solid var(--harbor-border);\n}\n\n.material-symbols-outlined {\n  font-family: \"Material Symbols Outlined\", sans-serif;\n  font-weight: normal;\n  font-style: normal;\n  font-size: 18px;\n  line-height: 1;\n  display: inline-block;\n  font-variation-settings: \"FILL\" 0, \"wght\" 400, \"GRAD\" 0, \"opsz\" 24;\n}\n\n</style>\n  </head>\n  <body>\n    <div class=\"workspace-shell\">\n      <section id=\"agentsScreen\" class=\"screen\" hidden>\n        <div class=\"agents-top\">\n          <div class=\"agents-title\" data-i18n=\"agents\">Agents</div>\n          <button type=\"button\" class=\"icon-btn\" id=\"newAgentBtn\" title=\"New chat\" aria-label=\"New chat\">+</button>\n        </div>\n        <div id=\"agentsList\" class=\"agents-list\"></div>\n      </section>\n\n      <section id=\"chatScreen\" class=\"screen\">\n        <div class=\"chat-top\">\n          <button type=\"button\" class=\"icon-btn\" id=\"toggleAgentsRailBtn\" title=\"Agents\" aria-label=\"Agents\">List</button>\n          <div id=\"chatAgentName\" class=\"chat-agent-name\">Chat</div>\n          <button type=\"button\" class=\"ghost-btn\" id=\"copyBriefBtn\" data-i18n=\"copyBrief\" hidden>Copy brief</button>\n          <button type=\"button\" class=\"icon-btn\" id=\"openSettingsBtn\" title=\"Settings\" aria-label=\"Settings\">Set</button>\n        </div>\n        <div id=\"messages\"></div>\n        <div class=\"composer-wrap\">\n          <div id=\"selectionPreview\" class=\"selection-preview\" hidden>\n            <img id=\"selectionThumb\" alt=\"\" hidden />\n            <div id=\"selectionMeta\"></div>\n          </div>\n          <div class=\"composer\" id=\"composer\" data-mode=\"ask\">\n            <textarea id=\"prompt\" rows=\"3\" placeholder=\"Ask about the selection…\"></textarea>\n            <div class=\"composer-footer\">\n              <div class=\"composer-footer-left\">\n                <div class=\"model-picker mode-picker\" id=\"modePicker\">\n                  <button type=\"button\" class=\"model-trigger\" id=\"modeTrigger\">\n                    <span class=\"model-label\" id=\"modeLabel\">Ask</span>\n                    <span aria-hidden=\"true\">v</span>\n                  </button>\n                  <div class=\"model-menu\" id=\"modeMenu\" role=\"listbox\" hidden></div>\n                </div>\n              </div>\n              <div class=\"composer-footer-right\">\n                <div class=\"model-picker\" id=\"modelPicker\">\n                  <button type=\"button\" class=\"model-trigger\" id=\"modelTrigger\">\n                    <span class=\"model-label\" id=\"modelLabel\">Model</span>\n                    <span aria-hidden=\"true\">v</span>\n                  </button>\n                  <div class=\"model-menu\" id=\"modelMenu\" role=\"listbox\" hidden></div>\n                </div>\n                <button type=\"button\" class=\"primary-btn\" id=\"sendBtn\" data-i18n=\"send\">Send</button>\n                <button type=\"button\" class=\"ghost-btn\" id=\"stopBtn\" data-i18n=\"stop\" hidden>Stop</button>\n              </div>\n            </div>\n          </div>\n          <div id=\"statusLine\" class=\"status-line\"></div>\n        </div>\n      </section>\n\n      <section id=\"settingsScreen\" class=\"screen\" hidden>\n        <div class=\"settings-top\">\n          <button type=\"button\" class=\"icon-btn\" id=\"closeSettingsBtn\" title=\"Back\" aria-label=\"Back\">Back</button>\n          <div class=\"settings-title\" data-i18n=\"settings\">Settings</div>\n        </div>\n        <div class=\"settings-body\">\n          <div class=\"settings-section\">\n            <h2 data-i18n=\"providers\">Providers</h2>\n            <div id=\"providersList\"></div>\n            <button type=\"button\" class=\"link-btn\" id=\"addProviderBtn\" data-i18n=\"addProvider\">+ Provider</button>\n          </div>\n          <div class=\"settings-section\">\n            <h2 data-i18n=\"models\">Models</h2>\n            <div id=\"modelsList\"></div>\n            <button type=\"button\" class=\"link-btn\" id=\"addModelBtn\" data-i18n=\"addModel\">+ Model</button>\n          </div>\n          <div class=\"settings-section\">\n            <h2 data-i18n=\"language\">Language</h2>\n            <select id=\"langSelect\">\n              <option value=\"en\">English</option>\n              <option value=\"ru\">Русский</option>\n            </select>\n          </div>\n          <div class=\"settings-section\">\n            <h2 data-i18n=\"appearance\">Appearance</h2>\n            <label>\n              <span data-i18n=\"fontSize\">Font size</span>\n              <input id=\"fontSizeRange\" type=\"range\" min=\"12\" max=\"18\" value=\"13\" />\n              <span id=\"fontSizeValue\">13 px</span>\n            </label>\n          </div>\n        </div>\n        <div class=\"settings-actions\">\n          <button type=\"button\" class=\"primary-btn\" id=\"saveSettingsBtn\" data-i18n=\"save\">Save</button>\n        </div>\n      </section>\n    </div>\n    <script>\n      (function () {\n        var memoryState = {};\n        globalThis.__harborHost = {\n          postMessage: function (message) {\n            parent.postMessage({ pluginMessage: message }, \"*\");\n          },\n          getState: function () {\n            return memoryState;\n          },\n          setState: function (s) {\n            memoryState = s || {};\n          },\n        };\n      })();\n    <\\/script>\n    <script>\n(function () {\n  /**\n   * Figma UI host bridge — only __harborHost (injected in ui.shell.html).\n   * IDE webview host APIs and JetBrains polyfills are excluded from this bundle.\n   */\n  const host =\n    typeof globalThis !== \"undefined\" && globalThis.__harborHost\n      ? globalThis.__harborHost\n      : {\n          postMessage: function () {},\n          getState: function () {\n            return {};\n          },\n          setState: function () {},\n        };\n\n  function cryptoToken(len) {\n    const bytes = new Uint8Array(len);\n    crypto.getRandomValues(bytes);\n    let out = \"\";\n    for (let i = 0; i < bytes.length; i++) {\n      out += bytes[i].toString(36).padStart(2, \"0\");\n    }\n    return out.slice(0, len);\n  }\n\n  const STORAGE_SETTINGS = \"harbor.figma.settings\";\n  const STORAGE_SESSION = \"harbor.figma.session\";\n  const STORAGE_UI = \"harbor.figma.uiState\";\n\n  const pendingStorage = new Map();\n  let storageReq = 0;\n\n  function storageGet(key) {\n    return new Promise(function (resolve) {\n      const requestId = \"s\" + ++storageReq;\n      pendingStorage.set(requestId, resolve);\n      host.postMessage({\n        type: \"figmaClientStorageGet\",\n        key: key,\n        requestId: requestId,\n      });\n      setTimeout(function () {\n        if (pendingStorage.has(requestId)) {\n          pendingStorage.delete(requestId);\n          resolve(null);\n        }\n      }, 3000);\n    });\n  }\n\n  function storageSet(key, value) {\n    return new Promise(function (resolve) {\n      const requestId = \"s\" + ++storageReq;\n      pendingStorage.set(requestId, function () {\n        resolve();\n      });\n      host.postMessage({\n        type: \"figmaClientStorageSet\",\n        key: key,\n        value: value,\n        requestId: requestId,\n      });\n      setTimeout(function () {\n        if (pendingStorage.has(requestId)) {\n          pendingStorage.delete(requestId);\n          resolve();\n        }\n      }, 3000);\n    });\n  }\n\n  window.__harborFigma = {\n    host: host,\n    cryptoToken: cryptoToken,\n    STORAGE_SETTINGS: STORAGE_SETTINGS,\n    STORAGE_SESSION: STORAGE_SESSION,\n    STORAGE_UI: STORAGE_UI,\n    storageGet: storageGet,\n    storageSet: storageSet,\n    pendingStorage: pendingStorage,\n  };\n})();\n\n(function () {\n  /** Slim OpenAI-compatible turn — Figma host only (no Cline / IDE tools). */\n\n  function trimSlash(url) {\n    return String(url || \"\").replace(/\\/+$/, \"\");\n  }\n\n  function designSystemPrompt(mode) {\n    const base =\n      \"You are Harbor Agents for Figma — a design assistant inside Figma. \" +\n      \"You help designers critique, name, structure, and hand off UI. \" +\n      \"You cannot edit the Figma canvas or run IDE tools. \" +\n      \"Ground answers in the current selection JSON and screenshot when provided. \" +\n      \"Be concise. Do not invent layers that are not in the selection.\";\n    if (mode === \"plan\") {\n      return (\n        base +\n        \" Mode: Plan. Produce a clear implementation / design plan with Goal and numbered Steps. \" +\n        \"End with a short handoff brief a developer can paste into an IDE agent.\"\n      );\n    }\n    return (\n      base +\n      \" Mode: Ask. Answer the design question; do not invent a full build plan unless asked.\"\n    );\n  }\n\n  function buildSelectionContextText(selection) {\n    if (!selection || !selection.nodes || selection.nodes.length === 0) {\n      return \"Current Figma selection: (none — ask the user to select a frame or node).\";\n    }\n    const header = [\n      selection.fileName ? \"File: \" + selection.fileName : null,\n      selection.pageName ? \"Page: \" + selection.pageName : null,\n      selection.nodeUrl ? \"Link: \" + selection.nodeUrl : null,\n    ]\n      .filter(Boolean)\n      .join(\"\\n\");\n    return (\n      header +\n      \"\\nSelection JSON:\\n\" +\n      JSON.stringify(selection.nodes, null, 2)\n    );\n  }\n\n  function buildHandoffBrief(planText, selection) {\n    const link = (selection && selection.nodeUrl) || \"(no node link)\";\n    const name =\n      (selection &&\n        selection.nodes &&\n        selection.nodes[0] &&\n        selection.nodes[0].name) ||\n      (selection && selection.fileName) ||\n      \"selection\";\n    return (\n      \"# Harbor handoff — \" +\n      name +\n      \"\\n\\nFigma: \" +\n      link +\n      \"\\n\\n\" +\n      String(planText || \"\").trim() +\n      \"\\n\"\n    );\n  }\n\n  function modelIdForApi(modelId) {\n    const id = String(modelId || \"\");\n    if (id.indexOf(\"/\") === -1) return id;\n    const parts = id.split(\"/\");\n    return parts.slice(1).join(\"/\") || id;\n  }\n\n  async function runFigmaAgentTurn(opts) {\n    const provider = opts.provider;\n    const model = opts.model;\n    const baseUrl = trimSlash(provider && provider.baseUrl);\n    if (!baseUrl) {\n      throw new Error(\n        \"Provider base URL is empty — open Settings and add a provider.\"\n      );\n    }\n    if (!model || !model.id) {\n      throw new Error(\"No model selected — open Settings and add a model.\");\n    }\n\n    const system = designSystemPrompt(opts.mode === \"plan\" ? \"plan\" : \"ask\");\n    const selectionText = buildSelectionContextText(opts.selection);\n    const useVision =\n      !!model.supportsVision &&\n      opts.selection &&\n      opts.selection.previewPngDataUrl;\n\n    const messages = [{ role: \"system\", content: system }];\n    (opts.history || []).forEach(function (m) {\n      messages.push({ role: m.role, content: m.text });\n    });\n\n    if (useVision) {\n      messages.push({\n        role: \"user\",\n        content: [\n          { type: \"text\", text: selectionText + \"\\n\\n\" + opts.userText },\n          {\n            type: \"image_url\",\n            image_url: { url: opts.selection.previewPngDataUrl },\n          },\n        ],\n      });\n    } else {\n      messages.push({\n        role: \"user\",\n        content: selectionText + \"\\n\\n\" + opts.userText,\n      });\n    }\n\n    const headers = { \"Content-Type\": \"application/json\" };\n    if (provider.apiKey) {\n      headers.Authorization = \"Bearer \" + provider.apiKey;\n    }\n\n    const res = await fetch(baseUrl + \"/chat/completions\", {\n      method: \"POST\",\n      headers: headers,\n      body: JSON.stringify({\n        model: modelIdForApi(model.id),\n        messages: messages,\n        stream: true,\n      }),\n      signal: opts.signal,\n    });\n\n    if (!res.ok) {\n      const errBody = await res.text().catch(function () {\n        return \"\";\n      });\n      throw new Error(\n        \"Provider error \" +\n          res.status +\n          \": \" +\n          (errBody.slice(0, 400) || res.statusText)\n      );\n    }\n\n    const reader = res.body && res.body.getReader();\n    if (!reader) throw new Error(\"No response body from provider\");\n\n    const decoder = new TextDecoder();\n    let full = \"\";\n    let buffer = \"\";\n    while (true) {\n      const step = await reader.read();\n      if (step.done) break;\n      buffer += decoder.decode(step.value, { stream: true });\n      const lines = buffer.split(\"\\n\");\n      buffer = lines.pop() || \"\";\n      for (let i = 0; i < lines.length; i++) {\n        const trimmed = lines[i].trim();\n        if (!trimmed.startsWith(\"data:\")) continue;\n        const data = trimmed.slice(5).trim();\n        if (data === \"[DONE]\") continue;\n        try {\n          const json = JSON.parse(data);\n          const piece =\n            json.choices &&\n            json.choices[0] &&\n            json.choices[0].delta &&\n            json.choices[0].delta.content;\n          if (piece) {\n            full += piece;\n            if (opts.onDelta) opts.onDelta(piece);\n          }\n        } catch (_e) {\n          /* ignore */\n        }\n      }\n    }\n    return full;\n  }\n\n  window.__harborFigmaTurn = {\n    runFigmaAgentTurn: runFigmaAgentTurn,\n    buildHandoffBrief: buildHandoffBrief,\n    buildSelectionContextText: buildSelectionContextText,\n  };\n})();\n\n(function () {\n  const F = window.__harborFigma;\n  const Turn = window.__harborFigmaTurn;\n  const host = F.host;\n\n  const els = {\n    agentsScreen: document.getElementById(\"agentsScreen\"),\n    chatScreen: document.getElementById(\"chatScreen\"),\n    settingsScreen: document.getElementById(\"settingsScreen\"),\n    agentsList: document.getElementById(\"agentsList\"),\n    messages: document.getElementById(\"messages\"),\n    prompt: document.getElementById(\"prompt\"),\n    sendBtn: document.getElementById(\"sendBtn\"),\n    stopBtn: document.getElementById(\"stopBtn\"),\n    modeLabel: document.getElementById(\"modeLabel\"),\n    modeMenu: document.getElementById(\"modeMenu\"),\n    modeTrigger: document.getElementById(\"modeTrigger\"),\n    modelLabel: document.getElementById(\"modelLabel\"),\n    modelMenu: document.getElementById(\"modelMenu\"),\n    modelTrigger: document.getElementById(\"modelTrigger\"),\n    chatAgentName: document.getElementById(\"chatAgentName\"),\n    selectionPreview: document.getElementById(\"selectionPreview\"),\n    selectionThumb: document.getElementById(\"selectionThumb\"),\n    selectionMeta: document.getElementById(\"selectionMeta\"),\n    copyBriefBtn: document.getElementById(\"copyBriefBtn\"),\n    openSettingsBtn: document.getElementById(\"openSettingsBtn\"),\n    closeSettingsBtn: document.getElementById(\"closeSettingsBtn\"),\n    toggleAgentsRailBtn: document.getElementById(\"toggleAgentsRailBtn\"),\n    newAgentBtn: document.getElementById(\"newAgentBtn\"),\n    providersList: document.getElementById(\"providersList\"),\n    modelsList: document.getElementById(\"modelsList\"),\n    addProviderBtn: document.getElementById(\"addProviderBtn\"),\n    addModelBtn: document.getElementById(\"addModelBtn\"),\n    langSelect: document.getElementById(\"langSelect\"),\n    fontSizeRange: document.getElementById(\"fontSizeRange\"),\n    fontSizeValue: document.getElementById(\"fontSizeValue\"),\n    saveSettingsBtn: document.getElementById(\"saveSettingsBtn\"),\n    statusLine: document.getElementById(\"statusLine\"),\n  };\n\n  /** @type {{ providers: any[], models: any[], language: string, fontSize: number }} */\n  let settings = {\n    providers: [],\n    models: [],\n    language: \"en\",\n    fontSize: 13,\n  };\n\n  /** @type {{ agents: any[], activeAgentId: string|null }} */\n  let session = { agents: [], activeAgentId: null };\n\n  /** @type {any} */\n  let selection = null;\n  let mode = \"ask\";\n  let selectedModelId = \"\";\n  let busy = false;\n  /** @type {AbortController|null} */\n  let abortCtrl = null;\n  let lastAssistantText = \"\";\n\n  const i18n = {\n    en: {\n      agents: \"Agents\",\n      settings: \"Settings\",\n      newAgent: \"New chat\",\n      ask: \"Ask\",\n      plan: \"Plan\",\n      send: \"Send\",\n      stop: \"Stop\",\n      copyBrief: \"Copy brief\",\n      providers: \"Providers\",\n      models: \"Models\",\n      language: \"Language\",\n      appearance: \"Appearance\",\n      fontSize: \"Font size\",\n      save: \"Save\",\n      addProvider: \"+ Provider\",\n      addModel: \"+ Model\",\n      noSelection: \"Select a frame or node\",\n      placeholder: \"Ask about the selection…\",\n      emptyChat: \"Select a frame, then ask a design question.\",\n      saved: \"Saved\",\n      briefCopied: \"Brief copied\",\n      needSettings: \"Add a provider and model in Settings.\",\n    },\n    ru: {\n      agents: \"Агенты\",\n      settings: \"Настройки\",\n      newAgent: \"Новый чат\",\n      ask: \"Ask\",\n      plan: \"Plan\",\n      send: \"Отправить\",\n      stop: \"Стоп\",\n      copyBrief: \"Копировать бриф\",\n      providers: \"Провайдеры\",\n      models: \"Модели\",\n      language: \"Язык\",\n      appearance: \"Оформление\",\n      fontSize: \"Размер шрифта\",\n      save: \"Сохранить\",\n      addProvider: \"+ Провайдер\",\n      addModel: \"+ Модель\",\n      noSelection: \"Выберите фрейм или ноду\",\n      placeholder: \"Вопрос по выделению…\",\n      emptyChat: \"Выберите фрейм и задайте вопрос по дизайну.\",\n      saved: \"Сохранено\",\n      briefCopied: \"Бриф скопирован\",\n      needSettings: \"Добавьте провайдера и модель в Настройках.\",\n    },\n  };\n\n  function t(key) {\n    const lang = settings.language === \"ru\" ? \"ru\" : \"en\";\n    return (i18n[lang] && i18n[lang][key]) || i18n.en[key] || key;\n  }\n\n  function setStatus(text) {\n    if (els.statusLine) els.statusLine.textContent = text || \"\";\n  }\n\n  function applyChrome() {\n    document.documentElement.style.setProperty(\n      \"--harbor-font-size\",\n      (settings.fontSize || 13) + \"px\"\n    );\n    if (els.prompt) els.prompt.placeholder = t(\"placeholder\");\n    if (els.modeLabel) {\n      els.modeLabel.textContent = mode === \"plan\" ? t(\"plan\") : t(\"ask\");\n    }\n    const composer = document.getElementById(\"composer\");\n    if (composer) composer.setAttribute(\"data-mode\", mode);\n    document.querySelectorAll(\"[data-i18n]\").forEach(function (node) {\n      const key = node.getAttribute(\"data-i18n\");\n      if (key) node.textContent = t(key);\n    });\n    if (els.fontSizeValue) {\n      els.fontSizeValue.textContent = (settings.fontSize || 13) + \" px\";\n    }\n  }\n\n  function showScreen(name) {\n    if (els.agentsScreen) els.agentsScreen.hidden = name !== \"agents\";\n    if (els.chatScreen) els.chatScreen.hidden = name !== \"chat\";\n    if (els.settingsScreen) els.settingsScreen.hidden = name !== \"settings\";\n  }\n\n  function activeAgent() {\n    return (\n      session.agents.find(function (a) {\n        return a.id === session.activeAgentId;\n      }) || null\n    );\n  }\n\n  function ensureAgent() {\n    if (activeAgent()) return activeAgent();\n    const agent = {\n      id: \"a_\" + F.cryptoToken(10),\n      name: \"Chat\",\n      messages: [],\n      mode: mode,\n      modelId: selectedModelId,\n      createdAt: Date.now(),\n    };\n    session.agents.unshift(agent);\n    session.activeAgentId = agent.id;\n    return agent;\n  }\n\n  function persistSession() {\n    return F.storageSet(F.STORAGE_SESSION, session);\n  }\n\n  function persistSettings() {\n    return F.storageSet(F.STORAGE_SETTINGS, settings);\n  }\n\n  function renderAgents() {\n    if (!els.agentsList) return;\n    els.agentsList.innerHTML = \"\";\n    session.agents.forEach(function (a) {\n      const row = document.createElement(\"button\");\n      row.type = \"button\";\n      row.className =\n        \"agent-row\" + (a.id === session.activeAgentId ? \" active\" : \"\");\n      row.textContent = a.name || \"Chat\";\n      row.addEventListener(\"click\", function () {\n        session.activeAgentId = a.id;\n        mode = a.mode === \"plan\" ? \"plan\" : \"ask\";\n        selectedModelId = a.modelId || selectedModelId;\n        renderChat();\n        renderAgents();\n        showScreen(\"chat\");\n        applyChrome();\n        void persistSession();\n      });\n      els.agentsList.appendChild(row);\n    });\n  }\n\n  function escapeHtml(s) {\n    return String(s)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\");\n  }\n\n  function simpleMarkdown(text) {\n    let html = escapeHtml(text);\n    html = html.replace(/^### (.+)$/gm, \"<h3>$1</h3>\");\n    html = html.replace(/^## (.+)$/gm, \"<h2>$1</h2>\");\n    html = html.replace(/^# (.+)$/gm, \"<h1>$1</h1>\");\n    html = html.replace(/\\*\\*(.+?)\\*\\*/g, \"<strong>$1</strong>\");\n    html = html.replace(/`([^`]+)`/g, \"<code>$1</code>\");\n    html = html.replace(/^\\- (.+)$/gm, \"<li>$1</li>\");\n    html = html.replace(/(<li>.*<\\/li>\\n?)+/g, function (m) {\n      return \"<ul>\" + m + \"</ul>\";\n    });\n    html = html.replace(/\\n\\n/g, \"</p><p>\");\n    html = html.replace(/\\n/g, \"<br>\");\n    return \"<p>\" + html + \"</p>\";\n  }\n\n  function renderChat() {\n    const agent = ensureAgent();\n    if (els.chatAgentName) els.chatAgentName.textContent = agent.name || \"Chat\";\n    if (!els.messages) return;\n    els.messages.innerHTML = \"\";\n    if (!agent.messages.length) {\n      const empty = document.createElement(\"div\");\n      empty.className = \"empty-hint\";\n      empty.textContent = t(\"emptyChat\");\n      els.messages.appendChild(empty);\n      return;\n    }\n    agent.messages.forEach(function (m) {\n      const bubble = document.createElement(\"div\");\n      bubble.className =\n        \"msg \" + (m.role === \"user\" ? \"msg-user\" : \"msg-assistant\");\n      if (m.role === \"assistant\") {\n        bubble.innerHTML = simpleMarkdown(m.text || \"\");\n      } else {\n        bubble.textContent = m.text || \"\";\n      }\n      els.messages.appendChild(bubble);\n    });\n    els.messages.scrollTop = els.messages.scrollHeight;\n    if (els.copyBriefBtn) {\n      els.copyBriefBtn.hidden = mode !== \"plan\" || !lastAssistantText;\n    }\n  }\n\n  function renderSelection() {\n    if (!els.selectionPreview) return;\n    const has = selection && selection.nodes && selection.nodes.length > 0;\n    els.selectionPreview.hidden = !has;\n    if (!has) {\n      if (els.selectionMeta) els.selectionMeta.textContent = t(\"noSelection\");\n      return;\n    }\n    const n = selection.nodes[0];\n    if (els.selectionMeta) {\n      els.selectionMeta.textContent =\n        n.name +\n        \" · \" +\n        n.type +\n        (n.width && n.height ? \" · \" + n.width + \"×\" + n.height : \"\");\n    }\n    if (els.selectionThumb) {\n      if (selection.previewPngDataUrl) {\n        els.selectionThumb.src = selection.previewPngDataUrl;\n        els.selectionThumb.hidden = false;\n      } else {\n        els.selectionThumb.hidden = true;\n      }\n    }\n  }\n\n  function renderModelMenu() {\n    if (!els.modelMenu || !els.modelLabel) return;\n    els.modelMenu.innerHTML = \"\";\n    const models = settings.models || [];\n    if (!selectedModelId && models[0]) selectedModelId = models[0].id;\n    const current = models.find(function (m) {\n      return m.id === selectedModelId;\n    });\n    els.modelLabel.textContent = current\n      ? current.label || current.id\n      : t(\"needSettings\");\n    models.forEach(function (m) {\n      const btn = document.createElement(\"button\");\n      btn.type = \"button\";\n      btn.className = \"menu-item\";\n      btn.role = \"option\";\n      btn.textContent = m.label || m.id;\n      btn.addEventListener(\"click\", function () {\n        selectedModelId = m.id;\n        const agent = activeAgent();\n        if (agent) agent.modelId = m.id;\n        els.modelMenu.hidden = true;\n        renderModelMenu();\n        void persistSession();\n      });\n      els.modelMenu.appendChild(btn);\n    });\n  }\n\n  function renderSettingsForm() {\n    if (els.providersList) {\n      els.providersList.innerHTML = \"\";\n      (settings.providers || []).forEach(function (p, idx) {\n        const card = document.createElement(\"div\");\n        card.className = \"settings-card\";\n        card.innerHTML =\n          '<label>Name<input data-p=\"' +\n          idx +\n          '\" data-f=\"name\" value=\"' +\n          escapeHtml(p.name || \"\") +\n          '\"/></label>' +\n          '<label>Base URL<input data-p=\"' +\n          idx +\n          '\" data-f=\"baseUrl\" value=\"' +\n          escapeHtml(p.baseUrl || \"\") +\n          '\"/></label>' +\n          '<label>API Key<input data-p=\"' +\n          idx +\n          '\" data-f=\"apiKey\" type=\"password\" value=\"' +\n          escapeHtml(p.apiKey || \"\") +\n          '\"/></label>' +\n          '<button type=\"button\" class=\"link-btn\" data-del-p=\"' +\n          idx +\n          '\">Remove</button>';\n        els.providersList.appendChild(card);\n      });\n    }\n    if (els.modelsList) {\n      els.modelsList.innerHTML = \"\";\n      const providerOptions = (settings.providers || [])\n        .map(function (p) {\n          return (\n            '<option value=\"' +\n            escapeHtml(p.id) +\n            '\">' +\n            escapeHtml(p.name || p.id) +\n            \"</option>\"\n          );\n        })\n        .join(\"\");\n      (settings.models || []).forEach(function (m, idx) {\n        const card = document.createElement(\"div\");\n        card.className = \"settings-card\";\n        card.innerHTML =\n          '<label>Model ID<input data-m=\"' +\n          idx +\n          '\" data-f=\"id\" value=\"' +\n          escapeHtml(m.id || \"\") +\n          '\"/></label>' +\n          '<label>Label<input data-m=\"' +\n          idx +\n          '\" data-f=\"label\" value=\"' +\n          escapeHtml(m.label || \"\") +\n          '\"/></label>' +\n          \"<label>Provider<select data-m=\\\"\" +\n          idx +\n          '\" data-f=\"providerId\">' +\n          providerOptions +\n          \"</select></label>\" +\n          '<label class=\"check\"><input type=\"checkbox\" data-m=\"' +\n          idx +\n          '\" data-f=\"supportsVision\"' +\n          (m.supportsVision ? \" checked\" : \"\") +\n          \"/> Vision</label>\" +\n          '<button type=\"button\" class=\"link-btn\" data-del-m=\"' +\n          idx +\n          '\">Remove</button>';\n        const sel = card.querySelector(\"select\");\n        if (sel) sel.value = m.providerId || \"\";\n        els.modelsList.appendChild(card);\n      });\n    }\n    if (els.langSelect) els.langSelect.value = settings.language || \"en\";\n    if (els.fontSizeRange) els.fontSizeRange.value = String(settings.fontSize || 13);\n  }\n\n  function readSettingsForm() {\n    if (els.providersList) {\n      els.providersList.querySelectorAll(\"input[data-p]\").forEach(function (inp) {\n        const idx = Number(inp.getAttribute(\"data-p\"));\n        const f = inp.getAttribute(\"data-f\");\n        if (!settings.providers[idx]) return;\n        settings.providers[idx][f] = inp.value;\n      });\n    }\n    if (els.modelsList) {\n      els.modelsList.querySelectorAll(\"input[data-m], select[data-m]\").forEach(\n        function (inp) {\n          const idx = Number(inp.getAttribute(\"data-m\"));\n          const f = inp.getAttribute(\"data-f\");\n          if (!settings.models[idx]) return;\n          if (inp.type === \"checkbox\") {\n            settings.models[idx][f] = inp.checked;\n          } else {\n            settings.models[idx][f] = inp.value;\n          }\n        }\n      );\n    }\n    if (els.langSelect) settings.language = els.langSelect.value || \"en\";\n    if (els.fontSizeRange) {\n      settings.fontSize = Number(els.fontSizeRange.value) || 13;\n    }\n  }\n\n  async function sendMessage() {\n    if (busy) return;\n    const text = (els.prompt && els.prompt.value.trim()) || \"\";\n    if (!text) return;\n    const model = (settings.models || []).find(function (m) {\n      return m.id === selectedModelId;\n    });\n    const provider =\n      model &&\n      (settings.providers || []).find(function (p) {\n        return p.id === model.providerId;\n      });\n    if (!model || !provider) {\n      setStatus(t(\"needSettings\"));\n      showScreen(\"settings\");\n      renderSettingsForm();\n      return;\n    }\n\n    const agent = ensureAgent();\n    agent.messages.push({ role: \"user\", text: text });\n    if (agent.name === \"Chat\" && text.length > 0) {\n      agent.name = text.slice(0, 40);\n    }\n    agent.mode = mode;\n    agent.modelId = selectedModelId;\n    if (els.prompt) els.prompt.value = \"\";\n    renderChat();\n    renderAgents();\n\n    busy = true;\n    abortCtrl = new AbortController();\n    if (els.sendBtn) els.sendBtn.hidden = true;\n    if (els.stopBtn) els.stopBtn.hidden = false;\n    setStatus(\"…\");\n\n    const history = agent.messages.slice(0, -1).map(function (m) {\n      return { role: m.role, text: m.text };\n    });\n    const assistant = { role: \"assistant\", text: \"\" };\n    agent.messages.push(assistant);\n    renderChat();\n\n    try {\n      const full = await Turn.runFigmaAgentTurn({\n        provider: provider,\n        model: model,\n        mode: mode,\n        userText: text,\n        history: history,\n        selection: selection,\n        signal: abortCtrl.signal,\n        onDelta: function (piece) {\n          assistant.text += piece;\n          lastAssistantText = assistant.text;\n          renderChat();\n        },\n      });\n      assistant.text = full || assistant.text;\n      lastAssistantText = assistant.text;\n      setStatus(\"\");\n    } catch (err) {\n      if (err && err.name === \"AbortError\") {\n        setStatus(\"Stopped\");\n      } else {\n        assistant.text =\n          assistant.text ||\n          \"Error: \" + ((err && err.message) || String(err));\n        setStatus(assistant.text.slice(0, 120));\n      }\n      lastAssistantText = assistant.text;\n    }\n\n    busy = false;\n    abortCtrl = null;\n    if (els.sendBtn) els.sendBtn.hidden = false;\n    if (els.stopBtn) els.stopBtn.hidden = true;\n    renderChat();\n    void persistSession();\n  }\n\n  function onHostMessage(msg) {\n    if (!msg || typeof msg !== \"object\") return;\n    if (msg.type === \"figmaSelectionChanged\") {\n      selection = msg.selection || null;\n      renderSelection();\n      return;\n    }\n    if (msg.type === \"figmaClientStorageValue\") {\n      const resolve = F.pendingStorage.get(msg.requestId);\n      if (resolve) {\n        F.pendingStorage.delete(msg.requestId);\n        resolve(msg.value);\n      }\n      return;\n    }\n    if (msg.type === \"figmaClientStorageSaved\") {\n      const resolve = F.pendingStorage.get(msg.requestId);\n      if (resolve) {\n        F.pendingStorage.delete(msg.requestId);\n        resolve();\n      }\n      return;\n    }\n    if (msg.type === \"copied\") {\n      setStatus(t(\"briefCopied\"));\n    }\n  }\n\n  window.onmessage = function (event) {\n    const data = event.data;\n    const msg =\n      data && typeof data === \"object\" && \"pluginMessage\" in data\n        ? data.pluginMessage\n        : data;\n    onHostMessage(msg);\n  };\n\n  // Events\n  if (els.sendBtn) els.sendBtn.addEventListener(\"click\", function () {\n    void sendMessage();\n  });\n  if (els.stopBtn) {\n    els.stopBtn.addEventListener(\"click\", function () {\n      if (abortCtrl) abortCtrl.abort();\n    });\n  }\n  if (els.prompt) {\n    els.prompt.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        void sendMessage();\n      }\n    });\n  }\n  if (els.modeTrigger && els.modeMenu) {\n    els.modeTrigger.addEventListener(\"click\", function () {\n      els.modeMenu.hidden = !els.modeMenu.hidden;\n    });\n    els.modeMenu.innerHTML = \"\";\n    [\"ask\", \"plan\"].forEach(function (m) {\n      const btn = document.createElement(\"button\");\n      btn.type = \"button\";\n      btn.className = \"menu-item\";\n      btn.textContent = m === \"plan\" ? \"Plan\" : \"Ask\";\n      btn.addEventListener(\"click\", function () {\n        mode = m;\n        const agent = activeAgent();\n        if (agent) agent.mode = m;\n        els.modeMenu.hidden = true;\n        applyChrome();\n        renderChat();\n        void persistSession();\n      });\n      els.modeMenu.appendChild(btn);\n    });\n  }\n  if (els.modelTrigger && els.modelMenu) {\n    els.modelTrigger.addEventListener(\"click\", function () {\n      els.modelMenu.hidden = !els.modelMenu.hidden;\n      renderModelMenu();\n    });\n  }\n  if (els.toggleAgentsRailBtn) {\n    els.toggleAgentsRailBtn.addEventListener(\"click\", function () {\n      const open = els.agentsScreen && els.agentsScreen.hidden;\n      showScreen(open ? \"agents\" : \"chat\");\n      renderAgents();\n    });\n  }\n  if (els.newAgentBtn) {\n    els.newAgentBtn.addEventListener(\"click\", function () {\n      session.activeAgentId = null;\n      ensureAgent();\n      lastAssistantText = \"\";\n      renderAgents();\n      renderChat();\n      showScreen(\"chat\");\n      void persistSession();\n    });\n  }\n  if (els.openSettingsBtn) {\n    els.openSettingsBtn.addEventListener(\"click\", function () {\n      renderSettingsForm();\n      showScreen(\"settings\");\n    });\n  }\n  if (els.closeSettingsBtn) {\n    els.closeSettingsBtn.addEventListener(\"click\", function () {\n      showScreen(\"chat\");\n    });\n  }\n  if (els.addProviderBtn) {\n    els.addProviderBtn.addEventListener(\"click\", function () {\n      readSettingsForm();\n      settings.providers.push({\n        id: \"p_\" + F.cryptoToken(6),\n        name: \"Provider\",\n        baseUrl: \"https://api.openai.com/v1\",\n        apiKey: \"\",\n      });\n      renderSettingsForm();\n    });\n  }\n  if (els.addModelBtn) {\n    els.addModelBtn.addEventListener(\"click\", function () {\n      readSettingsForm();\n      const p0 = settings.providers[0];\n      settings.models.push({\n        id: \"gpt-4o-mini\",\n        label: \"gpt-4o-mini\",\n        providerId: p0 ? p0.id : \"\",\n        supportsVision: false,\n      });\n      renderSettingsForm();\n    });\n  }\n  if (els.providersList) {\n    els.providersList.addEventListener(\"click\", function (e) {\n      const tEl = e.target;\n      if (!(tEl instanceof HTMLElement)) return;\n      const del = tEl.getAttribute(\"data-del-p\");\n      if (del != null) {\n        readSettingsForm();\n        settings.providers.splice(Number(del), 1);\n        renderSettingsForm();\n      }\n    });\n  }\n  if (els.modelsList) {\n    els.modelsList.addEventListener(\"click\", function (e) {\n      const tEl = e.target;\n      if (!(tEl instanceof HTMLElement)) return;\n      const del = tEl.getAttribute(\"data-del-m\");\n      if (del != null) {\n        readSettingsForm();\n        settings.models.splice(Number(del), 1);\n        renderSettingsForm();\n      }\n    });\n  }\n  if (els.saveSettingsBtn) {\n    els.saveSettingsBtn.addEventListener(\"click\", function () {\n      readSettingsForm();\n      void persistSettings().then(function () {\n        applyChrome();\n        renderModelMenu();\n        setStatus(t(\"saved\"));\n        showScreen(\"chat\");\n      });\n    });\n  }\n  if (els.fontSizeRange) {\n    els.fontSizeRange.addEventListener(\"input\", function () {\n      settings.fontSize = Number(els.fontSizeRange.value) || 13;\n      applyChrome();\n    });\n  }\n  if (els.copyBriefBtn) {\n    els.copyBriefBtn.addEventListener(\"click\", function () {\n      const brief = Turn.buildHandoffBrief(lastAssistantText, selection);\n      void navigator.clipboard.writeText(brief).then(\n        function () {\n          host.postMessage({ type: \"copyBrief\", text: brief });\n          setStatus(t(\"briefCopied\"));\n        },\n        function () {\n          host.postMessage({ type: \"copyBrief\", text: brief });\n        }\n      );\n    });\n  }\n\n  async function boot() {\n    const storedSettings = await F.storageGet(F.STORAGE_SETTINGS);\n    if (storedSettings && typeof storedSettings === \"object\") {\n      settings = Object.assign(settings, storedSettings);\n    }\n    const storedSession = await F.storageGet(F.STORAGE_SESSION);\n    if (storedSession && typeof storedSession === \"object\") {\n      session = Object.assign(session, storedSession);\n      if (!Array.isArray(session.agents)) session.agents = [];\n    }\n    ensureAgent();\n    const agent = activeAgent();\n    if (agent) {\n      mode = agent.mode === \"plan\" ? \"plan\" : \"ask\";\n      selectedModelId = agent.modelId || selectedModelId;\n    }\n    applyChrome();\n    renderModelMenu();\n    renderAgents();\n    renderChat();\n    renderSelection();\n    showScreen(\"chat\");\n    host.postMessage({ type: \"ready\", surface: \"figma\" });\n    host.postMessage({ type: \"figmaGetSelection\" });\n  }\n\n  void boot();\n})();\n\n<\\/script>\n  </body>\n</html>\n";

"use strict";
(() => {
  // figma/src/selection.ts
  var MAX_DEPTH = 4;
  var MAX_CHILDREN = 24;
  function serializeNode(node, depth) {
    const box = node.absoluteBoundingBox;
    const out = {
      id: node.id,
      name: node.name,
      type: node.type
    };
    if (box) {
      out.width = Math.round(box.width);
      out.height = Math.round(box.height);
    }
    if (depth < MAX_DEPTH && "children" in node) {
      const kids = node.children;
      if (kids && kids.length > 0) {
        out.children = kids.slice(0, MAX_CHILDREN).map((c) => serializeNode(c, depth + 1));
        if (kids.length > MAX_CHILDREN) {
          out.children.push({
            id: `${node.id}__more`,
            name: `\u2026 +${kids.length - MAX_CHILDREN} more`,
            type: "TRUNCATED"
          });
        }
      }
    }
    return out;
  }
  function bytesToBase64(bytes) {
    const chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
    let out = "";
    for (let i = 0; i < bytes.length; i += 3) {
      const a = bytes[i];
      const b = i + 1 < bytes.length ? bytes[i + 1] : 0;
      const c = i + 2 < bytes.length ? bytes[i + 2] : 0;
      const triple = a << 16 | b << 8 | c;
      out += chars[triple >> 18 & 63];
      out += chars[triple >> 12 & 63];
      out += i + 1 < bytes.length ? chars[triple >> 6 & 63] : "=";
      out += i + 2 < bytes.length ? chars[triple & 63] : "=";
    }
    return out;
  }
  function nodeDeepLink(fileKey, nodeId) {
    if (!fileKey) return void 0;
    const nid = nodeId.replace(/:/g, "-");
    return `https://www.figma.com/design/${fileKey}?node-id=${encodeURIComponent(nid)}`;
  }
  async function captureSelection() {
    const selection = figma.currentPage.selection;
    const fileKey = figma.fileKey;
    const nodes = selection.map((n) => serializeNode(n, 0));
    const primary = selection[0];
    let previewPngDataUrl;
    if (primary) {
      try {
        const bytes = await primary.exportAsync({
          format: "PNG",
          constraint: { type: "SCALE", value: 1 }
        });
        previewPngDataUrl = `data:image/png;base64,${bytesToBase64(bytes)}`;
      } catch {
        previewPngDataUrl = void 0;
      }
    }
    return {
      fileKey: fileKey || void 0,
      fileName: figma.root.name,
      pageName: figma.currentPage.name,
      nodes,
      previewPngDataUrl,
      nodeUrl: primary ? nodeDeepLink(fileKey, primary.id) : void 0
    };
  }

  // figma/src/storageKeys.ts
  var FIGMA_STORAGE_PREFIX = "harbor.figma.";
  var FIGMA_STORAGE_SETTINGS = `${FIGMA_STORAGE_PREFIX}settings`;
  var FIGMA_STORAGE_SESSION = `${FIGMA_STORAGE_PREFIX}session`;
  var FIGMA_STORAGE_UI_STATE = `${FIGMA_STORAGE_PREFIX}uiState`;

  // figma/src/main.ts
  figma.notify("Harbor Agents");
  try {
    if (typeof __HTML_UI__ !== "string" || __HTML_UI__.length < 32) {
      throw new Error("UI HTML missing \u2014 run npm run build:figma");
    }
    figma.showUI(__HTML_UI__, {
      width: 420,
      height: 640,
      title: "Harbor Agents"
    });
  } catch (err) {
    const message = err instanceof Error ? err.message : String(err);
    figma.notify("Harbor UI failed: " + message, { error: true });
    figma.closePlugin();
  }
  function postToUi(msg) {
    figma.ui.postMessage(msg);
  }
  async function pushSelection(requestId) {
    try {
      const selection = await captureSelection();
      postToUi({
        type: "figmaSelectionChanged",
        selection,
        requestId
      });
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      figma.notify("Selection sync failed: " + message, { error: true });
    }
  }
  async function handleMessage(raw) {
    if (!raw || typeof raw !== "object") return;
    const msg = raw;
    try {
      switch (msg.type) {
        case "ready":
          await pushSelection();
          break;
        case "figmaGetSelection":
          await pushSelection(msg.requestId);
          break;
        case "figmaClientStorageGet": {
          const value = await figma.clientStorage.getAsync(msg.key);
          postToUi({
            type: "figmaClientStorageValue",
            key: msg.key,
            value: value ?? null,
            requestId: msg.requestId
          });
          break;
        }
        case "figmaClientStorageSet": {
          const allowed = msg.key === FIGMA_STORAGE_SETTINGS || msg.key === FIGMA_STORAGE_SESSION || msg.key === FIGMA_STORAGE_UI_STATE || typeof msg.key === "string" && msg.key.startsWith("harbor.figma.");
          if (!allowed) {
            figma.notify("Blocked storage key (not harbor.figma.*)", {
              error: true
            });
            break;
          }
          await figma.clientStorage.setAsync(msg.key, msg.value);
          postToUi({
            type: "figmaClientStorageSaved",
            key: msg.key,
            requestId: msg.requestId
          });
          break;
        }
        case "copyBrief":
        case "copyText":
          postToUi({ type: "copied" });
          figma.notify(msg.type === "copyBrief" ? "Brief copied" : "Copied");
          break;
        case "notify":
          figma.notify(msg.message, { error: !!msg.error });
          break;
        case "resize":
          figma.ui.resize(
            Math.max(320, Math.min(900, msg.width | 0)),
            Math.max(400, Math.min(900, msg.height | 0))
          );
          break;
        default:
          break;
      }
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      figma.notify("Harbor error: " + message, { error: true });
    }
  }
  figma.ui.onmessage = (msg) => {
    void handleMessage(msg);
  };
  figma.on("selectionchange", () => {
    void pushSelection();
  });
  figma.on("currentpagechange", () => {
    void pushSelection();
  });
})();
