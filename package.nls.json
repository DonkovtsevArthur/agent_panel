{
  "displayName": "Harbor Agents",
  "description": "Local agent panel for VS Code. Bring your own OpenAI-compatible API — chats stay on your machine, no Harbor backend. Generate SCM commit messages from your model.",
  "viewsContainer.title": "Harbor Agents",
  "view.name": "Agents",
  "command.category": "Harbor Agents",
  "command.open.title": "Open",
  "command.openSettings.title": "Open Settings",
  "command.newAgent.title": "New Agent",
  "command.searchChat.title": "Search Chat",
  "command.clearChat.title": "New Agent",
  "command.attachFile.title": "Attach File",
  "command.addSelection.title": "Add Selection to Chat Harbor",
  "command.addSelectionToNewChat.title": "Add Selection to New Chat Harbor",
  "command.addFile.title": "Add File to Chat Harbor",
  "command.addFileToNewChat.title": "Add File to New Chat Harbor",
  "command.generateCommitMessage.title": "Generate Commit Message",
  "config.title": "Harbor Agents",
  "config.language.description": "Plugin UI language. Auto follows the VS Code display language.",
  "config.language.enum.auto": "Follow VS Code language",
  "config.language.enum.en": "English",
  "config.language.enum.ru": "Russian",
  "config.fontSize.description": "Panel font size in pixels (chat messages and composer).",
  "config.baseUrl.description": "Legacy mirror of the primary provider baseUrl (prefer agentPanel.providers)",
  "config.apiKey.description": "Legacy mirror of the primary provider apiKey (prefer agentPanel.providers). Do not commit keys to git.",
  "config.providers.description": "OpenAI-compatible providers (base URL + API key). If empty, built from legacy baseUrl/apiKey.",
  "config.providers.id.description": "Stable provider id (for example: default, zai, kimi)",
  "config.providers.name.description": "Display name",
  "config.providers.baseUrl.description": "Base URL without trailing slash",
  "config.providers.statusUrl.description": "Optional connection check URL (GET). Empty = Base URL + /models",
  "config.providers.apiKey.description": "API key for this provider",
  "config.providers.promptCache.description": "Emit Anthropic-style cache_control prompt-cache markers for this provider (overrides the global agentPanel.promptCache.enabled). Turn on only for upstreams that accept them (LiteLLM / OpenRouter with Claude|Qwen, Anthropic-compatible endpoints); strict OpenAI rejects the marker with 400.",
  "config.providers.protocol.description": "Wire protocol: \"openai-compatible\" (default, Chat Completions /chat/completions) or \"anthropic\" (Messages API /v1/messages). Set to \"anthropic\" for endpoints that speak the Anthropic wire format.",
  "config.models.description": "List of models available in the agent panel (id as in /openai/models)",
  "config.models.id.description": "Model id sent to the API",
  "config.models.label.description": "Display name in the selector",
  "config.models.providerId.description": "Provider id from agentPanel.providers",
  "config.models.contextWindow.description": "Model context window in tokens (max input). Also accepts max_input_tokens, maxInputTokens, context_length, and similar aliases.",
  "config.models.maxOutputTokens.description": "Model max output tokens. Also accepts max_output_tokens, max_completion_tokens, and similar aliases.",
  "config.models.enabled.description": "Show model in the chat selector",
  "config.models.favorite.description": "Favorite model shown higher in the chat selector",
  "config.models.supportsVision.description": "Model accepts images (vision). If omitted, inferred from id",
  "config.models.reasoningEffort.description": "Default reasoning effort for thinking models (low, medium, high, xhigh). Empty = auto from model capabilities.",
  "config.defaultModel.description": "Default model (must match an id from agentPanel.models)",
  "config.defaultContextWindow.description": "Default context window in tokens when a model has no contextWindow",
  "config.systemPrompt.description": "System prompt for the agent",
  "config.modes.description": "Custom composer modes (Agent/Plan/Ask are built in). Each item: { id, label, description?, tools: \"agent\"|\"readonly\", prompt?, enabled? }",
  "config.visionRouting.preferredModelIds.description": "Preferred vision models when the chat model cannot view images (and for Figma screenshots). First enabled match wins.",
  "config.visionRouting.preferredModelId.description": "Deprecated. Use agentPanel.visionRouting.preferredModelIds (also unused for chat turns).",
  "config.soundNotifications.enabled.description": "Play a sound when an agent run succeeds or fails.",
  "config.subagents.enabled.description": "Allow parallel sub-agents (spawn_agent) in Agent, Plan, and Ask. When on, the model is instructed to delegate independent work via spawn_agent; when off, the tool is unavailable. Children follow the current mode: Plan/Ask stay read-focused; Agent may edit.",
  "config.parallelToolCalls.enabled.description": "Execute multiple tool calls from one model response at the same time (reads, searches, and other independent tools).",
  "config.autoCompact.enabled.description": "Automatically compress conversation context when it approaches the model input limit.",
  "config.sessions.idleEvictMinutes.description": "Minutes an agent session may stay idle before it is stopped in the background; the next message in that chat starts a fresh session from saved history. Higher values keep sessions warm (faster first token) at the cost of memory.",
  "config.sessions.inactivityTimeoutMinutes.description": "Minutes to wait for agent activity before auto-aborting the turn. Counts only silent gaps while waiting on the model — the timer pauses while tools are running (shell, MCP, sub-agents). Prevents indefinite hangs when a provider stalls. Set higher for slow providers.",
  "config.promptCache.enabled.description": "Send Anthropic-style prompt-cache markers (cache_control) on chat requests to OpenAI-compatible providers. Enable only when your endpoint accepts them (LiteLLM/OpenRouter proxying Claude or Qwen, Anthropic-compatible endpoints): cached prompt prefixes become several times cheaper and replies start faster. Strict OpenAI endpoints may reject the marker with a 400 error — keep it off there.",
  "config.tools.autoApprove.description": "Run agent tools without asking. Off — confirm each tool (edit, terminal, MCP) before it runs.",
  "config.tools.approvals.description": "Per-group auto-approve overrides. Each group (reads, web, edits, commands, mcp, subagents) can force auto-approve (true) or prompting (false) regardless of the master toggle; unset = follow agentPanel.tools.autoApprove.",
  "config.tools.approvals.reads.description": "Auto-approve read-only tools (read_files, search_codebase, skills). Unset = follow the master toggle.",
  "config.tools.approvals.web.description": "Auto-approve fetch_web_content. Unset = follow the master toggle.",
  "config.tools.approvals.edits.description": "Auto-approve file edits (editor, apply_patch). Unset = follow the master toggle.",
  "config.tools.approvals.commands.description": "Auto-approve shell commands (run_commands). Unset = follow the master toggle.",
  "config.tools.approvals.mcp.description": "Auto-approve MCP tools (Figma and custom MCP servers). Unset = follow the master toggle.",
  "config.tools.approvals.subagents.description": "Auto-approve spawn_agent. Unset = follow the master toggle.",
  "config.tools.approvals.plan.description": "Auto-approve plan tools (update_todo, inspect_images). Unset = follow the master toggle.",
  "config.turnContext.followUps.description": "How much IDE context (editor state, git, diagnostics, active file, rules, terminal) to re-send on follow-up turns of the same chat. The first turn always gets the full block; old blocks stay in the session history, which inflates long chats.",
  "config.turnContext.followUps.full.description": "Full block on every turn (legacy behavior).",
  "config.turnContext.followUps.slim.description": "Slim block on follow-ups: keep editor state, git, diagnostics and the enclosing symbol; drop the active-file prefetch, workspace rules, terminal snapshot and recently viewed files.",
  "config.turnContext.followUps.none.description": "No IDE context block on follow-up turns.",
  "config.checkpoints.enabled.description": "Save a git checkpoint at the start of each agent run so you can restore workspace files from the chat.",
  "config.skills.enabled.description": "Allow Agent Skills (SKILL.md) from Harbor skill folders. When off, the skills tool is unavailable.",
  "config.skills.workspaceEnabled.description": "Scan <workspace>/.harbor/skills for skills.",
  "config.skills.globalEnabled.description": "Scan ~/.harbor/skills for skills.",
  "config.skills.extraDirectories.description": "Extra absolute directories to scan for skills (in addition to Harbor workspace and global folders).",
  "config.skills.disabledExtraDirectories.description": "Extra skill directories that are toggled off in Settings.",
  "config.skills.disabled.description": "Skill names to exclude from the skills tool (frontmatter name or directory name).",
  "config.selectionHints.enabled.description": "Show floating Add to Chat actions above the selected code in the editor.",
  "config.rejectUnauthorized.description": "Validate TLS certificates. On (default) = strict verify using the system trust store. Off accepts self-signed / MITM proxy certificates (insecure; opt-in for corporate gateways without a CA bundle).",
  "config.commitMessage.prompt.description": "Custom rule/prompt for SCM commit message generation. Empty = use project rules, then the built-in default.",
  "config.commitMessage.modelIds.description": "Model ids from agentPanel.models for commit message generation. First available model is used; if one doesn't respond in 10s, the next is tried. Empty = automatic light/utility model.",
  "config.commitMessage.language.description": "Language for generated commit messages",
  "config.commitMessage.language.enum.auto": "Follow plugin UI language",
  "config.commitMessage.language.enum.en": "English",
  "config.commitMessage.language.enum.ru": "Russian",
  "config.figma.enabled.description": "Enable Figma MCP tools in Harbor Agents (Connect in panel Settings → MCP Servers)",
  "config.autoglm.enabled.description": "Enable browser_task (AutoGLM) for multi-step work in the user's Chrome/Edge. Requires AutoGLM CLI and browser extension.",
  "config.autoglm.binaryPath.description": "Optional path to the autoglm binary. Empty = PATH, then ~/.agents/skills/autoglm-browser-agent/dist/autoglm-browser-service.",
  "config.autoglm.browser.description": "Browser for AutoGLM tasks (chrome or edge)",
  "config.autoglm.autoApprove.description": "Auto-approve sensitive AutoGLM actions (login/captcha still require the user)",
  "config.mcp.servers.description": "Custom MCP servers (stdio or HTTP). Manage them in Harbor Agents Settings → MCP Servers."
}
